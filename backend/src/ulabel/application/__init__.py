"""Application layer containing use cases for the uLabel platform.

Each module in this package implements a single use case that orchestrates
domain entities and repository ports to fulfill a business workflow.
"""

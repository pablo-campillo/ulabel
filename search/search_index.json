{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"uLabel API Documentation","text":"<p>Welcome to the uLabel documentation \u2014 a backend API for an image labeling and annotation platform.</p>"},{"location":"#overview","title":"Overview","text":"<p>uLabel provides a REST API built with FastAPI that enables:</p> <ul> <li>Project management: Create and configure labeling projects with custom categories.</li> <li>Image handling: Upload, import from S3/MinIO storage, and manage images within projects.</li> <li>Labeling workflow: Assign images to labelers and collect annotations.</li> <li>Export: Export labeled data for downstream use.</li> <li>Observability: Built-in tracing, metrics, and structured logging via OpenTelemetry.</li> </ul>"},{"location":"#documentation-structure","title":"Documentation Structure","text":"<p>This documentation follows the Diataxis framework:</p> Section Purpose Tutorials Step-by-step guides to get started How-To Guides Recipes for common tasks Reference Auto-generated API reference from source code Explanation Background and architecture decisions"},{"location":"#quick-start","title":"Quick Start","text":"<pre><code># Install dependencies\nuv sync\n\n# Run the API\nmake run\n</code></pre> <p>See the Getting Started tutorial for a complete walkthrough.</p>"},{"location":"explanation/api-design/","title":"API Design","text":"<p>uLabel exposes a REST API built with FastAPI. This page explains the endpoint design, schema conventions, error handling strategy, and middleware architecture.</p>"},{"location":"explanation/api-design/#endpoint-map","title":"Endpoint Map","text":"<p>All endpoints live under the <code>/v1</code> prefix.</p>"},{"location":"explanation/api-design/#authentication","title":"Authentication","text":"Method Path Purpose <code>POST</code> <code>/v1/token</code> Sign in by username, returns a <code>Claim</code> with user ID and role"},{"location":"explanation/api-design/#projects","title":"Projects","text":"Method Path Purpose <code>GET</code> <code>/v1/projects</code> List projects with pagination and optional name filter <code>POST</code> <code>/v1/projects</code> Create a new labeling project (admin only) <code>PATCH</code> <code>/v1/projects/{project_id}</code> Update project name, description, or labelers <code>POST</code> <code>/v1/projects/{project_id}/labelers</code> Add a labeler to a project"},{"location":"explanation/api-design/#images-labeling","title":"Images &amp; Labeling","text":"Method Path Purpose <code>POST</code> <code>/v1/projects/{project_id}/images</code> Register an existing storage object as a project image <code>POST</code> <code>/v1/projects/{project_id}/images/upload</code> Upload an image file directly <code>POST</code> <code>/v1/projects/{project_id}/images/import</code> Start async bulk import from a storage prefix <code>GET</code> <code>/v1/projects/{project_id}/images/imports/{import_id}</code> Poll bulk import job status <code>POST</code> <code>/v1/projects/{project_id}/images/{image_id}/label</code> Submit a label for an assigned image"},{"location":"explanation/api-design/#assignments","title":"Assignments","text":"Method Path Purpose <code>POST</code> <code>/v1/projects/{project_id}/assignments</code> Get next pending image with a presigned URL (expiry from <code>config.yml</code>)"},{"location":"explanation/api-design/#exports","title":"Exports","text":"Method Path Purpose <code>GET</code> <code>/v1/projects/{project_id}/export</code> Export labels as JSON or CSV (redirects to presigned URL)"},{"location":"explanation/api-design/#statistics","title":"Statistics","text":"Method Path Purpose <code>GET</code> <code>/v1/projects/{project_id}/stats</code> Get project statistics (counts, distribution, daily activity)"},{"location":"explanation/api-design/#labelers","title":"Labelers","text":"Method Path Purpose <code>GET</code> <code>/v1/labelers/autocomplete</code> Search labelers by username prefix <code>GET</code> <code>/v1/labelers/{labeler_id}/projects</code> List projects assigned to a labeler"},{"location":"explanation/api-design/#schema-design","title":"Schema Design","text":""},{"location":"explanation/api-design/#request-and-response-separation","title":"Request and Response Separation","text":"<p>Every endpoint has distinct request and response schemas. This keeps the API contract explicit and avoids leaking internal fields:</p> <pre><code>class CreateProjectRequest(BaseModel):\n    owner_id: UUID\n    name: str\n    description: str\n    labels: set[str]\n\nclass ProjectResponse(BaseModel):\n    id: UUID\n    owner_id: UUID\n    name: str\n    description: str\n    labels: set[str]\n    labelers: list[LabelerInfo]\n    created_at: datetime | None\n</code></pre>"},{"location":"explanation/api-design/#generic-pagination","title":"Generic Pagination","text":"<p>Paginated list endpoints use a generic wrapper:</p> <pre><code>class PaginatedResponse(BaseModel, Generic[T]):\n    items: list[T]\n    total: int\n    limit: int\n    offset: int\n</code></pre> <p>This is reused for project listing and any future paginated resource. Query parameters <code>limit</code> and <code>offset</code> control pagination, with an optional <code>name</code> filter for search.</p>"},{"location":"explanation/api-design/#pydantic-v2-with-orjson","title":"Pydantic v2 with orjson","text":"<p>All schemas use Pydantic v2 <code>BaseModel</code> with <code>model_config = ConfigDict(from_attributes=True)</code> where needed. FastAPI is configured with <code>orjson</code> for high-performance JSON serialization, which is particularly beneficial for large export payloads and statistics responses.</p>"},{"location":"explanation/api-design/#error-handling","title":"Error Handling","text":""},{"location":"explanation/api-design/#domain-error-mapping","title":"Domain Error Mapping","text":"<p>A single global exception handler maps every <code>DomainError</code> subclass to its HTTP response:</p> Domain Error HTTP Status Code <code>ProjectNotFound</code> 404 <code>PROJECT_NOT_FOUND</code> <code>UserNotFound</code> 404 <code>USER_NOT_FOUND</code> <code>ImageNotFound</code> 404 <code>IMAGE_NOT_FOUND</code> <code>NoLabelsFound</code> 404 <code>NO_LABELS_FOUND</code> <code>ImportJobNotFound</code> 404 <code>IMPORT_JOB_NOT_FOUND</code> <code>Unauthorized</code> 403 <code>UNAUTHORIZED</code> <code>LabelerNotInProject</code> 403 <code>LABELER_NOT_IN_PROJECT</code> <code>LabelerMismatch</code> 403 <code>LABELER_MISMATCH</code> <code>ProjectNameAlreadyExists</code> 409 <code>PROJECT_NAME_ALREADY_EXISTS</code> <code>ImageNotInProgress</code> 409 <code>IMAGE_NOT_IN_PROGRESS</code> <code>AssignmentMismatch</code> 409 <code>ASSIGNMENT_MISMATCH</code> <code>InvalidLabel</code> 422 <code>INVALID_LABEL</code> <code>NoImageAvailable</code> 204 (no content)"},{"location":"explanation/api-design/#uniform-error-response","title":"Uniform Error Response","text":"<p>All errors return the same JSON structure:</p> <pre><code>{\n  \"error\": {\n    \"code\": \"PROJECT_NOT_FOUND\",\n    \"message\": \"Project not found\",\n    \"details\": null\n  }\n}\n</code></pre> <p>This makes it easy for the frontend to parse errors consistently. The handler also logs each domain error and increments the <code>domain_errors_total</code> Prometheus metric.</p>"},{"location":"explanation/api-design/#authentication-model","title":"Authentication Model","text":"<p>uLabel uses a simplified username-based authentication:</p> <ol> <li>The frontend sends <code>POST /v1/token</code> with a username.</li> <li>The backend looks up or creates the user and returns a <code>Claim</code> containing the user's <code>id</code>, <code>username</code>, and <code>role</code>.</li> <li>Subsequent requests include the user ID in request bodies (e.g., <code>labeler_id</code>, <code>owner_id</code>).</li> </ol> <p>Roles determine access:</p> <ul> <li>Admin: Create and manage projects, upload images, configure labelers.</li> <li>Labeler: View assigned projects, receive image assignments, submit labels.</li> </ul> <p>Authorization is enforced at the use case level \u2014 routers pass the user ID, and use cases validate permissions against the domain (e.g., checking that a labeler belongs to the project).</p>"},{"location":"explanation/api-design/#middleware","title":"Middleware","text":""},{"location":"explanation/api-design/#prometheus-metrics","title":"Prometheus Metrics","text":"<p>The <code>PrometheusMiddleware</code> wraps every request and collects:</p> <ul> <li><code>http_requests_total</code> \u2014 Counter by method, path template, and status code.</li> <li><code>http_request_duration_seconds</code> \u2014 Histogram with buckets from 5ms to 10s.</li> <li><code>http_requests_in_progress</code> \u2014 Gauge for concurrent requests.</li> <li><code>http_exceptions_total</code> \u2014 Counter for unhandled exceptions.</li> </ul> <p>Path labels use route templates (<code>/v1/projects/{project_id}/images</code>) rather than actual URLs to prevent cardinality explosion from UUIDs.</p> <p>Each metric observation includes an exemplar linking to the current trace ID, enabling one-click navigation from a Grafana metric panel to the corresponding trace in Tempo.</p>"},{"location":"explanation/api-design/#versioning","title":"Versioning","text":"<p>The API uses a <code>/v1</code> prefix on all routes, applied by mounting the feature routers under a versioned <code>APIRouter</code>. This allows future breaking changes to coexist under <code>/v2</code> without disrupting existing clients.</p>"},{"location":"explanation/api-design/#router-organization","title":"Router Organization","text":"<p>Routers are organized by domain resource:</p> <pre><code>api/routers/\n\u251c\u2500\u2500 tokens.py        # Authentication\n\u251c\u2500\u2500 projects.py      # Project CRUD + labeler management\n\u251c\u2500\u2500 images.py        # Image upload, import, label submission\n\u251c\u2500\u2500 assignments.py   # Image assignment to labelers\n\u251c\u2500\u2500 exports.py       # Label data export\n\u251c\u2500\u2500 stats.py         # Project statistics\n\u2514\u2500\u2500 labelers.py      # Labeler search and project listing\n</code></pre> <p>Each router is a FastAPI <code>APIRouter</code> with appropriate <code>prefix</code> and <code>tags</code>. They are aggregated in <code>api.py</code> and mounted on the main app in <code>main.py</code>.</p>"},{"location":"explanation/api-design/#dependency-injection-in-routers","title":"Dependency Injection in Routers","text":"<p>Route handlers receive use cases via <code>dependency-injector</code>'s <code>@inject</code> decorator:</p> <pre><code>@router.post(\"/{project_id}/assignments\")\n@inject\nasync def create_assignment(\n    project_id: UUID,\n    body: CreateAssignmentRequest,\n    use_case: CreateAssignmentUseCase = Depends(\n        Provide[Container.create_assignment_use_case]\n    ),\n    storage: StorageService = Depends(Provide[Container.storage_service]),\n):\n    image = await use_case.execute(project_id, body.labeler_id)\n    ...\n</code></pre> <p>This keeps handlers thin \u2014 they parse HTTP, call the use case, and format the response. All business logic lives in the application layer.</p>"},{"location":"explanation/architecture/","title":"Architecture","text":"<p>uLabel is an image labeling platform composed of multiple services working together. This page explains the overall platform design and the internal architecture of the backend.</p>"},{"location":"explanation/architecture/#platform-overview","title":"Platform Overview","text":"<pre><code>graph TB\n    subgraph Client\n        FE[React Frontend&lt;br/&gt;Vite + TypeScript]\n    end\n\n    subgraph Backend\n        API[FastAPI Application&lt;br/&gt;uvicorn + uvloop]\n    end\n\n    subgraph Data\n        DB[(PostgreSQL 16)]\n        S3[(MinIO / S3&lt;br/&gt;Object Storage)]\n    end\n\n    subgraph Observability\n        Prometheus[(Prometheus)]\n        Tempo[(Tempo)]\n        Loki[(Loki)]\n        Promtail[Promtail]\n        Grafana[Grafana]\n    end\n\n    FE --&gt;|REST API /v1| API\n    API --&gt;|asyncpg| DB\n    API --&gt;|aioboto3| S3\n    API --&gt;|OTLP gRPC| Tempo\n    API --&gt;|stdout JSON| Promtail\n    Promtail --&gt; Loki\n    Prometheus --&gt;|scrape /metrics| API\n    Tempo --&gt; Grafana\n    Loki --&gt; Grafana\n    Prometheus --&gt; Grafana</code></pre> Component Technology Purpose Frontend React 18 + Vite + TypeScript Labeling UI, admin dashboard, statistics Backend FastAPI + Python 3.12 REST API, business logic, background tasks Database PostgreSQL 16 (asyncpg) Persistent storage for projects, images, labels, users Object Storage MinIO (S3-compatible) Image file storage with presigned URL access Observability Prometheus, Tempo, Loki, Grafana Metrics, traces, logs, dashboards <p>The frontend communicates with the backend exclusively through the REST API under the <code>/v1</code> prefix. The backend owns all state: relational data lives in PostgreSQL and binary objects (images, exports) live in MinIO.</p>"},{"location":"explanation/architecture/#hexagonal-architecture-ports-adapters","title":"Hexagonal Architecture (Ports &amp; Adapters)","text":"<p>The backend follows Hexagonal Architecture to keep business logic independent of frameworks and infrastructure.</p> <pre><code>graph TB\n    subgraph API Layer\n        Routers[FastAPI Routers]\n        Schemas[Pydantic Schemas]\n        Errors[Error Handlers]\n    end\n\n    subgraph Application Layer\n        UC[Use Cases]\n    end\n\n    subgraph Domain Layer\n        Entities[Entities]\n        Ports[Ports \u2014 Abstract Interfaces]\n        DomainErrors[Domain Errors]\n    end\n\n    subgraph Infrastructure Layer\n        Repos[SQLAlchemy Repositories]\n        InMemRepos[In-Memory Repositories]\n        Storage[S3 Storage Service]\n        Obs[Observability]\n    end\n\n    Routers --&gt; UC\n    UC --&gt; Entities\n    UC --&gt; Ports\n    Ports -.-&gt;|implements| Repos\n    Ports -.-&gt;|implements| InMemRepos\n    Ports -.-&gt;|implements| Storage</code></pre>"},{"location":"explanation/architecture/#domain-layer","title":"Domain Layer","text":"<p>The innermost layer contains:</p> <ul> <li>Entities: Core business objects (<code>Project</code>, <code>Image</code>, <code>LabelRecord</code>, <code>User</code>) as plain Python dataclasses with factory methods and state transitions.</li> <li>Ports: Abstract base classes (<code>ProjectRepository</code>, <code>ImageRepository</code>, <code>LabelRepository</code>, <code>UserRepository</code>, <code>StatsRepository</code>, <code>StorageService</code>) that define the contracts the domain needs from the outside world.</li> <li>Errors: Domain-specific exceptions (<code>ProjectNotFound</code>, <code>ImageNotInProgress</code>, <code>LabelerMismatch</code>, etc.) that the API layer maps to HTTP responses.</li> </ul> <p>The domain has zero external dependencies \u2014 it defines contracts that other layers implement.</p>"},{"location":"explanation/architecture/#application-layer","title":"Application Layer","text":"<p>Contains use cases that orchestrate business workflows:</p> <ul> <li>Each use case is a single class in its own file with an <code>execute()</code> method (e.g., <code>CreateAssignmentUseCase</code>, <code>SubmitLabelUseCase</code>).</li> <li>Use cases receive a <code>UnitOfWork</code> via constructor and open a transactional context (<code>async with self._uow as uow:</code>) to access repositories.</li> <li>They enforce business rules and coordinate between multiple repositories within a single database transaction.</li> <li>No logging or infrastructure concerns \u2014 the application layer stays pure.</li> </ul>"},{"location":"explanation/architecture/#api-layer","title":"API Layer","text":"<p>The outermost entry point:</p> <ul> <li>Routers: FastAPI route handlers that translate HTTP into use case calls.</li> <li>Schemas: Pydantic v2 models for request validation and response serialization.</li> <li>Error Handlers: A global handler that maps every <code>DomainError</code> subclass to its HTTP status code and structured JSON body.</li> <li>Business event logging lives here, not in use cases \u2014 see Observability Design.</li> </ul>"},{"location":"explanation/architecture/#infrastructure-layer","title":"Infrastructure Layer","text":"<p>Implements the domain ports:</p> <ul> <li>SQLAlchemy Repositories: Production database access using async SQLAlchemy 2.0, one repository per aggregate.</li> <li>In-Memory Repositories: Test doubles for unit testing without a database, using simple dictionaries.</li> <li>S3 Storage Service: Image storage and presigned URL generation via aioboto3, compatible with AWS S3 and MinIO.</li> <li>Observability: OpenTelemetry tracing, Prometheus metrics middleware, structured JSON logging.</li> </ul>"},{"location":"explanation/architecture/#dependency-injection-unit-of-work","title":"Dependency Injection &amp; Unit of Work","text":"<p>uLabel uses dependency-injector with a <code>DeclarativeContainer</code> to wire everything together, combined with the Unit of Work pattern for transactional database access.</p> <p>The <code>Container</code> class in <code>container.py</code> defines:</p> <ul> <li>Singletons for expensive resources: database engine, session factory, storage service.</li> <li>Factories for per-request resources: <code>UnitOfWork</code> and use cases.</li> <li>Configuration loaded from <code>config.yml</code> with environment variable interpolation.</li> </ul> <pre><code>graph LR\n    Config[config.yml] --&gt; Container\n    Container --&gt;|Singleton| Engine[SQLAlchemy Engine]\n    Container --&gt;|Singleton| StorageSvc[S3 Storage]\n    Container --&gt;|Factory| UoW[UnitOfWork]\n    UoW --&gt;|creates| Repos[Repositories]\n    Container --&gt;|Factory| UseCases[Use Cases]\n    UseCases --&gt; Routers[FastAPI Routers]</code></pre>"},{"location":"explanation/architecture/#unit-of-work-pattern","title":"Unit of Work Pattern","text":"<p>Each use case receives a <code>UnitOfWork</code> that, when entered as an async context manager, opens a single database session shared by all repositories. This ensures:</p> <ul> <li>One connection per request instead of one per repository method call.</li> <li>Atomicity: all writes within a use case either commit together or roll back together.</li> <li>Reduced latency: eliminates repeated connection acquire/release overhead.</li> </ul> <pre><code>async with self._uow as uow:\n    project = await uow.project_repository.get_by_id(project_id)\n    await uow.label_repository.save(label_record)\n    await uow.image_repository.save(image)\n    await uow.commit()\n</code></pre> <p>This means:</p> <ul> <li>Production uses <code>SqlAlchemyUnitOfWork</code> which creates a real <code>AsyncSession</code> shared by all SQL repositories.</li> <li>Unit tests use <code>InMemoryUnitOfWork</code> wrapping in-memory repository doubles \u2014 just construct with <code>make_uow()</code> helper.</li> <li>Integration tests use the real container pointed at a test database.</li> <li>E2E tests use a test FastAPI app with the real container pointed at a test database, verifying the full HTTP \u2192 use case \u2192 UoW \u2192 repository \u2192 database flow.</li> </ul>"},{"location":"explanation/architecture/#key-design-decisions","title":"Key Design Decisions","text":"Decision Rationale Hexagonal Architecture Testability and framework independence \u2014 business logic has zero dependency on FastAPI, SQLAlchemy, or S3 Async throughout All I/O (database, storage, HTTP) is async for high throughput under concurrent labeling workloads One use case per file Clear responsibility boundaries and easy navigation Domain ports as ABCs Explicit contracts make it obvious what each layer expects In-memory test doubles Fast unit tests (milliseconds) without infrastructure Logging only in API layer Domain and application layers stay pure \u2014 see Observability Design Unit of Work One DB session per use case execution \u2014 atomicity and reduced connection overhead Dependency-injector Declarative wiring with lifecycle management (singleton/factory), avoids manual DI boilerplate <code>config.yml</code> with env interpolation Readable defaults with environment-specific overrides, no scattered <code>os.getenv()</code> calls"},{"location":"explanation/database/","title":"Database Design","text":"<p>uLabel uses PostgreSQL as its primary data store, accessed asynchronously through SQLAlchemy 2.0 and asyncpg. This page explains the schema design and the reasoning behind key database decisions.</p>"},{"location":"explanation/database/#entity-relationship-diagram","title":"Entity-Relationship Diagram","text":"<pre><code>erDiagram\n    users {\n        uuid id PK\n        varchar username UK\n        varchar role\n    }\n\n    projects {\n        uuid id PK\n        uuid owner_id FK\n        varchar name UK\n        text description\n        timestamp created_at\n    }\n\n    project_labels {\n        uuid project_id FK,PK\n        varchar label PK\n    }\n\n    project_labelers {\n        uuid project_id FK,PK\n        uuid labeler_id PK\n    }\n\n    images {\n        uuid id PK\n        uuid project_id FK\n        varchar storage_key\n        varchar status\n        uuid labeler_id\n        timestamp assigned_at\n        uuid assignment_id\n    }\n\n    label_records {\n        uuid id PK\n        uuid project_id FK\n        uuid image_id FK,UK\n        uuid labeler_id\n        varchar label\n        timestamp created_at\n    }\n\n    users ||--o{ projects : \"owns\"\n    projects ||--o{ project_labels : \"defines\"\n    projects ||--o{ project_labelers : \"assigns\"\n    projects ||--o{ images : \"contains\"\n    projects ||--o{ label_records : \"collects\"\n    images ||--o| label_records : \"labeled as\"\n\n    import_jobs {\n        uuid id PK\n        uuid project_id FK\n        varchar prefix\n        varchar status\n        integer imported\n        text error\n    }\n\n    projects ||--o{ import_jobs : \"imports\"</code></pre>"},{"location":"explanation/database/#tables","title":"Tables","text":""},{"location":"explanation/database/#users","title":"<code>users</code>","text":"<p>Stores both admin and labeler accounts. The <code>role</code> field (<code>admin</code> or <code>labeler</code>) determines access level. Usernames are unique and used for login.</p>"},{"location":"explanation/database/#projects","title":"<code>projects</code>","text":"<p>A labeling project groups images under a set of allowed labels and assigned labelers. The <code>owner_id</code> foreign key points to the admin who created it. Project names are unique (enforced by migration 0003).</p>"},{"location":"explanation/database/#project_labels-and-project_labelers","title":"<code>project_labels</code> and <code>project_labelers</code>","text":"<p>Junction tables implementing the many-to-many relationships. Both use cascade delete \u2014 removing a project removes all its label definitions and labeler assignments. These are fully replaced on project save (delete all + re-insert) to keep the logic simple and avoid complex diffing.</p>"},{"location":"explanation/database/#images","title":"<code>images</code>","text":"<p>The central workflow table. Each image belongs to a project and moves through statuses: <code>pending</code> \u2192 <code>in_progress</code> \u2192 <code>done</code>. Key fields:</p> <ul> <li><code>storage_key</code> + <code>project_id</code> form a unique constraint, preventing duplicate imports.</li> <li><code>status</code> is indexed for efficient filtering during assignment queries.</li> <li><code>labeler_id</code>, <code>assigned_at</code>, and <code>assignment_id</code> are populated when an image is assigned and cleared when expired.</li> </ul>"},{"location":"explanation/database/#label_records","title":"<code>label_records</code>","text":"<p>Stores submitted labels. The unique constraint on <code>image_id</code> enforces one label per image \u2014 once labeled, an image cannot be relabeled. The <code>created_at</code> timestamp uses a server-side default (<code>func.now()</code>) for consistent time tracking.</p>"},{"location":"explanation/database/#import_jobs","title":"<code>import_jobs</code>","text":"<p>Tracks asynchronous bulk image imports from object storage. Each job belongs to a project and progresses through statuses: <code>pending</code> \u2192 <code>running</code> \u2192 <code>done</code> (or <code>failed</code>). The <code>imported</code> counter is updated after each chunk, allowing the frontend to poll for progress. The <code>error</code> field captures failure details when a job fails.</p>"},{"location":"explanation/database/#indexing-strategy","title":"Indexing Strategy","text":"<p>Beyond primary keys and unique constraints, the schema uses composite indexes tuned to the application's most frequent and performance-critical queries:</p> Index Table Columns Query <code>ix_images_project_status</code> images (project_id, status) <code>assign_next_pending</code>: <code>WHERE project_id = ? AND status = 'pending' FOR UPDATE SKIP LOCKED</code> <code>ix_images_status_assigned_at</code> images (status, assigned_at) <code>expire_in_progress</code>: <code>WHERE status = 'in_progress' AND assigned_at &lt; cutoff</code> <code>ix_project_labelers_labeler_id</code> project_labelers (labeler_id) <code>get_by_labeler_id</code>: <code>JOIN ... WHERE labeler_id = ?</code> (PK is (project_id, labeler_id) \u2014 wrong column order for this access pattern) <code>ix_label_records_project_image</code> label_records (project_id, image_id) <code>get_export_data</code>: <code>WHERE project_id = ? ORDER BY image_id</code> <code>ix_label_records_project_labeler</code> label_records (project_id, labeler_id) Stats queries: <code>WHERE project_id = ? GROUP BY labeler_id</code> <code>ix_projects_created_at</code> projects (created_at DESC) <code>get_all</code>: <code>ORDER BY created_at DESC LIMIT ... OFFSET ...</code> <p>Individual column indexes on <code>images.project_id</code> and <code>images.status</code> are retained for queries that filter on a single column.</p>"},{"location":"explanation/database/#design-decisions","title":"Design Decisions","text":""},{"location":"explanation/database/#postgresql-asyncpg","title":"PostgreSQL + asyncpg","text":"<p>PostgreSQL was chosen for its robustness, JSON support, and advanced locking features (<code>SELECT ... FOR UPDATE SKIP LOCKED</code>). The <code>asyncpg</code> driver provides native async support without connection pool overhead, integrating naturally with FastAPI's async request handling.</p>"},{"location":"explanation/database/#sqlalchemy-20-async","title":"SQLAlchemy 2.0 Async","text":"<p>SQLAlchemy 2.0 with <code>mapped_column</code> provides:</p> <ul> <li>Full type safety with <code>Mapped[T]</code> annotations.</li> <li>Native async session support via <code>AsyncSession</code> and <code>async_sessionmaker</code>.</li> <li>Compatibility with Alembic for migrations.</li> </ul> <p>Repositories receive an <code>AsyncSession</code> from the Unit of Work, which manages a single session per use case execution. This keeps all operations within one transaction and avoids creating multiple connections per request.</p>"},{"location":"explanation/database/#orm-domain-mapping","title":"ORM-Domain Mapping","text":"<p>ORM models are not used as domain entities. Instead, each model has <code>to_domain()</code> and <code>from_domain()</code> class methods that convert between the SQLAlchemy model and the plain dataclass domain entity. This keeps the domain layer free of SQLAlchemy dependencies and allows the ORM to evolve independently.</p> <pre><code># Infrastructure model \u2192 Domain entity\nimage = image_model.to_domain()\n\n# Domain entity \u2192 Infrastructure model\nmodel = ImageModel.from_domain(image)\n</code></pre>"},{"location":"explanation/database/#upsert-semantics","title":"Upsert Semantics","text":"<p>All save operations use PostgreSQL's <code>INSERT ... ON CONFLICT</code> instead of separate insert/update paths:</p> <ul> <li><code>on_conflict_do_update</code>: Used for images and users \u2014 updates mutable fields if the row already exists.</li> <li><code>on_conflict_do_nothing</code>: Used for bulk imports and label submission \u2014 silently skips duplicates.</li> </ul> <p>This makes operations idempotent and avoids race conditions in concurrent scenarios.</p>"},{"location":"explanation/database/#row-level-locking-for-assignment","title":"Row-Level Locking for Assignment","text":"<p>The image assignment query uses <code>SELECT ... FOR UPDATE SKIP LOCKED</code>:</p> <pre><code>SELECT * FROM images\nWHERE project_id = :id AND status = 'pending'\nORDER BY id\nLIMIT 1\nFOR UPDATE SKIP LOCKED\n</code></pre> <ul> <li><code>FOR UPDATE</code> locks the selected row, preventing two labelers from being assigned the same image.</li> <li><code>SKIP LOCKED</code> skips already-locked rows instead of waiting, so concurrent requests get different images instantly.</li> <li><code>ORDER BY id</code> uses the UUID primary key, which provides an implicit shuffle of the original ingestion order (see Image Ingestion \u2014 Assignment and Implicit Shuffle).</li> </ul>"},{"location":"explanation/database/#bulk-insert-in-chunks","title":"Bulk Insert in Chunks","text":"<p>The bulk import operation inserts images in chunks of 1,000 rows:</p> <pre><code>chunk_size = 1000\nfor i in range(0, len(images), chunk_size):\n    chunk = images[i : i + chunk_size]\n    await session.execute(\n        insert(ImageModel).values([...]).on_conflict_do_nothing()\n    )\nawait session.commit()\n</code></pre> <p>Chunking prevents memory spikes and keeps individual SQL statements within PostgreSQL's parameter limit. <code>on_conflict_do_nothing</code> makes re-imports safe \u2014 running the same import twice won't create duplicates.</p>"},{"location":"explanation/database/#eager-loading-strategy","title":"Eager Loading Strategy","text":"<p>Project queries use strategic eager loading to avoid N+1 problems:</p> Relationship Strategy Why <code>project.owner</code> <code>joinedload</code> Single related row \u2014 efficient as a JOIN <code>project.label_entries</code> <code>selectinload</code> Collection \u2014 a separate <code>SELECT ... IN (...)</code> avoids row multiplication <code>project.labeler_entries</code> <code>selectinload</code> Same reasoning as labels <p>After joinedload, <code>.unique()</code> is called on the result set to deduplicate rows that were multiplied by the JOIN.</p>"},{"location":"explanation/database/#connection-pool-configuration","title":"Connection Pool Configuration","text":"<p>The engine is configured with:</p> <ul> <li><code>pool_size</code> and <code>max_overflow</code> for connection limits.</li> <li><code>pool_recycle</code> to rotate connections before PostgreSQL's timeout.</li> <li><code>pool_pre_ping</code> to detect and discard stale connections before use.</li> </ul> <p>All configurable via <code>config.yml</code> under the <code>database</code> key.</p>"},{"location":"explanation/database/#migrations-with-alembic","title":"Migrations with Alembic","text":"<p>Schema changes are managed by Alembic with async support. The migration runner in <code>env.py</code> uses the same <code>asyncpg</code> driver as the application. Migrations are numbered sequentially (<code>0001_</code>, <code>0002_</code>, <code>0003_</code>) for clear ordering.</p>"},{"location":"explanation/development-log/","title":"Development Log","text":"<p>Chronological record of all features implemented and design decisions made during the development of uLabel, an image labeling platform. The project was built between March 27-31, 2026 across 20 Claude Code sessions and 28 commits.</p>"},{"location":"explanation/development-log/#day-1-march-27-2026-full-backend","title":"Day 1 - March 27, 2026: Full Backend","text":""},{"location":"explanation/development-log/#commit-a33272e-first-version-of-all-backend-features","title":"Commit <code>a33272e</code> - First version of all backend features","text":"<ul> <li>Complete hexagonal architecture: domain, application (use cases), API (FastAPI), infrastructure</li> <li>Entities: Project, Image, Labeler, Assignment, Label</li> <li>Ports and adapters for repositories and storage</li> <li>Dependency injection with <code>dependency-injector</code></li> <li>PostgreSQL database with SQLAlchemy async + Alembic migrations</li> <li>MinIO as object storage (images)</li> <li>Basic token authentication</li> </ul>"},{"location":"explanation/development-log/#commit-bf462e0-labeler-submit-stats","title":"Commit <code>bf462e0</code> - Labeler submit stats","text":"<ul> <li>Statistics endpoint per labeler (submissions count)</li> </ul>"},{"location":"explanation/development-log/#commit-e8efd23-project-listing-with-pagination","title":"Commit <code>e8efd23</code> - Project listing with pagination","text":""},{"location":"explanation/development-log/#day-2-march-28-2026-backend-improvements-frontend-configuration","title":"Day 2 - March 28, 2026: Backend improvements + Frontend + Configuration","text":""},{"location":"explanation/development-log/#commit-4be4701-fix-alembic-in-docker","title":"Commit <code>4be4701</code> - Fix: Alembic in Docker","text":"<ul> <li>Dockerfile fix to copy Alembic migrations into the container</li> </ul>"},{"location":"explanation/development-log/#commit-7f1d060-labeler-autocomplete","title":"Commit <code>7f1d060</code> - Labeler autocomplete","text":"<ul> <li>Labeler search by username with autocomplete</li> </ul>"},{"location":"explanation/development-log/#commit-8f64e01-add-labelers-to-projects","title":"Commit <code>8f64e01</code> - Add labelers to projects","text":""},{"location":"explanation/development-log/#commit-dde0456-filter-by-name-in-project-listing","title":"Commit <code>dde0456</code> - Filter by name in project listing","text":""},{"location":"explanation/development-log/#commit-4c51917-unique-project-name","title":"Commit <code>4c51917</code> - Unique project name","text":"<ul> <li>Uniqueness constraint on the name field</li> </ul>"},{"location":"explanation/development-log/#commit-1953371-initial-frontend","title":"Commit <code>1953371</code> - Initial frontend","text":"<ul> <li>React + TypeScript + Vite application</li> <li>Admin dashboard: project CRUD, labeler assignment, statistics with charts (Recharts)</li> <li>Labeler dashboard: image labeling interface, image loading from presigned URLs</li> <li>Dockerized with nginx</li> </ul>"},{"location":"explanation/development-log/#commit-9d9cd7b-yaml-configuration-with-dependency-injector","title":"Commit <code>9d9cd7b</code> - YAML configuration with dependency-injector","text":"<ul> <li>Design decision: Migrated from environment variables to <code>config.yml</code> file using the <code>Configuration</code> provider from dependency-injector</li> </ul>"},{"location":"explanation/development-log/#day-3-march-29-2026-api-refactoring-observability-frontend-merge","title":"Day 3 - March 29, 2026: API Refactoring + Observability + Frontend merge","text":""},{"location":"explanation/development-log/#commit-6830ed2-changed-get-to-post-for-assignments","title":"Commit <code>6830ed2</code> - Changed GET to POST for assignments","text":"<ul> <li>Design decision: The \"next image\" endpoint changed from <code>GET /images/next?labeler_id=...</code> to <code>POST /assignments</code> with JSON body, because creating an assignment is an action with side-effects, not a query</li> </ul>"},{"location":"explanation/development-log/#commit-f2112db-global-error-handler-and-unified-format","title":"Commit <code>f2112db</code> - Global error handler and unified format","text":"<ul> <li>Error response refactoring with centralized handler</li> <li>Unified response format across the entire API</li> </ul>"},{"location":"explanation/development-log/#commit-a077233-observability-3-pillars","title":"Commit <code>a077233</code> - Observability (3 pillars)","text":"<ul> <li>Metrics: Prometheus with histograms and counters, <code>/metrics</code> endpoint with OpenMetrics + exemplars with TraceID</li> <li>Traces: OpenTelemetry with Tempo, FastAPI and SQLAlchemy instrumentation</li> <li>Logs: Structured JSON logging, trace_id correlation via LoggingInstrumentor</li> <li>Grafana dashboards (3):<ul> <li>uLabel Overview: req/s, error rate, P99 latency</li> <li>uLabel Labeling Activity: labels/min, uploads/min, assignments/min</li> <li>uLabel Traces &amp; Logs: metrics-traces-logs correlation</li> </ul> </li> <li>Design decision: Business logging in routers (API layer), NOT in use cases, because in hexagonal architecture the domain/application layers should remain pure</li> <li>Design decision: Logger via <code>logging.getLogger(__name__)</code> (stdlib), not injected via DI container, because OTel LoggingInstrumentor injects trace/span IDs globally</li> <li>Stack: Prometheus + Grafana + Tempo + Loki + Promtail</li> </ul>"},{"location":"explanation/development-log/#commits-4307962-487b007-merge-featfrontend-into-main","title":"Commits <code>4307962</code>, <code>487b007</code> - Merge feat/frontend into main","text":""},{"location":"explanation/development-log/#commit-5267307-uvloop","title":"Commit <code>5267307</code> - uvloop","text":"<ul> <li>Design decision: uvloop as high-performance event loop (2-4x improvement), configured via <code>--loop uvloop</code> in uvicorn</li> </ul>"},{"location":"explanation/development-log/#session-frontend-update-for-new-assignments-api","title":"Session: Frontend update for new assignments API","text":"<ul> <li>Renamed <code>getNextImage()</code> to <code>createAssignment()</code> to align with the new POST endpoint</li> <li>\"Back\" button in the labeling interface (disabled during active labeling)</li> </ul>"},{"location":"explanation/development-log/#day-4-march-30-2026-observability-fixes-documentation-ux","title":"Day 4 - March 30, 2026: Observability fixes + Documentation + UX","text":""},{"location":"explanation/development-log/#commit-35679ea-fix-observability","title":"Commit <code>35679ea</code> - Fix observability","text":"<ul> <li>Critical bug: Promtail crashing due to multiple configuration errors:<ul> <li><code>docker: {}</code> stage incompatible with <code>docker_sd_configs</code></li> <li>Incorrect meta-label for compose service</li> <li><code>trace_id</code> as Loki label (high cardinality, 1 stream per request)</li> </ul> </li> <li>Bug: <code>generate_latest()</code> called without <code>registry</code> argument causing <code>/metrics</code> to return 500</li> <li>Bug: <code>FastAPIInstrumentor.instrument_app()</code> inside the lifespan handler was too late (middleware stack already built), resulting in trace_id always being \"0\"</li> <li>Fix: Split instrumentation: <code>setup_tracing()</code> in lifespan, <code>instrument_app()</code> at module level</li> <li>Bug: <code>await</code> on synchronous container resources (<code>configure_logging</code>, tracer provider) causing crash</li> <li>Design decision: \"Recent Traces\" panel changed from Tempo query to Loki query, because Grafana 10.4.1 Tempo plugin only supports traceId lookups, not search queries</li> </ul>"},{"location":"explanation/development-log/#promtail-migration-to-docker_sd_configs","title":"Promtail migration to docker_sd_configs","text":"<ul> <li>From <code>static_configs</code> (path-based scraping) to <code>docker_sd_configs</code> (automatic container discovery via Docker socket)</li> <li>Design decision: Do NOT use Loki Docker driver plugin (it's an alternative to Promtail, not a complement)</li> </ul>"},{"location":"explanation/development-log/#commit-4203401-mkdocs-documentation","title":"Commit <code>4203401</code> - MkDocs documentation","text":"<ul> <li>Design decision: Diataxis framework (Tutorials, How-To Guides, Reference, Explanation)</li> <li>MkDocs with Material theme + mkdocstrings (auto-generation from Google-style docstrings)</li> <li>Google-style docstrings added to ~67 Python files</li> <li>14 documentation pages</li> <li>Design decision: All Makefile targets containerized (only Docker required on host)</li> <li><code>make docs</code> on port 8080 (avoids conflict with backend on 8000)</li> </ul>"},{"location":"explanation/development-log/#commits-ce38556-c5bb5eb-documentation-cicd","title":"Commits <code>ce38556</code> - <code>c5bb5eb</code> - Documentation CI/CD","text":"<ul> <li>GitHub Actions workflow for automatic docs deployment to GitHub Pages</li> <li>Repository made public for free GitHub Pages hosting</li> <li>Bug: <code>uv sync --extra docs</code> incorrect; fix: <code>uv sync --group docs</code> (dependency-groups vs optional-dependencies)</li> </ul>"},{"location":"explanation/development-log/#commit-bc1edb6-extended-documentation","title":"Commit <code>bc1edb6</code> - Extended documentation","text":"<ul> <li>5 explanation pages:<ul> <li><code>architecture.md</code>: Mermaid diagrams (platform, hexagonal, DI)</li> <li><code>database.md</code>: ER diagram, decisions (async, upsert, skip_locked, eager loading)</li> <li><code>api-design.md</code>: endpoint map, schemas, error handling, middleware stack</li> <li><code>image-ingestion.md</code>: image lifecycle (statechart), bulk ingestion, implicit shuffle via UUID v4</li> <li><code>tooling.md</code>: Python toolchain</li> </ul> </li> <li>Documented design decision: UUID v4s are random by nature, so ordering by <code>id</code> acts as an implicit shuffle -- no need for explicit randomization</li> </ul>"},{"location":"explanation/development-log/#commit-23207b6-interactive-chart-zoom","title":"Commit <code>23207b6</code> - Interactive chart zoom","text":"<ul> <li>Click-drag to select area + automatic zoom + \"Reset Zoom\" button on Daily Labeling Progress</li> <li>Optional <code>isZoomed</code>/<code>onResetZoom</code> props on <code>ChartContainer.tsx</code> for reusability</li> </ul>"},{"location":"explanation/development-log/#day-5-march-31-2026-final-documentation-fixes","title":"Day 5 - March 31, 2026: Final documentation + Fixes","text":""},{"location":"explanation/development-log/#commit-e7e3f41-readmemd-and-quick-start","title":"Commit <code>e7e3f41</code> - README.md and Quick Start","text":"<ul> <li>Complete README: description, prerequisites, <code>make bootstrap</code>, service URL/credentials table, user guide with screenshots</li> <li>Quick-start tutorial in MkDocs with 6 screenshots</li> <li>Content duplicated (README for GitHub, docs for MkDocs site) and adapted to each format</li> </ul>"},{"location":"explanation/development-log/#commit-0e4463b-documentation-site-reference","title":"Commit <code>0e4463b</code> - Documentation site reference","text":"<ul> <li>Link to <code>https://pablo-campillo.github.io/ulabel/</code> in README</li> </ul>"},{"location":"explanation/development-log/#commit-23b6a22-fix-missing-mkdocsyml","title":"Commit <code>23b6a22</code> - Fix missing mkdocs.yml","text":""},{"location":"explanation/development-log/#commit-b6856f3-fix-observability-and-grafana","title":"Commit <code>b6856f3</code> - Fix observability and Grafana","text":"<ul> <li>Final adjustments to datasources and Grafana dashboards</li> </ul>"},{"location":"explanation/development-log/#commit-09405b0-fix-frontend","title":"Commit <code>09405b0</code> - Fix frontend","text":"<ul> <li>TypeScript error in Recharts: <code>activeLabel</code> type <code>string | number</code> not accepted by handlers expecting <code>string</code></li> </ul>"},{"location":"explanation/development-log/#day-6-april-4-2026-project-listing-redesign","title":"Day 6 - April 4, 2026: Project listing redesign","text":""},{"location":"explanation/development-log/#fix-race-condition-in-createassignmentusecase","title":"Fix: Race condition in CreateAssignmentUseCase","text":"<p>Bug identified: <code>CreateAssignmentUseCase</code> called <code>get_next_pending()</code> and <code>save()</code> in separate database sessions. The <code>FOR UPDATE SKIP LOCKED</code> lock acquired during <code>get_next_pending</code> was released when its session closed, before <code>save</code> opened a new session. In the gap between the two transactions, a concurrent request could select and assign the same image to a different labeler.</p> <p>Design decision: Atomic <code>assign_next_pending</code> repository method instead of full Unit of Work pattern - New <code>ImageRepository.assign_next_pending(project_id, labeler_id, assigned_at)</code> method that performs SELECT FOR UPDATE SKIP LOCKED + domain mutation + UPDATE within a single transaction - The use case now calls one method instead of three (<code>get_next_pending</code> + <code>image.assign()</code> + <code>save</code>) - Rationale: Only one confirmed race condition. A full Unit of Work would touch 20+ files across all repositories and use cases. The atomic method is surgically targeted (5 files), does not block a future UoW migration, and <code>assign_next_pending</code> is a legitimate domain operation (\"atomically claim the next available image\")</p>"},{"location":"explanation/development-log/#fix-race-condition-in-expireimagestask","title":"Fix: Race condition in ExpireImagesTask","text":"<p>Bug identified: <code>ExpireImagesTask.tick()</code> had a classic read-modify-write race condition. It called <code>get_expired_in_progress()</code> (a simple SELECT without row locking), mutated images in memory with <code>image.expire()</code>, and saved each one in a separate transaction via <code>save()</code>. Between the read and write, a concurrent <code>SubmitLabelUseCase</code> could complete the image (transition to DONE), and the expire task would overwrite it back to PENDING \u2014 losing the user's label.</p> <p>Design decision: Atomic <code>expire_in_progress</code> repository method (same pattern as <code>assign_next_pending</code>) - New <code>ImageRepository.expire_in_progress(cutoff)</code> method that performs <code>SELECT ... FOR UPDATE SKIP LOCKED</code> + domain <code>expire()</code> + upsert within a single transaction - The row-level lock prevents concurrent operations from modifying the same images between select and update - <code>SKIP LOCKED</code> ensures multiple instances of the expire task don't block each other - <code>ExpireImagesTask.tick()</code> simplified to a single repository call - Rationale: Follows the precedent established by <code>assign_next_pending</code> \u2014 surgical atomic method in the repository rather than a full Unit of Work pattern</p>"},{"location":"explanation/development-log/#removed-hardcoded-assignment_timeout-in-favor-of-config","title":"Removed hardcoded ASSIGNMENT_TIMEOUT in favor of config","text":"<p>Problem: <code>ASSIGNMENT_TIMEOUT = timedelta(minutes=30)</code> was hardcoded in the <code>create_assignment</code> router, while <code>config.yml</code> already had <code>tasks.image_assignment_timeout_seconds: 60</code>. The two values diverged (30 min vs 60 s) and the presigned URL expiry was not configurable.</p> <p>Solution: Removed the <code>ASSIGNMENT_TIMEOUT</code> constant and injected <code>config.tasks.image_assignment_timeout_seconds</code> via dependency-injector's <code>Provide[Container.config.tasks.image_assignment_timeout_seconds.as_int()]</code> directly into the endpoint function. Updated all documentation references from \"30 minutes\" / \"1800\" to reference the config file value (60 seconds).</p> <p>Design decision: Used <code>Provide[Container.config...]</code> injection (option B) instead of creating a new provider in the container \u2014 follows the existing pattern and requires fewer changes.</p>"},{"location":"explanation/development-log/#refactor-importjob-as-domain-entity-restore-execute-pattern","title":"Refactor: ImportJob as domain entity + restore execute pattern","text":"<p>Problem identified: <code>ImportImagesFromStorageUseCase</code> broke two project conventions: 1. Used <code>start()</code>/<code>run()</code>/<code>get_job()</code> instead of the standard <code>execute()</code> pattern used by all other use cases 2. <code>ImportJob</code> was a plain dataclass stored in a module-level <code>dict</code> in memory \u2014 not a domain entity, not persisted to database, lost on restart</p> <p>Design decision: Promote <code>ImportJob</code> to a proper domain entity with database persistence - <code>ImportJob</code> domain entity in <code>domain/import_jobs.py</code> with factory method <code>create()</code> and state-transition methods (<code>mark_progress</code>, <code>mark_done</code>, <code>mark_failed</code>) - <code>ImportJobRepository</code> port with SQLAlchemy implementation (upsert semantics) and in-memory implementation for tests - New <code>import_jobs</code> table via Alembic migration - <code>ImportImagesFromStorageUseCase</code> refactored to use <code>execute()</code> (creates and persists the job, returns immediately) + <code>run_import()</code> (background execution, updates job in DB after each chunk) - Polling extracted to a separate <code>GetImportJobUseCase</code> with its own <code>execute()</code> method - Rationale: Aligns with the hexagonal architecture (domain entities with ports and adapters), ensures job state survives restarts, and maintains consistency with the <code>execute</code> pattern across all use cases. The async background nature of the import does not justify breaking the pattern \u2014 the router orchestrates the background task, not the use case</p>"},{"location":"explanation/development-log/#planned-refactor-separate-projectsummary-vs-projectdetail","title":"Planned refactor: Separate ProjectSummary vs ProjectDetail","text":"<p>Problem identified: The <code>GET /projects</code> list endpoint resolves labeler usernames by making N individual queries to <code>UserRepository</code> from the API layer (<code>_resolve_labelers</code>). However:</p> <ul> <li>The admin list frontend only uses <code>labelers.length</code> (count) \u2014 never the usernames</li> <li>The labeler list frontend doesn't show labelers at all</li> <li>Only the admin detail view needs resolved labeler usernames</li> <li>No <code>GET /projects/{id}</code> endpoint exists \u2014 the detail page fetches ALL projects (<code>getProjects(100, 0)</code>) and filters client-side</li> <li>Labeler resolution happens in the API layer (router) instead of the use case, violating the hexagonal architecture</li> </ul> <p>Design decisions:</p> <ul> <li>Two response schemas: <code>ProjectSummary</code> (with <code>labeler_count: int</code>) for listings and <code>ProjectDetail</code> (with <code>labelers: list[LabelerInfo]</code>) for detail/mutation responses</li> <li>New <code>GET /projects/{id}</code> endpoint: Returns <code>ProjectDetail</code> with resolved labelers, eliminates the client-side fetch-all-and-filter antipattern</li> <li>Batch user resolution: New <code>UserRepository.get_by_ids(ids: set[UUID]) -&gt; list[User]</code> method with a single <code>WHERE id IN (...)</code> query instead of N individual lookups</li> <li>New <code>GetProjectUseCase</code>: Centralizes labeler resolution in the application layer (not the API router), receiving both <code>ProjectRepository</code> and <code>UserRepository</code></li> <li>Application DTOs: <code>ProjectWithLabelers</code> dataclass in the application layer to carry the resolved data without polluting the domain model</li> </ul> <p>Rationale: The list endpoint was doing unnecessary work (resolving usernames nobody displays) and the resolution was architecturally misplaced (API layer calling repositories directly). The summary/detail split follows the principle of not fetching data you don't need, and moving business logic to the application layer respects the hexagonal architecture boundaries.</p>"},{"location":"explanation/development-log/#streaming-export-to-minio","title":"Streaming export to MinIO","text":"<p>Problem: <code>ExportLabelsUseCase</code> loaded all label rows into memory (<code>list[LabelExportRow]</code>), built the entire CSV/JSON file as <code>bytes</code>, and uploaded it with a single <code>put_object</code>. For large projects this consumed excessive memory proportional to the number of labels.</p> <p>Solution: End-to-end streaming pipeline across three layers: 1. Database: <code>get_export_data</code> changed from <code>list</code> return to <code>AsyncIterator</code> using SQLAlchemy's <code>session.stream()</code> for server-side cursoring 2. Formatters: <code>_generate_csv</code> and <code>_generate_json</code> converted to async generators yielding byte chunks (CSV: one chunk per row; JSON: manual array framing with <code>[</code>, <code>,</code>, <code>]</code>) 3. Storage: New <code>upload_file_streaming</code> method using S3 multipart upload with a 5MB <code>bytearray</code> buffer per part</p> <p>Design decisions: - New <code>upload_file_streaming</code> method instead of modifying <code>upload_file</code>: The existing <code>upload_file(data: bytes)</code> is still used by image upload and other callers that have the full data in memory. Adding a separate streaming method avoids breaking existing contracts - 5MB buffer for multipart parts: S3 requires a minimum of 5MB per part (except the last). The implementation accumulates chunks in a <code>bytearray</code> and flushes when the threshold is reached - <code>try/except</code> with <code>abort_multipart_upload</code>: On any error during streaming, orphaned multipart parts are cleaned up to avoid storage leaks - Metadata preserved: <code>label_count</code> metadata is passed via <code>create_multipart_upload</code>, so the caching mechanism (head_object + label_count comparison) works unchanged</p>"},{"location":"explanation/development-log/#refactor-batch-labeler-validation-in-updateprojectusecase","title":"Refactor: Batch labeler validation in UpdateProjectUseCase","text":"<p>Problem: <code>UpdateProjectUseCase</code> validated labeler IDs by calling <code>get_by_id</code> in a loop \u2014 one database query per labeler. This was the same N+1 query antipattern that was already solved in <code>GetProjectUseCase</code> with the batch <code>get_by_ids</code> method.</p> <p>Solution: Replaced the <code>for labeler_id in labeler_ids</code> loop with a single <code>get_by_ids(labeler_ids)</code> call. Validation logic (existence check via length comparison, role check via <code>any()</code>) preserved with equivalent semantics.</p> <p>Rationale: <code>UserRepository.get_by_ids</code> already existed in the port and both implementations (SQLAlchemy and in-memory). This is a one-line change that follows the established pattern from <code>GetProjectUseCase</code>.</p>"},{"location":"explanation/development-log/#removed-hardcoded-presigned_url_expiry-in-favor-of-config","title":"Removed hardcoded PRESIGNED_URL_EXPIRY in favor of config","text":"<p>Problem: <code>PRESIGNED_URL_EXPIRY = timedelta(hours=1)</code> was hardcoded in <code>ExportLabelsUseCase</code>, inconsistent with the established pattern where durations are managed from <code>config.yml</code> and injected via <code>dependency-injector</code>.</p> <p>Solution: Moved the value to <code>config.yml</code> as <code>storage.presigned_url_expiry_seconds: 3600</code> and injected it into the use case constructor via the container's <code>providers.Factory</code>, using <code>.as_(lambda s: timedelta(seconds=int(s)))</code> \u2014 the same pattern already used by <code>expire_images_task</code>.</p> <p>Design decision: Injected via constructor (not router endpoint) because the presigned URL expiry is an internal concern of the use case, unlike <code>image_assignment_timeout_seconds</code> which is used directly in the router.</p>"},{"location":"explanation/development-log/#performance-indexes-for-query-optimization","title":"Performance indexes for query optimization","text":"<p>Problem identified: Several critical queries relied on individual column indexes that couldn't efficiently serve multi-column <code>WHERE</code>, <code>ORDER BY</code>, or <code>GROUP BY</code> clauses. The most impactful case was <code>assign_next_pending</code> \u2014 the core image assignment query using <code>SELECT ... FOR UPDATE SKIP LOCKED</code> \u2014 which filters on both <code>project_id</code> and <code>status</code> but only had separate indexes on each column, forcing PostgreSQL to pick one index and scan-filter the rest.</p> <p>Solution: Added 6 composite indexes via Alembic migration <code>0005_add_performance_indexes</code>, targeting the application's hottest query patterns:</p> <ul> <li><code>ix_images_project_status</code> (project_id, status) \u2014 covers <code>assign_next_pending</code> and <code>get_next_pending</code></li> <li><code>ix_images_status_assigned_at</code> (status, assigned_at) \u2014 covers <code>expire_in_progress</code></li> <li><code>ix_project_labelers_labeler_id</code> (labeler_id) \u2014 the PK <code>(project_id, labeler_id)</code> can't serve <code>WHERE labeler_id = ?</code> lookups</li> <li><code>ix_label_records_project_image</code> (project_id, image_id) \u2014 covers <code>get_export_data</code> with <code>ORDER BY image_id</code></li> <li><code>ix_label_records_project_labeler</code> (project_id, labeler_id) \u2014 covers stats queries grouping by labeler within a project</li> <li><code>ix_projects_created_at</code> (created_at DESC) \u2014 covers paginated project listing</li> </ul> <p>Design decision: Composite indexes instead of relying on individual column indexes. B-tree indexes only satisfy multi-column predicates efficiently when the columns appear together in index order. The existing individual indexes on <code>images.project_id</code> and <code>images.status</code> are retained for single-column queries. Also updated <code>database.md</code> with the <code>import_jobs</code> table (missing from the ER diagram) and a new \"Indexing Strategy\" section.</p>"},{"location":"explanation/development-log/#day-7-april-5-2026-observability-gap-fix","title":"Day 7 - April 5, 2026: Observability gap fix","text":""},{"location":"explanation/development-log/#fix-unhandled-exceptions-invisible-in-grafana","title":"Fix: Unhandled exceptions invisible in Grafana","text":"<p>Bug identified: Both JSON and CSV exports (<code>GET /v1/projects/{project_id}/export</code>) returned 500 Internal Server Error, but no error logs appeared in Grafana and no entries showed in the \"Error Rate by Endpoint &amp; Status\" dashboard panel. The issue affected all endpoints, not just exports \u2014 any non-<code>DomainError</code> exception was invisible.</p> <p>Root cause (two gaps in the observability chain): 1. No structured error log: Only <code>DomainError</code> had a registered exception handler (<code>domain_error_handler</code>). Non-domain exceptions (S3 failures, DB streaming errors) hit FastAPI's default 500 handler, which produces no structured JSON log entry \u2014 so nothing reached Loki/Grafana 2. No 500 in <code>http_requests_total</code> metric: In <code>PrometheusMiddleware.dispatch()</code>, when an exception was thrown, execution jumped to the <code>except</code> block which incremented <code>http_exceptions_total</code> and re-raised. The <code>REQUESTS_TOTAL.labels(..., status=response.status_code).inc()</code> line was never reached. The \"Error Rate by Endpoint &amp; Status\" panel queries <code>http_requests_total{status=~\"5..\"}</code>, so it always showed zero for unhandled exceptions</p> <p>Solution: - Added catch-all <code>Exception</code> handler (<code>unhandled_error_handler</code>) with <code>logger.exception()</code> for full traceback in structured logs, returning a JSON 500 response - Added <code>REQUESTS_TOTAL.labels(status=500).inc()</code> in the middleware's <code>except</code> block so unhandled exceptions are counted in the same metric the dashboard queries</p> <p>Design decision: Logging in the catch-all handler (API layer) follows the existing convention established in commit <code>a077233</code> \u2014 business logging in routers/API, not in use cases or domain layer</p>"},{"location":"explanation/development-log/#fix-export-fails-with-500-when-minio-storage-is-full","title":"Fix: Export fails with 500 when MinIO storage is full","text":"<p>Bug identified: Clicking \"Export JSON\" or \"Export CSV\" produced an unhandled <code>botocore.exceptions.ClientError: An error occurred (XMinioStorageFull) when calling the UploadPart operation</code>. The error appeared as a raw 500 in a new browser tab with no user-friendly message.</p> <p>Root cause: The MinIO Docker volume reached its minimum free drive threshold. The <code>upload_file_streaming</code> multipart upload in <code>S3StorageService</code> re-raised the <code>ClientError</code> as-is, which was not a <code>DomainError</code> and therefore got caught only by the generic <code>unhandled_error_handler</code>.</p> <p>Solution (three layers): 1. Domain: New <code>StorageFull(DomainError)</code> exception in <code>domain/errors.py</code> 2. Infrastructure: <code>S3StorageService.upload_file_streaming</code> now catches <code>ClientError</code> with <code>XMinioStorageFull</code> code and raises <code>StorageFull</code> instead, translating the S3-specific error into a domain concept 3. API: <code>StorageFull</code> mapped to HTTP 507 (Insufficient Storage) with code <code>STORAGE_FULL</code> in the error handler 4. Frontend: <code>exportProject</code> replaced <code>window.open</code> with <code>fetch</code> + error handling. Export errors now display inline in the project detail page instead of opening a blank tab with raw JSON</p> <p>Design decisions: - HTTP 507 (Insufficient Storage): Standard status code for this exact scenario (RFC 4918), more informative than a generic 500 - Translation at infrastructure layer: The <code>S3StorageService</code> catches the S3-specific <code>ClientError</code> and raises a domain <code>StorageFull</code>. This keeps the domain layer S3-agnostic (hexagonal boundary) while providing specific error information - <code>fetch</code> instead of <code>window.open</code>: Allows the frontend to inspect the response status before acting \u2014 errors show inline, success triggers a file download via blob URL</p>"},{"location":"explanation/development-log/#api-level-tests-for-export-endpoint","title":"API-level tests for export endpoint","text":"<p>Gap identified: Every other endpoint had API-level tests using <code>TestClient</code> + container overrides, but exports only had application-level tests with fakes. Added <code>tests/api/test_exports.py</code> covering: - Happy path: JSON and CSV export return 307 redirect - Error: project not found returns 404 - Error: no labels returns 404 - Error: storage full returns 507</p>"},{"location":"explanation/development-log/#day-8-april-7-2026-end-to-end-api-tests","title":"Day 8 - April 7, 2026: End-to-end API tests","text":""},{"location":"explanation/development-log/#e2e-test-infrastructure","title":"E2E test infrastructure","text":"<p>Gap identified: Unit tests used in-memory repositories (mocks) and integration tests only verified individual SQLAlchemy repositories against PostgreSQL. No test exercised the full HTTP \u2192 use case \u2192 repository \u2192 database flow. A bug in DI wiring, SQLAlchemy mapping, or layer interaction would go undetected.</p> <p>Solution: New <code>tests/e2e/</code> directory with a test FastAPI app that shares the real DI container but overrides its config to point at a test database: - <code>conftest.py</code>: Container config override via <code>from_dict()</code>, singleton reset (<code>engine.reset()</code>, <code>sessionmaker.reset()</code>), <code>FakeStorageService</code> for storage, simplified lifespan (no tracing, metrics, or background tasks), <code>httpx.AsyncClient</code> with <code>ASGITransport</code> - <code>test_labeling_workflow.py</code>: Single sequential test covering the complete labeling flow \u2014 login \u2192 create project \u2192 add labeler \u2192 register image \u2192 create assignment \u2192 submit label \u2192 verify stats \u2192 confirm no pending images</p> <p>Design decisions: - Separate test app instead of importing the production <code>app</code>: The production app has <code>PrometheusMiddleware</code> and <code>instrument_fastapi</code> baked in at module level, which would fail or produce side effects during tests - <code>FakeStorageService</code> instead of real MinIO: The goal is testing HTTP-to-DB flow; storage is not the layer under test. Keeps test infrastructure to just PostgreSQL - Flow-based tests, not per-endpoint: Unit tests already cover individual endpoints exhaustively. E2E tests verify the integration between layers through real workflows</p>"},{"location":"explanation/development-log/#makefile-improvements","title":"Makefile improvements","text":"<ul> <li>All test targets (<code>test-unit</code>, <code>test-integration</code>, <code>test-cov</code>, <code>test-e2e</code>) now mount <code>src</code> and <code>tests</code> as volumes \u2014 no <code>make setup</code> needed after code changes</li> <li><code>TEST_DATABASE_URL</code> default value added (<code>?=</code>) matching the docker-compose PostgreSQL config</li> </ul>"},{"location":"explanation/development-log/#day-8-cont-april-7-2026-load-testing-with-locust","title":"Day 8 (cont.) - April 7, 2026: Load testing with Locust","text":""},{"location":"explanation/development-log/#labeling-load-test","title":"Labeling load test","text":"<p>Need identified: No way to simulate realistic concurrent labeling activity against a running instance. Useful for validating performance after changes (new indexes, pool size tuning, code optimizations) and stress-testing the assignment flow under contention (<code>SELECT ... FOR UPDATE SKIP LOCKED</code> with many concurrent labelers).</p> <p>Solution: Locust-based load test in <code>benchmarks/locustfile.py</code> with two components: - <code>on_test_start</code> listener fetches project details (labels + labelers) via <code>GET /v1/projects/{id}</code> - <code>LabelerUser</code> simulates the labeling loop: <code>POST /assignments</code> \u2192 <code>POST /label</code> (random label from project). Stops on <code>204 No Content</code> (no more pending images)</p> <p>Auto-detection of labeler count is handled in the Makefile: when <code>BENCHMARK_USERS=0</code> (default), it queries the project API and passes the count as <code>--users</code> to Locust.</p> <p>Design decisions: - Locust over alternatives (k6, wrk, ab): Python-native (consistent with the project), supports custom user behavior (not just URL hammering), built-in web UI for real-time monitoring - Auto-detect in Makefile, not LoadTestShape: An initial implementation used a custom <code>LoadTestShape</code> to auto-detect the labeler count, but <code>LoadTestShape.tick()</code> runs before <code>on_test_start</code> (where project data is fetched), causing the shape to see zero labelers and stop immediately. It also silently ignores <code>--run-time</code>. Moving auto-detection to the Makefile (via <code>curl</code> + <code>python3</code>) is simpler and works with Locust's standard <code>--users</code>/<code>--run-time</code> flags - <code>itertools.cycle</code> for labeler assignment: Each spawned Locust user picks the next labeler from a cycle, distributing work evenly across all project labelers - <code>catch_response=True</code> with explicit success/failure: <code>204 No Content</code> on assignments is a valid response (no more images), not an error. Locust's default would count it as a failure, so explicit response validation is needed</p>"},{"location":"explanation/development-log/#cross-cutting-sessions","title":"Cross-cutting sessions","text":""},{"location":"explanation/development-log/#unified-make-bootstrap-and-docker-compose","title":"Unified make bootstrap and Docker Compose","text":"<ul> <li>Design decision: Root <code>docker-compose.yaml</code> uses <code>include:</code> to compose backend and frontend (Docker Compose v2.20+)</li> <li>Each sub-compose is independently usable</li> <li><code>make bootstrap</code>: single command to bring up everything, initialize data, and display a banner with URLs</li> <li>Hot reload on backend via volume mounts <code>./src:/app/src</code></li> </ul>"},{"location":"explanation/development-log/#assignment-expiration-investigation","title":"Assignment expiration investigation","text":"<ul> <li>Identified timing gap: <code>image_assignment_timeout_seconds: 60</code> but the expiration job runs every 5 min (<code>image_expiry_interval_seconds: 300</code>), so expiration is not instantaneous</li> <li>~~Tests use <code>TIMEOUT = 30 min</code> vs production <code>60s</code> (discrepancy noted)~~ \u2014 resolved: both now use <code>config.tasks.image_assignment_timeout_seconds</code></li> </ul>"},{"location":"explanation/development-log/#summary-of-key-design-decisions","title":"Summary of key design decisions","text":"Decision Rationale Hexagonal architecture Separation of concerns, testability, framework independence <code>config.yml</code> with dependency-injector Centralized and typed configuration, better than scattered env vars POST for creating assignments (not GET) Side-effects require POST method Logging in routers, not in use cases Domain/application layers should remain pure (hexagonal) uvloop 2-4x performance improvement with no code changes Promtail + docker_sd_configs Automatic container discovery, better than static paths Diataxis for documentation Proven framework for technical documentation Containerized Makefile targets Only Docker required on host, reproducible environment Docker Compose <code>include:</code> Each service with its own independent compose, clean composition UUID v4 as implicit shuffle No need for explicit randomization for image ordering ProjectSummary vs ProjectDetail schemas Don't fetch data you don't need; list vs detail have different requirements Batch <code>get_by_ids</code> in UserRepository Single <code>WHERE IN</code> query instead of N individual lookups Labeler resolution in use case, not API Hexagonal: business logic belongs in application layer, not routers Composite indexes for hot queries Individual indexes don't cover multi-column WHERE/ORDER BY; composites eliminate sequential scans E2E flow tests, not per-endpoint Unit tests cover endpoints individually; E2E tests verify layer integration through real workflows Volume mounts in test targets Avoid rebuilding Docker image after every code change Locust for load testing Python-native, custom user behaviors, auto-detect labeler count via LoadTestShape"},{"location":"explanation/image-ingestion/","title":"Image Ingestion &amp; Labeling Workflow","text":"<p>This page explains how images enter the system, how they are distributed to labelers, and the mechanisms that keep the workflow reliable under concurrent use.</p>"},{"location":"explanation/image-ingestion/#image-lifecycle","title":"Image Lifecycle","text":"<p>Every image follows a three-state lifecycle:</p> <pre><code>stateDiagram-v2\n    [*] --&gt; PENDING : Image created&lt;br/&gt;(upload, register, or bulk import)\n    PENDING --&gt; IN_PROGRESS : Assigned to labeler\n    IN_PROGRESS --&gt; DONE : Label submitted\n    IN_PROGRESS --&gt; PENDING : Assignment expired&lt;br/&gt;(background task)</code></pre> Status Meaning <code>PENDING</code> Available for assignment \u2014 no labeler is working on it <code>IN_PROGRESS</code> Assigned to a labeler who has an active session with a presigned URL <code>DONE</code> Labeled \u2014 a <code>LabelRecord</code> exists for this image"},{"location":"explanation/image-ingestion/#ingestion-methods","title":"Ingestion Methods","text":""},{"location":"explanation/image-ingestion/#single-image-upload","title":"Single Image Upload","text":"<p><code>POST /v1/projects/{project_id}/images/upload</code> accepts a file upload. The backend:</p> <ol> <li>Generates a UUID for the image.</li> <li>Uploads the file to MinIO/S3 under the key <code>{project_id}/{image_id}</code>.</li> <li>Creates an <code>Image</code> entity in <code>PENDING</code> status.</li> </ol>"},{"location":"explanation/image-ingestion/#register-existing-storage-object","title":"Register Existing Storage Object","text":"<p><code>POST /v1/projects/{project_id}/images</code> registers an image that already exists in object storage by its <code>storage_key</code>. Useful when images are uploaded through external pipelines.</p>"},{"location":"explanation/image-ingestion/#bulk-import-from-storage-prefix","title":"Bulk Import from Storage Prefix","text":"<p><code>POST /v1/projects/{project_id}/images/import</code> is designed for large-scale ingestion \u2014 importing thousands or millions of images from a storage prefix.</p> <pre><code>sequenceDiagram\n    participant Client\n    participant API\n    participant ImportUseCase\n    participant S3 as MinIO / S3\n    participant DB as PostgreSQL\n\n    Client-&gt;&gt;API: POST /import {prefix: \"dataset/cats/\"}\n    API-&gt;&gt;ImportUseCase: start(project_id, prefix)\n    ImportUseCase--&gt;&gt;API: ImportJob (status: running)\n    API--&gt;&gt;Client: 202 {import_id, status: running}\n\n    Note over ImportUseCase: Background task\n    ImportUseCase-&gt;&gt;S3: list_objects(\"dataset/cats/\")\n    loop Every 1,000 objects\n        S3--&gt;&gt;ImportUseCase: batch of storage keys\n        ImportUseCase-&gt;&gt;DB: INSERT ... ON CONFLICT DO NOTHING\n    end\n    ImportUseCase-&gt;&gt;ImportUseCase: status = done\n\n    Client-&gt;&gt;API: GET /imports/{import_id}\n    API--&gt;&gt;Client: {status: done, imported: 25000}</code></pre> <p>Key design decisions:</p> <ul> <li>Async job model: The import starts immediately and returns a job ID. The client polls for completion. This avoids HTTP timeouts on large datasets.</li> <li>Chunked inserts (1,000 rows): Each batch is inserted in a single <code>INSERT</code> statement, keeping memory usage bounded and staying within PostgreSQL's parameter limits.</li> <li><code>ON CONFLICT DO NOTHING</code>: The unique constraint on <code>(project_id, storage_key)</code> prevents duplicates. Re-running an import is safe and idempotent.</li> <li>In-memory job tracking: Import jobs are stored in a module-level dictionary. This is sufficient for a single-instance deployment; a production multi-instance setup would use a persistent job store.</li> </ul>"},{"location":"explanation/image-ingestion/#assignment-and-implicit-shuffle","title":"Assignment and Implicit Shuffle","text":"<p>When a labeler requests an image, the system selects the next pending image:</p> <pre><code>SELECT * FROM images\nWHERE project_id = :id AND status = 'pending'\nORDER BY id\nLIMIT 1\nFOR UPDATE SKIP LOCKED\n</code></pre> <p>The <code>ORDER BY id</code> clause orders by the image's UUID primary key. Since UUIDs (v4) are generated randomly, this ordering implicitly shuffles the images relative to their original ingestion order.</p> <p>This is an important property: when a dataset is imported in bulk from a structured source (e.g., images organized by photographer, date, or category), the original order would cause labelers to see long runs of similar images. Ordering by random UUID breaks this pattern \u2014 labelers receive a mixed, varied stream of images, which:</p> <ul> <li>Reduces labeler fatigue from repetitive content.</li> <li>Distributes edge cases more evenly across labeling sessions.</li> <li>Prevents bias from labelers always seeing a particular subset first.</li> </ul>"},{"location":"explanation/image-ingestion/#concurrent-assignment-safety","title":"Concurrent Assignment Safety","text":"<p>The <code>FOR UPDATE SKIP LOCKED</code> clause ensures safe concurrent assignment:</p> <ul> <li><code>FOR UPDATE</code> locks the selected row, preventing two labelers from receiving the same image.</li> <li><code>SKIP LOCKED</code> causes concurrent queries to skip already-locked rows rather than waiting, so multiple labelers can request images simultaneously without blocking each other.</li> </ul> <p>This is a PostgreSQL-specific feature that provides optimistic concurrency without application-level locking or retry loops.</p>"},{"location":"explanation/image-ingestion/#assignment-session","title":"Assignment Session","text":"<p>When an image is assigned:</p> <ol> <li>The image transitions to <code>IN_PROGRESS</code> with the labeler's ID and a timestamp.</li> <li>A unique <code>assignment_id</code> (UUID) is generated and stored on the image.</li> <li>A presigned URL is generated for the image, with an expiry configured via <code>tasks.image_assignment_timeout_seconds</code> in <code>config.yml</code> (default: 60 seconds).</li> </ol> <p>The <code>assignment_id</code> is returned to the client and must be included when submitting a label. This prevents stale sessions from submitting labels after an assignment has expired and been reassigned.</p>"},{"location":"explanation/image-ingestion/#label-submission","title":"Label Submission","text":"<p>When a labeler submits a label, the system validates:</p> <ol> <li>The image exists and is <code>IN_PROGRESS</code>.</li> <li>The <code>assignment_id</code> matches (guards against expired/reassigned sessions).</li> <li>The <code>labeler_id</code> matches the assigned labeler.</li> <li>The label value is in the project's allowed label set.</li> </ol> <p>If all checks pass, the image moves to <code>DONE</code> and a <code>LabelRecord</code> is created. The unique constraint on <code>label_records.image_id</code> ensures each image is labeled exactly once.</p> <p>The response includes the labeler's ranking within the project (position by label count among all labelers), providing real-time gamification feedback.</p>"},{"location":"explanation/image-ingestion/#expiration-of-abandoned-assignments","title":"Expiration of Abandoned Assignments","text":"<p>A background task (<code>ExpireImagesTask</code>) runs continuously and handles abandoned assignments:</p> <pre><code>sequenceDiagram\n    participant Task as ExpireImagesTask\n    participant DB as PostgreSQL\n\n    loop Every interval (configurable)\n        Task-&gt;&gt;DB: SELECT images WHERE status='in_progress' AND assigned_at &lt; cutoff\n        DB--&gt;&gt;Task: expired images\n        loop Each expired image\n            Task-&gt;&gt;DB: UPDATE status='pending', clear assignment fields\n        end\n    end</code></pre> <ul> <li>The timeout and check interval are configurable via <code>config.yml</code>.</li> <li>Expired images return to <code>PENDING</code> status, making them available for reassignment.</li> <li>This handles cases where a labeler closes their browser, loses connectivity, or simply doesn't complete the task.</li> </ul>"},{"location":"explanation/image-ingestion/#presigned-urls","title":"Presigned URLs","text":"<p>All image access goes through presigned URLs generated by the storage service:</p> <ul> <li>Assignment URLs (expiry configured via <code>config.yml</code>, default 60 s): Allow labelers to view the image in the browser without direct storage credentials.</li> <li>Export URLs (1-hour expiry): Allow downloading exported label files (JSON/CSV).</li> </ul> <p>The backend generates URLs using aioboto3's <code>generate_presigned_url</code>, supporting both MinIO (development) and AWS S3 (production) with the same code path.</p>"},{"location":"explanation/observability/","title":"Observability Design","text":"<p>uLabel implements the three pillars of observability \u2014 logs, traces, and metrics \u2014 with full correlation between them. This page explains the design decisions and how the pieces fit together.</p>"},{"location":"explanation/observability/#stack-overview","title":"Stack Overview","text":"<pre><code>graph LR\n    App[uLabel API] --&gt;|OTLP gRPC| Tempo[(Tempo)]\n    App --&gt;|stdout JSON| Loki[(Loki)]\n    Prometheus[(Prometheus)] --&gt;|scrape /metrics| App\n    Tempo --&gt;|remote write span metrics| Prometheus\n    Tempo --&gt; Grafana[Grafana]\n    Loki --&gt; Grafana\n    Prometheus --&gt; Grafana</code></pre> Component Role Port Prometheus Scrapes and stores metrics 9090 Tempo Receives and stores traces 3200 Loki Collects and indexes logs 3100 Grafana Unified visualization UI 3000"},{"location":"explanation/observability/#three-pillars","title":"Three Pillars","text":"<pre><code>graph TD\n    subgraph Metrics\n        M1[PrometheusMiddleware]\n        M2[http_requests_total]\n        M3[http_request_duration_seconds]\n        M4[domain_errors_total]\n    end\n\n    subgraph Traces\n        T1[FastAPIInstrumentor]\n        T2[SQLAlchemyInstrumentor]\n        T3[AioHttpClientInstrumentor]\n        T4[LoggingInstrumentor]\n    end\n\n    subgraph Logs\n        L1[JsonFormatter + orjson]\n        L2[Business events in routers]\n        L3[Domain errors in error_handlers]\n    end\n\n    M1 --&gt;|exemplar TraceID| Traces\n    T4 --&gt;|injects trace_id/span_id| Logs\n    Logs --&gt;|derived field regex| Traces</code></pre>"},{"location":"explanation/observability/#logging","title":"Logging","text":"<p>Structured JSON logging via <code>JsonFormatter</code> using <code>orjson</code> for performance. Each log entry contains:</p> <pre><code>{\n  \"timestamp\": \"2025-01-15T10:30:00+00:00\",\n  \"level\": \"INFO\",\n  \"logger\": \"ulabel.api.routers.images\",\n  \"message\": \"Image uploaded: project=... image=... size_bytes=1024 content_type=image/png\",\n  \"service\": \"ulabel-backend\",\n  \"trace_id\": \"abc123...\",\n  \"span_id\": \"def456...\"\n}\n</code></pre> <p><code>trace_id</code> and <code>span_id</code> are injected automatically by OpenTelemetry's <code>LoggingInstrumentor</code> \u2014 no manual effort needed. Any <code>logger.info()</code> call anywhere in the application gets trace context for free.</p> <p>Configuration is centralized in <code>config.yml</code>:</p> <pre><code>observability:\n  log_level: \"${LOG_LEVEL:INFO}\"\n  log_format: \"json\"            # or \"text\" for local development\n  service_name: \"ulabel-backend\"\n</code></pre>"},{"location":"explanation/observability/#tracing","title":"Tracing","text":"<p>OpenTelemetry distributed tracing with automatic instrumentation:</p> Instrumentor What it captures <code>FastAPIInstrumentor</code> HTTP method, path, status, duration <code>SQLAlchemyInstrumentor</code> SQL queries, database operations <code>AioHttpClientInstrumentor</code> Outbound HTTP calls <code>LoggingInstrumentor</code> Injects trace/span IDs into log records <p>Sampling uses <code>ForceTraceSampler</code>:</p> <ul> <li>By default, traces are sampled at a configurable ratio (default 100%).</li> <li>Sending the header <code>X-Force-Trace: true</code> forces a request to be sampled regardless of the ratio. Useful for debugging specific requests in production.</li> </ul> <p>Traces are exported via OTLP gRPC to Tempo using <code>BatchSpanProcessor</code> for efficient batching.</p> <p>Metrics from traces: Tempo's <code>metrics_generator</code> automatically derives RED (Rate, Errors, Duration) metrics from ingested spans and writes them to Prometheus via remote write. This enables TraceQL search in Grafana dashboards \u2014 querying individual traces by endpoint and duration \u2014 without additional application-side instrumentation. The generated span metrics include dimensions for <code>http.method</code>, <code>http.route</code>, and <code>http.status_code</code>.</p>"},{"location":"explanation/observability/#metrics","title":"Metrics","text":"<p>Prometheus metrics collected by <code>PrometheusMiddleware</code>:</p> Metric Type Labels <code>http_requests_total</code> Counter method, path, status <code>http_request_duration_seconds</code> Histogram method, path <code>http_requests_in_progress</code> Gauge method, path <code>http_exceptions_total</code> Counter method, path, exception_type <code>domain_errors_total</code> Counter code, status <p>Path labels use the route template (e.g., <code>/v1/projects/{project_id}/images</code>) instead of actual UUIDs to avoid cardinality explosion.</p> <p>Exemplars link each metric observation to its trace. The middleware attaches <code>{TraceID: &lt;hex&gt;}</code> to every <code>observe()</code> and <code>inc()</code> call. Metrics are served in OpenMetrics format at <code>/metrics</code> to support exemplar exposition.</p>"},{"location":"explanation/observability/#correlation-between-pillars","title":"Correlation Between Pillars","text":"<pre><code>graph LR\n    Metrics --&gt;|exemplar with TraceID| Traces\n    Traces --&gt;|trace_id in span| Logs\n    Logs --&gt;|derived field regex| Traces</code></pre> From To Mechanism Metrics Traces Exemplars: click a data point in Grafana to jump to its trace Logs Traces Loki derived field extracts <code>trace_id</code> from JSON and links to Tempo Traces Logs Tempo <code>tracesToLogsV2</code> links to Loki, filterable by trace_id Traces Metrics Tempo <code>metrics_generator</code> writes span metrics to Prometheus via remote write <p>This is configured in <code>etc/grafana/provisioning/datasources/datasources.yml</code>:</p> <ul> <li>Prometheus \u2192 Tempo: <code>exemplarTraceIdDestinations</code> maps <code>TraceID</code> label to Tempo</li> <li>Loki \u2192 Tempo: <code>derivedFields</code> with regex <code>\"trace_id\":\"(\\w+)\"</code> links to Tempo</li> </ul>"},{"location":"explanation/observability/#architectural-decision-where-to-log","title":"Architectural Decision: Where to Log","text":"<p>Business event logging lives in the API layer (routers), not in use cases or domain:</p> <pre><code>graph TD\n    Router[\"API Router&lt;br/&gt;(logging here)\"] --&gt; UseCase[\"Application Use Case&lt;br/&gt;(no logging)\"]\n    UseCase --&gt; Domain[\"Domain&lt;br/&gt;(no logging)\"]\n    UseCase --&gt; Port[\"Domain Port\"]\n    Port --&gt; Repo[\"Infrastructure Repository\"]</code></pre> <p>Why: <code>logging</code> is I/O (infrastructure). The domain and application layers must remain pure and free of infrastructure dependencies, following hexagonal architecture principles. The API layer is already infrastructure and has full access to HTTP context (request parameters, file sizes, response data).</p>"},{"location":"explanation/observability/#business-events-logged","title":"Business Events Logged","text":"Router Event Key fields tokens <code>User logged in</code> id, username, role projects <code>Project created</code> id, name, owner, label count projects <code>Project updated</code> id projects <code>Labeler added to project</code> project, labeler images <code>Image registered</code> project, image, storage key images <code>Image uploaded</code> project, image, size_bytes, content_type images <code>Import started</code> job, project, prefix images <code>Label submitted</code> project, image, labeler, label assignments <code>Assignment created</code> project, image, labeler exports <code>Export generated</code> project, format error_handlers <code>Domain error</code> code, method, path"},{"location":"explanation/tooling/","title":"Tooling &amp; Technology Choices","text":"<p>This page documents the tools and libraries used in the uLabel backend, explaining why each was chosen and what trade-offs were considered.</p>"},{"location":"explanation/tooling/#language-runtime","title":"Language &amp; Runtime","text":""},{"location":"explanation/tooling/#python-312","title":"Python 3.12","text":"<p>uLabel targets Python 3.12+ as its minimum version.</p> <p>Why 3.12:</p> <ul> <li><code>StrEnum</code> in the standard library \u2014 used for <code>ImageStatus</code>, <code>UserRole</code>, and <code>ImportJobStatus</code> without third-party dependencies.</li> <li>Improved error messages and performance optimizations (PEP 709 inlined comprehensions).</li> <li>Modern type hint syntax: <code>X | None</code> instead of <code>Optional[X]</code>, <code>list[T]</code> instead of <code>List[T]</code>.</li> </ul> <p>Trade-off: Requiring 3.12+ excludes environments stuck on older Python versions. Since uLabel runs in Docker, this is not a practical concern.</p>"},{"location":"explanation/tooling/#uvicorn-uvloop","title":"uvicorn + uvloop","text":"<p>The ASGI server uses <code>uvicorn</code> with <code>uvloop</code> as the event loop implementation.</p> <p>Why: <code>uvloop</code> is a drop-in replacement for <code>asyncio</code>'s default event loop, written in Cython, that provides significantly higher throughput for I/O-bound workloads. Since every request touches the database and potentially object storage, this directly benefits latency under load.</p>"},{"location":"explanation/tooling/#package-management","title":"Package Management","text":""},{"location":"explanation/tooling/#uv","title":"uv","text":"<p>uv is used for dependency management and virtual environment handling.</p> <p>Why uv over pip/pip-tools:</p> <ul> <li>10-100x faster dependency resolution and installation (written in Rust).</li> <li>Deterministic lockfile (<code>uv.lock</code>) with cross-platform resolution.</li> <li>Built-in virtual environment management.</li> <li>Compatible with standard <code>pyproject.toml</code> \u2014 no proprietary configuration format.</li> <li>Active development by the same team behind <code>ruff</code>.</li> </ul> <p>Trade-off: Relatively new tool. Mitigated by its standard <code>pyproject.toml</code> compatibility \u2014 falling back to pip is always possible.</p>"},{"location":"explanation/tooling/#code-quality","title":"Code Quality","text":""},{"location":"explanation/tooling/#ruff","title":"ruff","text":"<p>ruff handles both linting and formatting.</p> <p>Why ruff over flake8 + isort + black:</p> <ul> <li>Single tool replaces three (linter + import sorter + formatter).</li> <li>Written in Rust \u2014 runs in milliseconds, even on the full codebase.</li> <li>Compatible rule sets: supports flake8, isort, pyupgrade, and many more.</li> <li>Unified configuration in <code>pyproject.toml</code>.</li> </ul> <p>Configuration:</p> <pre><code>[tool.ruff]\nline-length = 100\n\n[tool.ruff.lint]\nselect = [\"E\", \"F\", \"I\"]\n</code></pre> <p>Only <code>E</code> (pycodestyle errors), <code>F</code> (pyflakes), and <code>I</code> (isort) rules are enabled \u2014 enough to catch real issues without being noisy.</p> <p>Trade-off: Less established than black/flake8 in enterprise environments. The output is effectively identical and the speed difference is substantial.</p>"},{"location":"explanation/tooling/#mypy-strict-mode","title":"mypy (strict mode)","text":"<p>Static type checking with <code>mypy</code> in strict mode ensures type correctness across the codebase.</p> <pre><code>[tool.mypy]\nstrict = true\npython_version = \"3.12\"\n</code></pre> <p>Why strict mode: Catches missing type annotations, implicit <code>Any</code> types, and type errors that would otherwise surface at runtime. Combined with SQLAlchemy 2.0's <code>Mapped[T]</code> annotations, this provides end-to-end type safety from the database layer to the API response.</p>"},{"location":"explanation/tooling/#pre-commit","title":"pre-commit","text":"<p>pre-commit runs quality checks automatically before each commit. The configuration file (<code>.pre-commit-config.yaml</code>) lives in the repository root (not in <code>backend/</code>) so that Git can find it. All hooks are scoped to <code>backend/</code> files via the <code>files: ^backend/</code> filter.</p> <p>Installation (from the repo root):</p> <pre><code>pre-commit install\n</code></pre> <p>Configured hooks:</p> <ul> <li><code>trailing-whitespace</code> \u2014 removes trailing whitespace.</li> <li><code>end-of-file-fixer</code> \u2014 ensures files end with a newline.</li> <li><code>check-yaml</code> / <code>check-toml</code> \u2014 validates YAML/TOML syntax.</li> <li><code>check-merge-conflict</code> \u2014 prevents committing unresolved merge conflicts.</li> <li><code>debug-statements</code> \u2014 catches leftover <code>breakpoint()</code> / <code>pdb</code> calls.</li> <li><code>ruff check --fix</code> \u2014 linting with auto-fix.</li> <li><code>ruff format</code> \u2014 formatting.</li> <li><code>mypy</code> \u2014 type checking (runs via <code>make typecheck</code> inside Docker).</li> </ul> <p>Why: Prevents poorly formatted or linted code from entering the repository. Fast enough (ruff runs in milliseconds) that it doesn't slow down the development workflow.</p>"},{"location":"explanation/tooling/#web-framework","title":"Web Framework","text":""},{"location":"explanation/tooling/#fastapi","title":"FastAPI","text":"<p>FastAPI is the web framework for the REST API.</p> <p>Why FastAPI:</p> <ul> <li>Native async support \u2014 all route handlers are <code>async def</code>.</li> <li>Automatic OpenAPI documentation from type hints and Pydantic schemas.</li> <li>Dependency injection system via <code>Depends()</code>, which integrates naturally with <code>dependency-injector</code>.</li> <li>Pydantic v2 integration for request validation and response serialization.</li> <li>High performance (Starlette-based ASGI framework).</li> </ul> <p>Trade-off: The ecosystem is younger than Django/Flask. For a focused API service (no admin panel, no template rendering), FastAPI's async-first approach is a better fit.</p>"},{"location":"explanation/tooling/#database","title":"Database","text":""},{"location":"explanation/tooling/#sqlalchemy-20-async","title":"SQLAlchemy 2.0 (async)","text":"<p>SQLAlchemy 2.0 with the async API provides the ORM layer.</p> <p>Why SQLAlchemy 2.0:</p> <ul> <li><code>mapped_column</code> with <code>Mapped[T]</code> gives full type safety for model definitions.</li> <li>Native async session support via <code>AsyncSession</code> \u2014 no need for thread pool executors.</li> <li><code>insert().on_conflict_do_update/nothing()</code> for PostgreSQL upsert semantics.</li> <li>Mature migration support via Alembic.</li> <li>Excellent integration with mypy for static analysis.</li> </ul> <p>Trade-off: More complex than lighter alternatives (e.g., <code>databases</code>, <code>encode/orm</code>). The complexity pays off in type safety, migration support, and the ability to express advanced queries (subqueries, window functions, row-level locking).</p>"},{"location":"explanation/tooling/#asyncpg","title":"asyncpg","text":"<p>The PostgreSQL driver, chosen for:</p> <ul> <li>Native async protocol implementation (not a wrapper around a sync driver).</li> <li>Prepared statements and binary protocol for performance.</li> <li>Direct integration with SQLAlchemy's async engine.</li> </ul>"},{"location":"explanation/tooling/#alembic","title":"Alembic","text":"<p>Database migrations use Alembic with async support. The migration environment (<code>env.py</code>) runs migrations using the same <code>asyncpg</code> driver as the application, ensuring consistent behavior between migrations and runtime.</p>"},{"location":"explanation/tooling/#object-storage","title":"Object Storage","text":""},{"location":"explanation/tooling/#aioboto3","title":"aioboto3","text":"<p>aioboto3 provides async S3 access.</p> <p>Why aioboto3:</p> <ul> <li>Async wrapper around <code>boto3</code> \u2014 the standard AWS SDK for Python.</li> <li>Compatible with any S3-compatible service (MinIO in development, AWS S3 in production).</li> <li>Supports presigned URL generation, object listing, metadata, and uploads.</li> </ul> <p>Trade-off: <code>aioboto3</code> is a community wrapper, not an official AWS library. The underlying <code>botocore</code> does the real work; <code>aioboto3</code> just makes it async-friendly.</p>"},{"location":"explanation/tooling/#serialization","title":"Serialization","text":""},{"location":"explanation/tooling/#orjson","title":"orjson","text":"<p>orjson is used for JSON serialization, specifically in the structured logging <code>JsonFormatter</code>.</p> <p>Why orjson:</p> <ul> <li>Written in Rust \u2014 2-10x faster than the standard <code>json</code> module.</li> <li>Native support for <code>datetime</code>, <code>UUID</code>, and <code>dataclass</code> serialization.</li> <li>Used in the hot path: every log line is serialized to JSON via orjson.</li> </ul> <p>Trade-off: C extension dependency \u2014 requires a compatible build for the target platform. In practice, wheels are available for all common platforms.</p>"},{"location":"explanation/tooling/#dependency-injection","title":"Dependency Injection","text":""},{"location":"explanation/tooling/#dependency-injector","title":"dependency-injector","text":"<p>dependency-injector provides the DI framework.</p> <p>Why:</p> <ul> <li><code>DeclarativeContainer</code> pattern for centralized dependency wiring.</li> <li>Lifecycle management: <code>Singleton</code> for expensive resources (DB engine, storage), <code>Factory</code> for per-request resources (Unit of Work, use cases).</li> <li>YAML configuration loading with environment variable interpolation.</li> <li>Integration with FastAPI via <code>Provide[]</code> and <code>@inject</code>.</li> </ul> <p>Trade-off: Adds a framework dependency for something that could be done manually. The benefit is eliminating boilerplate and getting lifecycle management (singleton vs factory) for free. The container is also the single place to swap implementations for testing.</p>"},{"location":"explanation/tooling/#testing","title":"Testing","text":""},{"location":"explanation/tooling/#pytest-pytest-anyio","title":"pytest + pytest-anyio","text":"<p>Testing uses <code>pytest</code> with <code>pytest-anyio</code> for async test support.</p> <p>Why pytest-anyio over pytest-asyncio:</p> <ul> <li>Supports multiple async backends (asyncio, trio).</li> <li>Consistent fixture handling for async fixtures.</li> </ul> <p>Testing strategy:</p> <ul> <li>Unit tests (<code>tests/application/</code>, <code>tests/api/</code>): Use in-memory repositories and fake storage. Fast (milliseconds), no infrastructure needed.</li> <li>Integration tests (<code>tests/integration/</code>): Run against a real PostgreSQL database to verify SQL queries and migrations.</li> <li>API tests (<code>tests/api/</code>): Use <code>httpx.AsyncClient</code> with FastAPI's <code>TestClient</code> for full HTTP request/response testing.</li> <li>E2E tests (<code>tests/e2e/</code>): Exercise full API workflows against a real PostgreSQL database using <code>httpx.AsyncClient</code> with the FastAPI app. Storage is replaced by <code>FakeStorageService</code>.</li> </ul>"},{"location":"explanation/tooling/#documentation","title":"Documentation","text":""},{"location":"explanation/tooling/#mkdocs-material","title":"MkDocs + Material","text":"<p>MkDocs with the Material theme generates the documentation site.</p> <p>Why MkDocs:</p> <ul> <li>Documentation as code \u2014 Markdown files versioned alongside the codebase.</li> <li>Material theme provides a modern, responsive UI with dark mode, search, and navigation tabs.</li> <li>mkdocstrings auto-generates API reference from Python docstrings.</li> <li>Mermaid diagram support via <code>pymdownx.superfences</code>.</li> </ul> <p>Documentation structure follows the Diataxis framework:</p> Section Purpose Tutorials Step-by-step learning guides How-To Guides Task-oriented recipes Reference Auto-generated API documentation Explanation Design decisions and architecture (this section)"},{"location":"how-to-guides/","title":"How-To Guides","text":"<p>Practical recipes for common tasks with the uLabel API.</p>"},{"location":"how-to-guides/#project-management","title":"Project Management","text":""},{"location":"how-to-guides/#create-a-new-labeling-project","title":"Create a New Labeling Project","text":"<pre><code>curl -X POST http://localhost:8000/api/v1/projects \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"name\": \"My Project\", \"description\": \"Image classification task\"}'\n</code></pre>"},{"location":"how-to-guides/#add-a-labeler-to-a-project","title":"Add a Labeler to a Project","text":"<pre><code>curl -X POST http://localhost:8000/api/v1/projects/{project_id}/labelers \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"labeler_id\": \"labeler-uuid\"}'\n</code></pre>"},{"location":"how-to-guides/#image-management","title":"Image Management","text":""},{"location":"how-to-guides/#upload-an-image-to-a-project","title":"Upload an Image to a Project","text":"<pre><code>curl -X POST http://localhost:8000/api/v1/projects/{project_id}/images \\\n  -F \"file=@image.jpg\"\n</code></pre>"},{"location":"how-to-guides/#import-images-from-s3-storage","title":"Import Images from S3 Storage","text":"<pre><code>curl -X POST http://localhost:8000/api/v1/projects/{project_id}/images/import \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"prefix\": \"dataset/batch-1/\"}'\n</code></pre>"},{"location":"how-to-guides/#labeling-workflow","title":"Labeling Workflow","text":""},{"location":"how-to-guides/#create-an-assignment","title":"Create an Assignment","text":"<pre><code>curl -X POST http://localhost:8000/api/v1/assignments \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"project_id\": \"project-uuid\", \"labeler_id\": \"labeler-uuid\"}'\n</code></pre>"},{"location":"how-to-guides/#submit-a-label","title":"Submit a Label","text":"<pre><code>curl -X POST http://localhost:8000/api/v1/assignments/{assignment_id}/labels \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"category\": \"cat\", \"confidence\": 0.95}'\n</code></pre>"},{"location":"how-to-guides/#data-export","title":"Data Export","text":""},{"location":"how-to-guides/#export-labels","title":"Export Labels","text":"<pre><code>curl http://localhost:8000/api/v1/projects/{project_id}/exports\n</code></pre>"},{"location":"how-to-guides/#load-testing","title":"Load Testing","text":""},{"location":"how-to-guides/#run-a-labeling-benchmark","title":"Run a labeling benchmark","text":"<pre><code>make benchmark PROJECT_ID=&lt;uuid&gt;\n</code></pre> <p>Spawns one concurrent user per labeler and loops <code>assign \u2192 label (random)</code> for 60 seconds. See Load Testing for configuration options and web UI mode.</p>"},{"location":"how-to-guides/#observability","title":"Observability","text":"<ul> <li>Grafana Dashboards \u2014 How to use the monitoring dashboards</li> <li>Adding Observability \u2014 How to add new logs, metrics, or traces</li> </ul>"},{"location":"how-to-guides/adding-observability/","title":"Adding Observability","text":"<p>How to add new logs, metrics, or traces to the uLabel backend.</p> <p>Architecture rule</p> <p>Logging belongs in the API layer (routers and error handlers), never in use cases or domain entities. See Observability Design for the rationale.</p>"},{"location":"how-to-guides/adding-observability/#adding-a-business-log","title":"Adding a Business Log","text":"<p>Business events are logged in router handlers after the use case completes successfully.</p>"},{"location":"how-to-guides/adding-observability/#steps","title":"Steps","text":"<ol> <li> <p>Ensure the router file has a logger:</p> <pre><code>import logging\n\nlogger = logging.getLogger(__name__)\n</code></pre> </li> <li> <p>Add a <code>logger.info()</code> call after the use case execution:</p> <pre><code>@router.post(\"/{project_id}/images/upload\", ...)\nasync def upload_image(project_id: UUID, file: UploadFile, ...):\n    data = await file.read()\n    image = await use_case.execute(\n        project_id=project_id, data=data,\n        content_type=file.content_type or \"application/octet-stream\",\n    )\n    logger.info(\n        \"Image uploaded: project=%s image=%s size_bytes=%d content_type=%s\",\n        project_id, image.id, len(data), file.content_type,\n    )\n    return ImageResponse(...)\n</code></pre> </li> </ol>"},{"location":"how-to-guides/adding-observability/#conventions","title":"Conventions","text":"<ul> <li>Use <code>logger.info()</code> for successful business events, <code>logger.warning()</code> for handled errors, <code>logger.error()</code> for failures.</li> <li>Format: <code>\"Event description: key1=%s key2=%s\"</code> with <code>%s</code>-style formatting (lazy evaluation, not f-strings).</li> <li>Log identifiers and metadata, not full payloads. For file uploads, log <code>size_bytes</code> instead of the file content.</li> <li><code>trace_id</code> and <code>span_id</code> are injected automatically by <code>LoggingInstrumentor</code> \u2014 do not add them manually.</li> </ul>"},{"location":"how-to-guides/adding-observability/#verification","title":"Verification","text":"<p>Check the log appears in Loki:</p> <pre><code>{service=\"ulabel-backend\"} |= \"Image uploaded\"\n</code></pre>"},{"location":"how-to-guides/adding-observability/#adding-a-prometheus-metric","title":"Adding a Prometheus Metric","text":"<p>Metrics are defined in <code>src/ulabel/infrastructure/observability/metrics.py</code>.</p>"},{"location":"how-to-guides/adding-observability/#steps_1","title":"Steps","text":"<ol> <li> <p>Define the metric at module level:</p> <pre><code>from prometheus_client import Counter\n\nIMAGES_UPLOADED_TOTAL = Counter(\n    \"images_uploaded_total\",\n    \"Total images uploaded\",\n    [\"project_id\", \"content_type\"],\n)\n</code></pre> </li> <li> <p>Increment it in the router or middleware:</p> <pre><code>from ulabel.infrastructure.observability.metrics import IMAGES_UPLOADED_TOTAL\n\nIMAGES_UPLOADED_TOTAL.labels(\n    project_id=str(project_id),\n    content_type=file.content_type,\n).inc()\n</code></pre> </li> <li> <p>To include an exemplar (links the metric to a trace):</p> <pre><code>from opentelemetry import trace\n\nspan = trace.get_current_span()\ntrace_id = trace.format_trace_id(span.get_span_context().trace_id)\nexemplar = {\"TraceID\": trace_id} if trace_id != \"0\" * 32 else None\n\nIMAGES_UPLOADED_TOTAL.labels(...).inc(exemplar=exemplar)\n</code></pre> </li> </ol>"},{"location":"how-to-guides/adding-observability/#conventions_1","title":"Conventions","text":"<ul> <li>Use route templates for path labels (e.g., <code>/v1/projects/{project_id}</code>), not actual UUIDs.</li> <li>Avoid high-cardinality labels (user IDs, image IDs) in metrics \u2014 use traces for per-request detail.</li> <li>The <code>/metrics</code> endpoint serves OpenMetrics format. Exemplars require this format.</li> </ul>"},{"location":"how-to-guides/adding-observability/#verification_1","title":"Verification","text":"<p>Check the metric appears at <code>http://localhost:8000/metrics</code>:</p> <pre><code>curl -s http://localhost:8000/metrics | grep images_uploaded_total\n</code></pre>"},{"location":"how-to-guides/adding-observability/#adding-tracing-instrumentation","title":"Adding Tracing Instrumentation","text":"<p>Most tracing is automatic via instrumentors configured in <code>instrument_app()</code> (<code>tracing.py</code>). Manual spans are needed for operations not covered by auto-instrumentation.</p>"},{"location":"how-to-guides/adding-observability/#adding-a-manual-span","title":"Adding a Manual Span","text":"<pre><code>from opentelemetry import trace\n\ntracer = trace.get_tracer(__name__)\n\nasync def some_operation():\n    with tracer.start_as_current_span(\"operation-name\") as span:\n        span.set_attribute(\"custom.key\", \"value\")\n        result = await do_work()\n        span.set_attribute(\"custom.result_count\", len(result))\n        return result\n</code></pre>"},{"location":"how-to-guides/adding-observability/#instrumenting-a-new-library","title":"Instrumenting a New Library","text":"<p>If you add a new external client (e.g., Redis, HTTP), check if an OpenTelemetry instrumentor exists:</p> <ol> <li>Install: <code>uv add opentelemetry-instrumentation-&lt;library&gt;</code></li> <li> <p>Add to <code>instrument_app()</code> in <code>tracing.py</code>:</p> <pre><code>from opentelemetry.instrumentation.redis import RedisInstrumentor\n\ndef instrument_app(app: FastAPI, engine: AsyncEngine) -&gt; None:\n    # ... existing instrumentors ...\n    RedisInstrumentor().instrument()\n</code></pre> </li> </ol>"},{"location":"how-to-guides/adding-observability/#forcing-a-trace","title":"Forcing a Trace","text":"<p>Send the <code>X-Force-Trace: true</code> header to guarantee a request is traced regardless of the sampling ratio:</p> <pre><code>curl -H \"X-Force-Trace: true\" http://localhost:8000/v1/projects\n</code></pre>"},{"location":"how-to-guides/adding-observability/#verification_2","title":"Verification","text":"<p>Check the trace appears in Tempo via Grafana's Explore view, or in the Traces &amp; Logs dashboard.</p>"},{"location":"how-to-guides/adding-observability/#adding-a-grafana-panel","title":"Adding a Grafana Panel","text":"<p>Dashboard JSON files live in <code>etc/grafana/provisioning/dashboards/</code>.</p>"},{"location":"how-to-guides/adding-observability/#steps_2","title":"Steps","text":"<ol> <li>Edit the appropriate dashboard JSON file (or create a new one).</li> <li> <p>Add a panel object to the <code>panels</code> array:</p> <pre><code>{\n  \"title\": \"My New Panel\",\n  \"type\": \"timeseries\",\n  \"gridPos\": { \"h\": 8, \"w\": 12, \"x\": 0, \"y\": 32 },\n  \"datasource\": { \"type\": \"prometheus\", \"uid\": \"${DS_PROMETHEUS}\" },\n  \"targets\": [\n    {\n      \"expr\": \"sum(rate(my_metric_total[5m])) by (label)\",\n      \"legendFormat\": \"{{ label }}\",\n      \"exemplar\": true\n    }\n  ],\n  \"fieldConfig\": {\n    \"defaults\": {\n      \"unit\": \"reqps\",\n      \"custom\": { \"fillOpacity\": 10 }\n    }\n  }\n}\n</code></pre> </li> <li> <p>For Loki log panels, use <code>\"type\": \"logs\"</code> and datasource <code>${DS_LOKI}</code>.</p> </li> <li> <p>For Tempo trace panels, use <code>\"type\": \"traces\"</code> and datasource uid <code>tempo</code>. Example with TraceQL search:</p> <pre><code>{\n  \"title\": \"Trace Search\",\n  \"type\": \"traces\",\n  \"gridPos\": { \"h\": 14, \"w\": 24, \"x\": 0, \"y\": 0 },\n  \"datasource\": { \"type\": \"tempo\", \"uid\": \"tempo\" },\n  \"targets\": [\n    {\n      \"refId\": \"A\",\n      \"queryType\": \"traceqlSearch\",\n      \"filters\": [\n        {\n          \"id\": \"service-name\",\n          \"tag\": \"resource.service.name\",\n          \"operator\": \"=\",\n          \"value\": [\"ulabel-backend\"],\n          \"scope\": \"resource\"\n        },\n        {\n          \"id\": \"min-duration\",\n          \"tag\": \"duration\",\n          \"operator\": \"&gt;\",\n          \"value\": [\"500ms\"]\n        }\n      ],\n      \"limit\": 50\n    }\n  ]\n}\n</code></pre> <p>Tempo's <code>metrics_generator</code> enables TraceQL metrics queries, so span metrics (rate, duration, errors) are available in Prometheus and can be used in <code>timeseries</code> panels as well.</p> </li> </ol>"},{"location":"how-to-guides/adding-observability/#conventions_2","title":"Conventions","text":"<ul> <li>Set <code>\"exemplar\": true</code> on Prometheus targets to enable trace linking.</li> <li>Use template variables (<code>${DS_PROMETHEUS}</code>, <code>${DS_LOKI}</code>) for datasource references.</li> <li>Use <code>gridPos</code> to position: <code>w</code> is width (max 24), <code>h</code> is height, <code>x</code>/<code>y</code> is position.</li> </ul>"},{"location":"how-to-guides/adding-observability/#verification_3","title":"Verification","text":"<p>Restart Grafana (or wait for auto-reload) and check the panel appears in the dashboard.</p>"},{"location":"how-to-guides/adding-observability/#checklist","title":"Checklist","text":"<p>When adding any observability element, verify the full chain:</p> <ul> <li>[ ] Log \u2014 appears in Loki (<code>{service=\"ulabel-backend\"} |= \"your message\"</code>)</li> <li>[ ] Metric \u2014 appears at <code>/metrics</code> endpoint</li> <li>[ ] Trace \u2014 appears in Tempo (use <code>X-Force-Trace: true</code> header)</li> <li>[ ] Panel \u2014 visible in the appropriate Grafana dashboard</li> <li>[ ] Correlation \u2014 log entry has clickable <code>trace_id</code>, metric exemplar links to trace</li> </ul>"},{"location":"how-to-guides/grafana-dashboards/","title":"Grafana Dashboards","text":"<p>How to access and use the uLabel monitoring dashboards.</p>"},{"location":"how-to-guides/grafana-dashboards/#accessing-grafana","title":"Accessing Grafana","text":"<p>With the local stack running (<code>docker compose up</code>):</p> <ul> <li>URL: http://localhost:3000</li> <li>User: <code>admin</code></li> <li>Password: <code>admin</code></li> </ul> <p>All dashboards are auto-provisioned from <code>etc/grafana/provisioning/dashboards/</code>. Changes made in the Grafana UI are not persisted \u2014 edit the JSON files instead.</p>"},{"location":"how-to-guides/grafana-dashboards/#dashboard-navigation","title":"Dashboard Navigation","text":"<p>The three dashboards are linked via header links. Click the dashboard name in the top-left corner to switch between them.</p> <pre><code>graph LR\n    A[uLabel Overview] &lt;--&gt; B[Labeling Activity]\n    A &lt;--&gt; C[Traces &amp; Logs]\n    B &lt;--&gt; C</code></pre>"},{"location":"how-to-guides/grafana-dashboards/#ulabel-overview","title":"uLabel Overview","text":"<p>Purpose: Service health and HTTP performance at a glance.</p> <p>URL: /d/ulabel-overview</p>"},{"location":"how-to-guides/grafana-dashboards/#panels","title":"Panels","text":"Panel What it shows Request Rate (stat) Current requests/second across all endpoints Error Rate (stat) Percentage of 4xx+5xx responses P99 Latency (stat) 99th percentile response time In Progress (stat) Number of requests currently being processed Request Rate by Endpoint Time series of req/s broken down by method + path Error Rate by Endpoint Time series of errors by method, path, status code Latency by Endpoint (P95) Per-endpoint 95th percentile latency Latency Percentiles Global P50/P90/P99 over time Domain Errors by Type Rate of domain errors by error code Requests In Progress Concurrent request count by endpoint Recent Logs Live log stream from Loki"},{"location":"how-to-guides/grafana-dashboards/#using-variables","title":"Using Variables","text":"<p>Use the dropdown selectors at the top of the dashboard:</p> <ul> <li>path \u2014 Filter all panels by route (e.g., <code>/v1/projects/{project_id}/images/upload</code>). Select multiple or \"All\".</li> <li>method \u2014 Filter by HTTP method (GET, POST, PATCH). Select multiple or \"All\".</li> </ul>"},{"location":"how-to-guides/grafana-dashboards/#using-exemplars","title":"Using Exemplars","text":"<p>Exemplars appear as diamond markers on the Request Rate, Error Rate, and Latency time series panels. Each diamond represents a specific request linked to its trace.</p> <p>To investigate a slow or failed request:</p> <ol> <li>Hover over a diamond on a latency panel</li> <li>Click \"View in Tempo\" in the tooltip</li> <li>Grafana opens the full trace in Tempo, showing the span waterfall</li> </ol> <p>This is the primary mechanism for going from \"I see a latency spike\" to \"here is exactly what happened\".</p>"},{"location":"how-to-guides/grafana-dashboards/#ulabel-labeling-activity","title":"uLabel Labeling Activity","text":"<p>Purpose: Business-level metrics derived from structured log events.</p> <p>URL: /d/ulabel-labeling-activity</p>"},{"location":"how-to-guides/grafana-dashboards/#panels_1","title":"Panels","text":"Panel What it shows Labels/min (stat) Current labeling throughput Uploads/min (stat) Current image upload rate Assignments/min (stat) Current assignment creation rate Exports/min (stat) Current export generation rate Labels Submitted Over Time Labeling activity trend (bar chart) Labels by Value Breakdown by label value, stacked Image Uploads Over Time Uploads vs registrations over time Upload Size Distribution Average and max image size in bytes Assignments Created Assignment creation trend Imports &amp; Exports Import started/completed and export generated/cached Logins Over Time User login frequency Project Management Events Projects created/updated, labelers added Business Event Log Live stream of business events from routers"},{"location":"how-to-guides/grafana-dashboards/#interpreting-the-data","title":"Interpreting the Data","text":"<ul> <li>Labels/min dropping while Assignments/min stays steady may indicate labelers are taking longer or encountering issues.</li> <li>Upload Size Distribution helps detect unusually large images that could slow processing.</li> <li>Imports &amp; Exports panel distinguishes cached exports (no regeneration needed) from freshly generated ones.</li> </ul>"},{"location":"how-to-guides/grafana-dashboards/#ulabel-traces-logs","title":"uLabel Traces &amp; Logs","text":"<p>Purpose: Debugging and investigation of errors and performance issues.</p> <p>URL: /d/ulabel-traces-logs</p>"},{"location":"how-to-guides/grafana-dashboards/#panels_2","title":"Panels","text":"Panel What it shows Errors/min (stat) Rate of ERROR-level log entries Warnings/min (stat) Rate of WARNING-level log entries Domain Errors/min Rate of domain error log entries Expired/min (stat) Rate of expired stale assignments Trace Search Individual traces from Tempo, filterable by endpoint and duration Trace Duration by Endpoint P95 and P50 latency per endpoint with exemplar links Recent Traces Log entries with trace IDs (click to open in Tempo) Error &amp; Warning Rate ERROR vs WARNING counts over time Log Volume by Logger Log entries per Python logger module Error Logs Filtered log stream (ERROR + WARNING only) Full Log Stream Complete log stream with level/logger filters"},{"location":"how-to-guides/grafana-dashboards/#using-variables_1","title":"Using Variables","text":"<ul> <li>endpoint \u2014 Filter trace search and latency panels by route (e.g., <code>/v1/projects/{project_id}/images/upload</code>). Select multiple or \"All\".</li> <li>min_duration \u2014 Filter the Trace Search panel to only show traces slower than this threshold (e.g., <code>500ms</code>, <code>1s</code>).</li> <li>level \u2014 Filter the Full Log Stream by log level (DEBUG, INFO, WARNING, ERROR). Select multiple.</li> <li>logger \u2014 Filter by Python logger module (e.g., <code>ulabel.api.routers.images</code>).</li> </ul>"},{"location":"how-to-guides/grafana-dashboards/#investigating-slow-requests","title":"Investigating Slow Requests","text":"<ol> <li>Select the endpoint(s) of interest in the endpoint dropdown</li> <li>Set min_duration to filter out fast requests (e.g., <code>500ms</code> or <code>1s</code>)</li> <li>The Trace Search panel shows individual traces matching the filters \u2014 each row shows trace ID, duration, and timestamp</li> <li>Click a trace row to open the full span waterfall in Tempo, showing the duration of each operation (HTTP handling, SQL queries, external calls)</li> <li>Use the Trace Duration by Endpoint panel below to see P95 and P50 latency trends \u2014 click an exemplar diamond to jump to the specific trace</li> </ol>"},{"location":"how-to-guides/grafana-dashboards/#investigating-an-error","title":"Investigating an Error","text":"<ol> <li>Check the Errors/min stat \u2014 is the error rate elevated?</li> <li>Open the Error Logs panel to see recent ERROR/WARNING messages</li> <li>Find the relevant log entry and expand its details to see <code>trace_id</code></li> <li>Click the <code>trace_id</code> link \u2014 Grafana opens the trace in Tempo</li> <li>In Tempo, inspect the span waterfall to see which operation failed and how long each step took</li> </ol>"},{"location":"how-to-guides/grafana-dashboards/#navigating-from-log-to-trace","title":"Navigating from Log to Trace","text":"<p>Loki is configured with a derived field that extracts <code>trace_id</code> from JSON log entries. When you expand a log entry in any log panel, the <code>trace_id</code> field appears as a clickable link to Tempo.</p>"},{"location":"how-to-guides/load-testing/","title":"Load Testing","text":"<p>Run realistic labeling simulations against a running uLabel instance using Locust. Given a project UUID, the load test spawns one concurrent user per labeler and loops through the assign-then-label cycle until time runs out or all images are labeled.</p>"},{"location":"how-to-guides/load-testing/#prerequisites","title":"Prerequisites","text":"<ul> <li>The backend running (<code>make dev</code> or <code>docker compose up app</code>)</li> <li>A project with labelers assigned and pending images</li> </ul>"},{"location":"how-to-guides/load-testing/#quick-start","title":"Quick start","text":"<pre><code>make benchmark PROJECT_ID=&lt;uuid&gt;\n</code></pre> <p>This will:</p> <ol> <li>Fetch the project details (labels and labelers) from the API</li> <li>Spawn one Locust user per labeler</li> <li>Each user loops: <code>POST /assignments</code> \u2192 <code>POST /label</code> (random label)</li> <li>Run for 60 seconds and print a summary</li> </ol>"},{"location":"how-to-guides/load-testing/#configuration","title":"Configuration","text":"Variable Default Description <code>PROJECT_ID</code> (required) UUID of the project to benchmark <code>BENCHMARK_DURATION</code> <code>60s</code> How long the test runs <code>BENCHMARK_USERS</code> <code>0</code> (auto) Number of concurrent users. <code>0</code> = one per labeler <code>BENCHMARK_URL</code> <code>http://localhost:8000</code> API base URL"},{"location":"how-to-guides/load-testing/#examples","title":"Examples","text":"<pre><code># Run for 2 minutes\nmake benchmark PROJECT_ID=&lt;uuid&gt; BENCHMARK_DURATION=120s\n\n# Override user count (e.g. stress test with 50 users)\nmake benchmark PROJECT_ID=&lt;uuid&gt; BENCHMARK_USERS=50\n\n# Point to a remote instance\nmake benchmark PROJECT_ID=&lt;uuid&gt; BENCHMARK_URL=https://api.example.com\n</code></pre>"},{"location":"how-to-guides/load-testing/#web-ui-mode","title":"Web UI mode","text":"<p>Locust includes a web dashboard for real-time monitoring:</p> <pre><code>make benchmark-ui PROJECT_ID=&lt;uuid&gt;\n</code></pre> <p>Then open http://localhost:8089 to configure the number of users and spawn rate, and see live charts of request throughput, response times, and failure rates.</p>"},{"location":"how-to-guides/load-testing/#how-it-works","title":"How it works","text":"<p>The load test is implemented in <code>benchmarks/locustfile.py</code> with two components:</p> <ul> <li><code>on_test_start</code> \u2014 Runs once before spawning users. Fetches <code>GET /v1/projects/{PROJECT_ID}</code> to discover the project's labels and labelers.</li> <li><code>LabelerUser</code> \u2014 Each user simulates one labeler in a loop:<ol> <li><code>POST /v1/projects/{id}/assignments</code> to get the next pending image</li> <li><code>POST /v1/projects/{id}/images/{image_id}/label</code> with a random label from the project</li> <li>If <code>204 No Content</code> (no more images), the test stops early</li> </ol> </li> </ul> <p>When <code>BENCHMARK_USERS=0</code> (default), the Makefile queries the project API to count labelers and passes the result as <code>--users</code> to Locust.</p>"},{"location":"how-to-guides/load-testing/#using-results-as-a-benchmark","title":"Using results as a benchmark","text":"<p>The test output includes Locust's standard metrics: request count, failure rate, median/average/p95/p99 response times, and requests per second.</p> <p>To use this as a benchmark (comparing performance across changes):</p> <ol> <li>Run the test under consistent conditions (same dataset size, same hardware)</li> <li>Note the RPS and p99 latency from the summary</li> <li>Make your change (e.g. new index, pool size tweak, code optimization)</li> <li>Re-run and compare</li> </ol> <p>You can also export results to CSV for tracking over time by removing the <code>--only-summary</code> flag and adding <code>--csv=results</code> to the Locust command.</p>"},{"location":"reference/","title":"API Reference","text":"<p>Auto-generated reference documentation from the uLabel source code.</p>"},{"location":"reference/#package-overview","title":"Package Overview","text":""},{"location":"reference/#ulabel","title":"<code>ulabel</code>","text":"<p>ULabel: an image labeling platform for machine learning projects.</p>"},{"location":"reference/#modules","title":"Modules","text":"Layer Description Reference Domain Core entities, ports, and business rules Entities, Ports, Errors Application Use cases orchestrating business logic Use Cases API FastAPI routers and Pydantic schemas Routers, Schemas Infrastructure Database, storage, and observability Repositories, Storage, Observability"},{"location":"reference/api/routers/","title":"API Routers","text":"<p>FastAPI router definitions for the REST API endpoints.</p>"},{"location":"reference/api/routers/#projects","title":"Projects","text":""},{"location":"reference/api/routers/#ulabel.api.routers.projects","title":"<code>projects</code>","text":"<p>Router for project management endpoints.</p> <p>Provides CRUD operations for labelling projects, including creation, listing with pagination, detail retrieval, updating, and adding labelers.</p>"},{"location":"reference/api/routers/#ulabel.api.routers.projects.list_projects","title":"<code>list_projects(limit=Query(default=20, ge=1, le=100, description='Max items per page.'), offset=Query(default=0, ge=0, description='Number of items to skip.'), name=Query(default=None, description='Filter projects by name (case-insensitive, contains match).'), use_case=Depends(Provide[Container.list_projects_use_case]))</code>  <code>async</code>","text":"<p>List all projects with pagination and optional name filtering.</p> <p>Parameters:</p> Name Type Description Default <code>limit</code> <code>int</code> <p>Maximum number of items per page.</p> <code>Query(default=20, ge=1, le=100, description='Max items per page.')</code> <code>offset</code> <code>int</code> <p>Number of items to skip.</p> <code>Query(default=0, ge=0, description='Number of items to skip.')</code> <code>name</code> <code>str | None</code> <p>Optional name filter (case-insensitive contains match).</p> <code>Query(default=None, description='Filter projects by name (case-insensitive, contains match).')</code> <code>use_case</code> <code>ListProjectsUseCase</code> <p>Injected list-projects use case.</p> <code>Depends(Provide[list_projects_use_case])</code> <p>Returns:</p> Type Description <code>PaginatedProjectSummaryResponse</code> <p>A PaginatedProjectSummaryResponse with project summaries and total count.</p> Source code in <code>src/ulabel/api/routers/projects.py</code> <pre><code>@router.get(\n    \"\",\n    response_model=PaginatedProjectSummaryResponse,\n    summary=\"List all projects\",\n    description=(\n        \"Returns a paginated list of all projects, ordered by creation date (newest first).\"\n    ),\n    responses={200: {\"description\": \"Paginated list of projects.\"}},\n)\n@inject\nasync def list_projects(\n    limit: int = Query(default=20, ge=1, le=100, description=\"Max items per page.\"),\n    offset: int = Query(default=0, ge=0, description=\"Number of items to skip.\"),\n    name: str | None = Query(\n        default=None,\n        description=\"Filter projects by name (case-insensitive, contains match).\",\n    ),\n    use_case: ListProjectsUseCase = Depends(Provide[Container.list_projects_use_case]),\n) -&gt; PaginatedProjectSummaryResponse:\n    \"\"\"List all projects with pagination and optional name filtering.\n\n    Args:\n        limit: Maximum number of items per page.\n        offset: Number of items to skip.\n        name: Optional name filter (case-insensitive contains match).\n        use_case: Injected list-projects use case.\n\n    Returns:\n        A PaginatedProjectSummaryResponse with project summaries and total count.\n    \"\"\"\n    result = await use_case.execute(limit=limit, offset=offset, name=name)\n    items = [_to_summary(p) for p in result.items]\n    return PaginatedProjectSummaryResponse(\n        items=items,\n        total=result.total,\n        limit=limit,\n        offset=offset,\n    )\n</code></pre>"},{"location":"reference/api/routers/#ulabel.api.routers.projects.get_project","title":"<code>get_project(project_id, use_case=Depends(Provide[Container.get_project_use_case]))</code>  <code>async</code>","text":"<p>Retrieve a single project with resolved labeler details.</p> <p>Parameters:</p> Name Type Description Default <code>project_id</code> <code>UUID</code> <p>The project's unique identifier.</p> required <code>use_case</code> <code>GetProjectUseCase</code> <p>Injected get-project use case.</p> <code>Depends(Provide[get_project_use_case])</code> <p>Returns:</p> Type Description <code>ProjectDetail</code> <p>A ProjectDetail with resolved labeler usernames.</p> Source code in <code>src/ulabel/api/routers/projects.py</code> <pre><code>@router.get(\n    \"/{project_id}\",\n    response_model=ProjectDetail,\n    summary=\"Get project detail\",\n    description=(\"Returns a single project with fully resolved labeler information.\"),\n    responses={\n        200: {\"description\": \"Project detail with resolved labelers.\"},\n        404: {\n            \"description\": \"Project not found.\",\n            \"content\": {\n                \"application/json\": {\n                    \"example\": {\n                        \"error\": {\n                            \"code\": \"PROJECT_NOT_FOUND\",\n                            \"message\": \"Project not found\",\n                            \"details\": [],\n                        }\n                    }\n                }\n            },\n        },\n    },\n)\n@inject\nasync def get_project(\n    project_id: UUID,\n    use_case: GetProjectUseCase = Depends(Provide[Container.get_project_use_case]),\n) -&gt; ProjectDetail:\n    \"\"\"Retrieve a single project with resolved labeler details.\n\n    Args:\n        project_id: The project's unique identifier.\n        use_case: Injected get-project use case.\n\n    Returns:\n        A ProjectDetail with resolved labeler usernames.\n    \"\"\"\n    result = await use_case.execute(project_id=project_id)\n    return _to_detail(result)\n</code></pre>"},{"location":"reference/api/routers/#ulabel.api.routers.projects.create_project","title":"<code>create_project(request, use_case=Depends(Provide[Container.create_project_use_case]), get_project_use_case=Depends(Provide[Container.get_project_use_case]))</code>  <code>async</code>","text":"<p>Create a new labelling project.</p> <p>Parameters:</p> Name Type Description Default <code>request</code> <code>CreateProjectRequest</code> <p>Project creation parameters including owner, name, and labels.</p> required <code>use_case</code> <code>CreateProjectUseCase</code> <p>Injected create-project use case.</p> <code>Depends(Provide[create_project_use_case])</code> <code>get_project_use_case</code> <code>GetProjectUseCase</code> <p>Injected get-project use case for response building.</p> <code>Depends(Provide[get_project_use_case])</code> <p>Returns:</p> Type Description <code>ProjectDetail</code> <p>A ProjectDetail with the newly created project details.</p> Source code in <code>src/ulabel/api/routers/projects.py</code> <pre><code>@router.post(\n    \"\",\n    response_model=ProjectDetail,\n    status_code=status.HTTP_201_CREATED,\n    summary=\"Create project\",\n    description=\"\"\"\nCreates a new labelling project. The `owner_id` field must reference a user with the\n`admin` role; otherwise a 403 error is returned.\n\nThe `labels` set defines all annotation options available to labelers and cannot be\nchanged after the project is created.\n\"\"\",\n    responses={\n        201: {\"description\": \"Project created successfully.\"},\n        403: {\n            \"description\": \"The specified owner does not have the `admin` role.\",\n            \"content\": {\n                \"application/json\": {\n                    \"example\": {\n                        \"error\": {\n                            \"code\": \"UNAUTHORIZED\",\n                            \"message\": \"Owner is not an admin\",\n                            \"details\": [],\n                        }\n                    }\n                }\n            },\n        },\n        404: {\n            \"description\": \"No user found with the given `owner_id`.\",\n            \"content\": {\n                \"application/json\": {\n                    \"example\": {\n                        \"error\": {\n                            \"code\": \"USER_NOT_FOUND\",\n                            \"message\": \"Owner not found\",\n                            \"details\": [],\n                        }\n                    }\n                }\n            },\n        },\n        409: {\n            \"description\": \"A project with the same name already exists.\",\n            \"content\": {\n                \"application/json\": {\n                    \"example\": {\n                        \"error\": {\n                            \"code\": \"PROJECT_NAME_ALREADY_EXISTS\",\n                            \"message\": \"Project name already exists\",\n                            \"details\": [],\n                        }\n                    }\n                }\n            },\n        },\n    },\n)\n@inject\nasync def create_project(\n    request: CreateProjectRequest,\n    use_case: CreateProjectUseCase = Depends(Provide[Container.create_project_use_case]),\n    get_project_use_case: GetProjectUseCase = Depends(Provide[Container.get_project_use_case]),\n) -&gt; ProjectDetail:\n    \"\"\"Create a new labelling project.\n\n    Args:\n        request: Project creation parameters including owner, name, and labels.\n        use_case: Injected create-project use case.\n        get_project_use_case: Injected get-project use case for response building.\n\n    Returns:\n        A ProjectDetail with the newly created project details.\n    \"\"\"\n    project = await use_case.execute(\n        owner_id=request.owner_id,\n        name=request.name,\n        description=request.description,\n        labels=request.labels,\n    )\n    logger.info(\n        \"Project created: id=%s name=%s owner=%s labels=%d\",\n        project.id,\n        request.name,\n        request.owner_id,\n        len(request.labels),\n    )\n    result = await get_project_use_case.execute(project_id=project.id)\n    return _to_detail(result)\n</code></pre>"},{"location":"reference/api/routers/#ulabel.api.routers.projects.update_project","title":"<code>update_project(project_id, request, use_case=Depends(Provide[Container.update_project_use_case]), get_project_use_case=Depends(Provide[Container.get_project_use_case]))</code>  <code>async</code>","text":"<p>Update a project's name, description, or labeler assignments.</p> <p>Parameters:</p> Name Type Description Default <code>project_id</code> <code>UUID</code> <p>The project to update.</p> required <code>request</code> <code>UpdateProjectRequest</code> <p>Fields to update (only provided fields are modified).</p> required <code>use_case</code> <code>UpdateProjectUseCase</code> <p>Injected update-project use case.</p> <code>Depends(Provide[update_project_use_case])</code> <code>get_project_use_case</code> <code>GetProjectUseCase</code> <p>Injected get-project use case for response building.</p> <code>Depends(Provide[get_project_use_case])</code> <p>Returns:</p> Type Description <code>ProjectDetail</code> <p>A ProjectDetail with the updated project details.</p> Source code in <code>src/ulabel/api/routers/projects.py</code> <pre><code>@router.patch(\n    \"/{project_id}\",\n    response_model=ProjectDetail,\n    summary=\"Update project\",\n    description=\"\"\"\nUpdates a project's name, description, and/or labeler assignments.\n\nOnly the fields present in the request body are modified. Omitted fields remain\nunchanged. When `labeler_ids` is provided, it **replaces** the full set of\nassigned labelers \u2014 every ID must reference an existing user with the `labeler`\nrole.\n\"\"\",\n    responses={\n        200: {\"description\": \"Project updated successfully.\"},\n        403: {\n            \"description\": (\"One of the provided labeler IDs does not have the `labeler` role.\"),\n            \"content\": {\n                \"application/json\": {\n                    \"example\": {\n                        \"error\": {\n                            \"code\": \"UNAUTHORIZED\",\n                            \"message\": \"User is not a labeler\",\n                            \"details\": [],\n                        }\n                    }\n                }\n            },\n        },\n        404: {\n            \"description\": \"Project or labeler not found.\",\n            \"content\": {\n                \"application/json\": {\n                    \"examples\": {\n                        \"project_not_found\": {\n                            \"value\": {\n                                \"error\": {\n                                    \"code\": \"PROJECT_NOT_FOUND\",\n                                    \"message\": \"Project not found\",\n                                    \"details\": [],\n                                }\n                            }\n                        },\n                        \"labeler_not_found\": {\n                            \"value\": {\n                                \"error\": {\n                                    \"code\": \"USER_NOT_FOUND\",\n                                    \"message\": \"Labeler not found\",\n                                    \"details\": [],\n                                }\n                            }\n                        },\n                    }\n                }\n            },\n        },\n    },\n)\n@inject\nasync def update_project(\n    project_id: UUID,\n    request: UpdateProjectRequest,\n    use_case: UpdateProjectUseCase = Depends(Provide[Container.update_project_use_case]),\n    get_project_use_case: GetProjectUseCase = Depends(Provide[Container.get_project_use_case]),\n) -&gt; ProjectDetail:\n    \"\"\"Update a project's name, description, or labeler assignments.\n\n    Args:\n        project_id: The project to update.\n        request: Fields to update (only provided fields are modified).\n        use_case: Injected update-project use case.\n        get_project_use_case: Injected get-project use case for response building.\n\n    Returns:\n        A ProjectDetail with the updated project details.\n    \"\"\"\n    await use_case.execute(\n        project_id=project_id,\n        name=request.name,\n        description=request.description,\n        labeler_ids=set(request.labeler_ids) if request.labeler_ids is not None else None,\n    )\n    logger.info(\"Project updated: id=%s\", project_id)\n    result = await get_project_use_case.execute(project_id=project_id)\n    return _to_detail(result)\n</code></pre>"},{"location":"reference/api/routers/#ulabel.api.routers.projects.add_labeler","title":"<code>add_labeler(project_id, request, use_case=Depends(Provide[Container.add_labeler_to_project_use_case]), get_project_use_case=Depends(Provide[Container.get_project_use_case]))</code>  <code>async</code>","text":"<p>Add a labeler to an existing project.</p> <p>Parameters:</p> Name Type Description Default <code>project_id</code> <code>UUID</code> <p>The project to add the labeler to.</p> required <code>request</code> <code>AddLabelerRequest</code> <p>Contains the labeler user ID.</p> required <code>use_case</code> <code>AddLabelerToProjectUseCase</code> <p>Injected add-labeler use case.</p> <code>Depends(Provide[add_labeler_to_project_use_case])</code> <code>get_project_use_case</code> <code>GetProjectUseCase</code> <p>Injected get-project use case for response building.</p> <code>Depends(Provide[get_project_use_case])</code> <p>Returns:</p> Type Description <code>ProjectDetail</code> <p>A ProjectDetail with the updated project details.</p> Source code in <code>src/ulabel/api/routers/projects.py</code> <pre><code>@router.post(\n    \"/{project_id}/labelers\",\n    response_model=ProjectDetail,\n    summary=\"Add labeler to project\",\n    description=\"\"\"\nAssigns a user with the `labeler` role to an existing project. From that point on,\nthe labeler can see the project in their list and request pending images.\n\nThe user referenced by `labeler_id` must exist and have the `labeler` role; otherwise\na 403 is returned.\n\"\"\",\n    responses={\n        200: {\"description\": \"Labeler added. Returns the updated project.\"},\n        403: {\n            \"description\": \"The specified user does not have the `labeler` role.\",\n            \"content\": {\n                \"application/json\": {\n                    \"example\": {\n                        \"error\": {\n                            \"code\": \"UNAUTHORIZED\",\n                            \"message\": \"User is not a labeler\",\n                            \"details\": [],\n                        }\n                    }\n                }\n            },\n        },\n        404: {\n            \"description\": \"Project or labeler not found.\",\n            \"content\": {\n                \"application/json\": {\n                    \"examples\": {\n                        \"project_not_found\": {\n                            \"value\": {\n                                \"error\": {\n                                    \"code\": \"PROJECT_NOT_FOUND\",\n                                    \"message\": \"Project not found\",\n                                    \"details\": [],\n                                }\n                            }\n                        },\n                        \"labeler_not_found\": {\n                            \"value\": {\n                                \"error\": {\n                                    \"code\": \"USER_NOT_FOUND\",\n                                    \"message\": \"Labeler not found\",\n                                    \"details\": [],\n                                }\n                            }\n                        },\n                    }\n                }\n            },\n        },\n    },\n)\n@inject\nasync def add_labeler(\n    project_id: UUID,\n    request: AddLabelerRequest,\n    use_case: AddLabelerToProjectUseCase = Depends(\n        Provide[Container.add_labeler_to_project_use_case]\n    ),\n    get_project_use_case: GetProjectUseCase = Depends(Provide[Container.get_project_use_case]),\n) -&gt; ProjectDetail:\n    \"\"\"Add a labeler to an existing project.\n\n    Args:\n        project_id: The project to add the labeler to.\n        request: Contains the labeler user ID.\n        use_case: Injected add-labeler use case.\n        get_project_use_case: Injected get-project use case for response building.\n\n    Returns:\n        A ProjectDetail with the updated project details.\n    \"\"\"\n    await use_case.execute(project_id=project_id, labeler_id=request.labeler_id)\n    logger.info(\"Labeler added to project: project=%s labeler=%s\", project_id, request.labeler_id)\n    result = await get_project_use_case.execute(project_id=project_id)\n    return _to_detail(result)\n</code></pre>"},{"location":"reference/api/routers/#images","title":"Images","text":""},{"location":"reference/api/routers/#ulabel.api.routers.images","title":"<code>images</code>","text":"<p>Router for image management endpoints.</p> <p>Provides endpoints for registering, uploading, bulk-importing images, checking import job status, and submitting labels for images.</p>"},{"location":"reference/api/routers/#ulabel.api.routers.images.add_image","title":"<code>add_image(project_id, request, use_case=Depends(Provide[Container.add_image_to_project_use_case]))</code>  <code>async</code>","text":"<p>Register an existing storage object as a project image.</p> <p>Parameters:</p> Name Type Description Default <code>project_id</code> <code>UUID</code> <p>The target project ID.</p> required <code>request</code> <code>AddImageRequest</code> <p>Contains the storage key of the existing object.</p> required <code>use_case</code> <code>AddImageToProjectUseCase</code> <p>Injected add-image use case.</p> <code>Depends(Provide[add_image_to_project_use_case])</code> <p>Returns:</p> Type Description <code>ImageResponse</code> <p>An ImageResponse with the registered image details.</p> Source code in <code>src/ulabel/api/routers/images.py</code> <pre><code>@router.post(\n    \"/{project_id}/images\",\n    response_model=ImageResponse,\n    status_code=status.HTTP_201_CREATED,\n    summary=\"Register image by storage key\",\n    description=\"\"\"\nRegisters an image that **already exists** in the storage bucket into the project.\nUse this endpoint when images are uploaded to the bucket externally and only need\nto be referenced in the project.\n\nTo upload an image directly from the client, use `POST /{project_id}/images/upload`.\nTo bulk-import from a bucket prefix, use `POST /{project_id}/images/import`.\n\"\"\",\n    responses={\n        201: {\"description\": \"Image registered with status `pending`.\"},\n        404: {\n            \"description\": \"Project not found.\",\n            \"content\": {\n                \"application/json\": {\n                    \"example\": {\n                        \"error\": {\n                            \"code\": \"PROJECT_NOT_FOUND\",\n                            \"message\": \"Project not found\",\n                            \"details\": [],\n                        }\n                    }\n                }\n            },\n        },\n    },\n)\n@inject\nasync def add_image(\n    project_id: UUID,\n    request: AddImageRequest,\n    use_case: AddImageToProjectUseCase = Depends(Provide[Container.add_image_to_project_use_case]),\n) -&gt; ImageResponse:\n    \"\"\"Register an existing storage object as a project image.\n\n    Args:\n        project_id: The target project ID.\n        request: Contains the storage key of the existing object.\n        use_case: Injected add-image use case.\n\n    Returns:\n        An ImageResponse with the registered image details.\n    \"\"\"\n    image = await use_case.execute(project_id=project_id, storage_key=request.storage_key)\n    logger.info(\n        \"Image registered: project=%s image=%s key=%s\",\n        project_id,\n        image.id,\n        request.storage_key,\n    )\n    return ImageResponse(\n        id=image.id,\n        project_id=image.project_id,\n        storage_key=image.storage_key,\n        status=image.status,\n    )\n</code></pre>"},{"location":"reference/api/routers/#ulabel.api.routers.images.upload_image","title":"<code>upload_image(project_id, file, use_case=Depends(Provide[Container.upload_image_to_project_use_case]))</code>  <code>async</code>","text":"<p>Upload an image file and register it in the project.</p> <p>Parameters:</p> Name Type Description Default <code>project_id</code> <code>UUID</code> <p>The target project ID.</p> required <code>file</code> <code>UploadFile</code> <p>The uploaded image file.</p> required <code>use_case</code> <code>UploadImageToProjectUseCase</code> <p>Injected upload-image use case.</p> <code>Depends(Provide[upload_image_to_project_use_case])</code> <p>Returns:</p> Type Description <code>ImageResponse</code> <p>An ImageResponse with the newly created image details.</p> Source code in <code>src/ulabel/api/routers/images.py</code> <pre><code>@router.post(\n    \"/{project_id}/images/upload\",\n    response_model=ImageResponse,\n    status_code=status.HTTP_201_CREATED,\n    summary=\"Upload image directly\",\n    description=\"\"\"\nUploads an image file to the storage bucket and registers it in the project in a\nsingle step.\n\nThe image is stored under the key `{project_id}/{image_id}` and immediately becomes\navailable for labelers to request.\n\n**Content-Type:** `multipart/form-data` \u2014 the form field must be named `file`.\n\"\"\",\n    responses={\n        201: {\"description\": \"Image uploaded and registered with status `pending`.\"},\n        404: {\n            \"description\": \"Project not found.\",\n            \"content\": {\n                \"application/json\": {\n                    \"example\": {\n                        \"error\": {\n                            \"code\": \"PROJECT_NOT_FOUND\",\n                            \"message\": \"Project not found\",\n                            \"details\": [],\n                        }\n                    }\n                }\n            },\n        },\n    },\n)\n@inject\nasync def upload_image(\n    project_id: UUID,\n    file: UploadFile,\n    use_case: UploadImageToProjectUseCase = Depends(\n        Provide[Container.upload_image_to_project_use_case]\n    ),\n) -&gt; ImageResponse:\n    \"\"\"Upload an image file and register it in the project.\n\n    Args:\n        project_id: The target project ID.\n        file: The uploaded image file.\n        use_case: Injected upload-image use case.\n\n    Returns:\n        An ImageResponse with the newly created image details.\n    \"\"\"\n    data = await file.read()\n    image = await use_case.execute(\n        project_id=project_id,\n        data=data,\n        content_type=file.content_type or \"application/octet-stream\",\n    )\n    logger.info(\n        \"Image uploaded: project=%s image=%s size_bytes=%d content_type=%s\",\n        project_id,\n        image.id,\n        len(data),\n        file.content_type,\n    )\n    return ImageResponse(\n        id=image.id,\n        project_id=image.project_id,\n        storage_key=image.storage_key,\n        status=image.status,\n    )\n</code></pre>"},{"location":"reference/api/routers/#ulabel.api.routers.images.import_images","title":"<code>import_images(project_id, request, background_tasks, use_case=Depends(Provide[Container.import_images_use_case]))</code>  <code>async</code>","text":"<p>Start an asynchronous bulk import of images from a storage prefix.</p> <p>Parameters:</p> Name Type Description Default <code>project_id</code> <code>UUID</code> <p>The target project ID.</p> required <code>request</code> <code>ImportImagesRequest</code> <p>Contains the bucket prefix to import from.</p> required <code>background_tasks</code> <code>BackgroundTasks</code> <p>FastAPI background task manager.</p> required <code>use_case</code> <code>ImportImagesFromStorageUseCase</code> <p>Injected import use case.</p> <code>Depends(Provide[import_images_use_case])</code> <p>Returns:</p> Type Description <code>ImportJobResponse</code> <p>An ImportJobResponse with the job ID and initial status.</p> Source code in <code>src/ulabel/api/routers/images.py</code> <pre><code>@router.post(\n    \"/{project_id}/images/import\",\n    response_model=ImportJobResponse,\n    status_code=status.HTTP_202_ACCEPTED,\n    summary=\"Bulk import images from storage\",\n    description=\"\"\"\nStarts an **asynchronous** import job that lists all objects in the bucket under the\ngiven `prefix` and registers them as project images.\n\nThe response is returned immediately with `status: \"running\"`. Use\n`GET /{project_id}/images/imports/{import_id}` to poll for progress.\n\nObjects are processed in batches of 1,000. If an error occurs during the import,\nthe job moves to `status: \"failed\"` and the `error` field contains the message.\n\"\"\",\n    responses={\n        202: {\n            \"description\": (\n                \"Import job started. Poll for progress using the returned `import_id`.\"\n            ),\n        },\n        404: {\n            \"description\": \"Project not found.\",\n            \"content\": {\n                \"application/json\": {\n                    \"example\": {\n                        \"error\": {\n                            \"code\": \"PROJECT_NOT_FOUND\",\n                            \"message\": \"Project not found\",\n                            \"details\": [],\n                        }\n                    }\n                }\n            },\n        },\n    },\n)\n@inject\nasync def import_images(\n    project_id: UUID,\n    request: ImportImagesRequest,\n    background_tasks: BackgroundTasks,\n    use_case: ImportImagesFromStorageUseCase = Depends(Provide[Container.import_images_use_case]),\n) -&gt; ImportJobResponse:\n    \"\"\"Start an asynchronous bulk import of images from a storage prefix.\n\n    Args:\n        project_id: The target project ID.\n        request: Contains the bucket prefix to import from.\n        background_tasks: FastAPI background task manager.\n        use_case: Injected import use case.\n\n    Returns:\n        An ImportJobResponse with the job ID and initial status.\n    \"\"\"\n    job = await use_case.execute(project_id=project_id, prefix=request.prefix)\n\n    async def _run_import() -&gt; None:\n        await use_case.run_import(job.id)\n\n    background_tasks.add_task(_run_import)\n    logger.info(\"Import started: job=%s project=%s prefix=%s\", job.id, project_id, request.prefix)\n    return ImportJobResponse(\n        import_id=job.id,\n        project_id=job.project_id,\n        prefix=job.prefix,\n        status=job.status,\n        imported=job.imported,\n    )\n</code></pre>"},{"location":"reference/api/routers/#ulabel.api.routers.images.get_import_status","title":"<code>get_import_status(project_id, import_id, use_case=Depends(Provide[Container.get_import_job_use_case]))</code>  <code>async</code>","text":"<p>Retrieve the current status of a bulk import job.</p> <p>Parameters:</p> Name Type Description Default <code>project_id</code> <code>UUID</code> <p>The project the import belongs to.</p> required <code>import_id</code> <code>UUID</code> <p>The import job identifier.</p> required <code>use_case</code> <code>GetImportJobUseCase</code> <p>Injected get-import-job use case.</p> <code>Depends(Provide[get_import_job_use_case])</code> <p>Returns:</p> Type Description <code>ImportJobResponse</code> <p>An ImportJobResponse with the current job state.</p> Source code in <code>src/ulabel/api/routers/images.py</code> <pre><code>@router.get(\n    \"/{project_id}/images/imports/{import_id}\",\n    response_model=ImportJobResponse,\n    summary=\"Get import job status\",\n    description=\"\"\"\nReturns the current state of an import job previously started with\n`POST /{project_id}/images/import`.\n\nPossible `status` values:\n- `running` \u2014 the import is still in progress.\n- `done` \u2014 all images under the prefix have been imported.\n- `failed` \u2014 an error occurred; check the `error` field for details.\n\"\"\",\n    responses={\n        200: {\"description\": \"Current import job state.\"},\n        404: {\n            \"description\": (\"Import job not found or does not belong to the given project.\"),\n            \"content\": {\n                \"application/json\": {\n                    \"example\": {\n                        \"error\": {\n                            \"code\": \"IMPORT_JOB_NOT_FOUND\",\n                            \"message\": \"Import job not found\",\n                            \"details\": [],\n                        }\n                    }\n                }\n            },\n        },\n    },\n)\n@inject\nasync def get_import_status(\n    project_id: UUID,\n    import_id: UUID,\n    use_case: GetImportJobUseCase = Depends(Provide[Container.get_import_job_use_case]),\n) -&gt; ImportJobResponse:\n    \"\"\"Retrieve the current status of a bulk import job.\n\n    Args:\n        project_id: The project the import belongs to.\n        import_id: The import job identifier.\n        use_case: Injected get-import-job use case.\n\n    Returns:\n        An ImportJobResponse with the current job state.\n    \"\"\"\n    job = await use_case.execute(import_id, project_id)\n    return ImportJobResponse(\n        import_id=job.id,\n        project_id=job.project_id,\n        prefix=job.prefix,\n        status=job.status,\n        imported=job.imported,\n        error=job.error,\n    )\n</code></pre>"},{"location":"reference/api/routers/#ulabel.api.routers.images.submit_label","title":"<code>submit_label(project_id, image_id, request, use_case=Depends(Provide[Container.submit_label_use_case]))</code>  <code>async</code>","text":"<p>Submit a label for an assigned image.</p> <p>Parameters:</p> Name Type Description Default <code>project_id</code> <code>UUID</code> <p>The project the image belongs to.</p> required <code>image_id</code> <code>UUID</code> <p>The image to label.</p> required <code>request</code> <code>SubmitLabelRequest</code> <p>Contains labeler ID, assignment ID, and the label value.</p> required <code>use_case</code> <code>SubmitLabelUseCase</code> <p>Injected submit-label use case.</p> <code>Depends(Provide[submit_label_use_case])</code> <p>Returns:</p> Type Description <code>SubmitLabelResponse</code> <p>A SubmitLabelResponse with the label record and labeler ranking.</p> Source code in <code>src/ulabel/api/routers/images.py</code> <pre><code>@router.post(\n    \"/{project_id}/images/{image_id}/label\",\n    response_model=SubmitLabelResponse,\n    status_code=status.HTTP_201_CREATED,\n    summary=\"Submit label for an image\",\n    description=\"\"\"\nSubmits a label for an image that is currently assigned (`in_progress`) to the labeler.\n\nThe `assignment_id` must match the one returned by `POST /{project_id}/assignments` to\nprevent stale or duplicate submissions. The `label` must be one of the labels configured\nfor the project.\n\nOn success the image transitions to `done` and cannot be re-assigned.\n\"\"\",\n    responses={\n        201: {\"description\": \"Label recorded and image marked as `done`.\"},\n        403: {\n            \"description\": \"The labeler does not match the assigned labeler.\",\n            \"content\": {\n                \"application/json\": {\n                    \"example\": {\n                        \"error\": {\n                            \"code\": \"LABELER_MISMATCH\",\n                            \"message\": \"Labeler mismatch\",\n                            \"details\": [],\n                        }\n                    }\n                }\n            },\n        },\n        404: {\n            \"description\": \"Project or image not found.\",\n            \"content\": {\n                \"application/json\": {\n                    \"example\": {\n                        \"error\": {\n                            \"code\": \"IMAGE_NOT_FOUND\",\n                            \"message\": \"Image not found\",\n                            \"details\": [],\n                        }\n                    }\n                }\n            },\n        },\n        409: {\n            \"description\": (\"Image is not in progress or assignment ID does not match.\"),\n            \"content\": {\n                \"application/json\": {\n                    \"example\": {\n                        \"error\": {\n                            \"code\": \"IMAGE_NOT_IN_PROGRESS\",\n                            \"message\": \"Image is not in progress\",\n                            \"details\": [],\n                        }\n                    }\n                }\n            },\n        },\n        422: {\n            \"description\": \"Label is not valid for this project.\",\n            \"content\": {\n                \"application/json\": {\n                    \"example\": {\n                        \"error\": {\n                            \"code\": \"INVALID_LABEL\",\n                            \"message\": \"Invalid label\",\n                            \"details\": [],\n                        }\n                    }\n                }\n            },\n        },\n    },\n)\n@inject\nasync def submit_label(\n    project_id: UUID,\n    image_id: UUID,\n    request: SubmitLabelRequest,\n    use_case: SubmitLabelUseCase = Depends(Provide[Container.submit_label_use_case]),\n) -&gt; SubmitLabelResponse:\n    \"\"\"Submit a label for an assigned image.\n\n    Args:\n        project_id: The project the image belongs to.\n        image_id: The image to label.\n        request: Contains labeler ID, assignment ID, and the label value.\n        use_case: Injected submit-label use case.\n\n    Returns:\n        A SubmitLabelResponse with the label record and labeler ranking.\n    \"\"\"\n    label_record, stats = await use_case.execute(\n        project_id=project_id,\n        image_id=image_id,\n        labeler_id=request.labeler_id,\n        assignment_id=request.assignment_id,\n        label=request.label,\n    )\n    logger.info(\n        \"Label submitted: project=%s image=%s labeler=%s label=%s\",\n        project_id,\n        image_id,\n        request.labeler_id,\n        request.label,\n    )\n    return SubmitLabelResponse(\n        id=label_record.id,\n        project_id=label_record.project_id,\n        image_id=label_record.image_id,\n        labeler_id=label_record.labeler_id,\n        label=label_record.label,\n        labeler_count=stats.labeler_count,\n        ranking=stats.ranking,\n        total_labelers=stats.total_labelers,\n    )\n</code></pre>"},{"location":"reference/api/routers/#assignments","title":"Assignments","text":""},{"location":"reference/api/routers/#ulabel.api.routers.assignments","title":"<code>assignments</code>","text":"<p>Router for image assignment endpoints.</p> <p>Handles creating assignments that pair labelers with pending images and generate time-limited presigned URLs for image access.</p>"},{"location":"reference/api/routers/#ulabel.api.routers.assignments.create_assignment","title":"<code>create_assignment(project_id, request, use_case=Depends(Provide[Container.create_assignment_use_case]), storage=Depends(Provide[Container.storage_service]), assignment_timeout_seconds=Depends(Provide[Container.config.tasks.image_assignment_timeout_seconds.as_int()]))</code>  <code>async</code>","text":"<p>Assign the next pending image to a labeler and return a presigned URL.</p> <p>Parameters:</p> Name Type Description Default <code>project_id</code> <code>UUID</code> <p>The project to assign an image from.</p> required <code>request</code> <code>CreateAssignmentRequest</code> <p>Contains the labeler ID to assign the image to.</p> required <code>use_case</code> <code>CreateAssignmentUseCase</code> <p>Injected assignment use case.</p> <code>Depends(Provide[create_assignment_use_case])</code> <code>storage</code> <code>StorageService</code> <p>Injected storage service for presigned URL generation.</p> <code>Depends(Provide[storage_service])</code> <p>Returns:</p> Type Description <code>AssignmentResponse</code> <p>An AssignmentResponse with image details and a presigned URL.</p> Source code in <code>src/ulabel/api/routers/assignments.py</code> <pre><code>@router.post(\n    \"/{project_id}/assignments\",\n    response_model=AssignmentResponse,\n    status_code=status.HTTP_201_CREATED,\n    summary=\"Create image assignment\",\n    description=\"\"\"\nAssigns the next `pending` image in the project to the given labeler.\n\nThe image transitions to `in_progress` and a **presigned URL** is generated for\ndirect file access from the browser or client. The URL lifetime is controlled by\n``storage.presigned_url_expiry_seconds`` in ``config.yml``, and the assignment\ntimeout by ``tasks.image_assignment_timeout_seconds``.\n\nIf the labeler does not complete the task before the time expires, the image is\nautomatically reset to `pending` and becomes available for another labeler.\n\n**Special responses:**\n- `204 No Content` \u2014 no pending images in this project right now.\n- `403 Forbidden` \u2014 the labeler is not assigned to this project.\n\"\"\",\n    responses={\n        201: {\"description\": \"Assignment created. Includes a presigned URL for file access.\"},\n        204: {\"description\": \"No pending images available in the project right now.\"},\n        403: {\n            \"description\": \"The labeler does not belong to this project.\",\n            \"content\": {\n                \"application/json\": {\n                    \"example\": {\n                        \"error\": {\n                            \"code\": \"LABELER_NOT_IN_PROJECT\",\n                            \"message\": \"Labeler is not in this project\",\n                            \"details\": [],\n                        }\n                    }\n                }\n            },\n        },\n        404: {\n            \"description\": \"Project not found.\",\n            \"content\": {\n                \"application/json\": {\n                    \"example\": {\n                        \"error\": {\n                            \"code\": \"PROJECT_NOT_FOUND\",\n                            \"message\": \"Project not found\",\n                            \"details\": [],\n                        }\n                    }\n                }\n            },\n        },\n    },\n)\n@inject\nasync def create_assignment(\n    project_id: UUID,\n    request: CreateAssignmentRequest,\n    use_case: CreateAssignmentUseCase = Depends(Provide[Container.create_assignment_use_case]),\n    storage: StorageService = Depends(Provide[Container.storage_service]),\n    assignment_timeout_seconds: int = Depends(\n        Provide[Container.config.tasks.image_assignment_timeout_seconds.as_int()]\n    ),\n) -&gt; AssignmentResponse:\n    \"\"\"Assign the next pending image to a labeler and return a presigned URL.\n\n    Args:\n        project_id: The project to assign an image from.\n        request: Contains the labeler ID to assign the image to.\n        use_case: Injected assignment use case.\n        storage: Injected storage service for presigned URL generation.\n\n    Returns:\n        An AssignmentResponse with image details and a presigned URL.\n    \"\"\"\n    image = await use_case.execute(project_id=project_id, labeler_id=request.labeler_id)\n    assert image.assignment_id is not None\n    timeout = timedelta(seconds=assignment_timeout_seconds)\n    presigned_url = await storage.get_presigned_url(image.storage_key, expires_in=timeout)\n    logger.info(\n        \"Assignment created: project=%s image=%s labeler=%s\",\n        project_id,\n        image.id,\n        request.labeler_id,\n    )\n    return AssignmentResponse(\n        id=image.id,\n        project_id=image.project_id,\n        status=image.status,\n        assignment_id=image.assignment_id,\n        presigned_url=presigned_url,\n        presigned_url_expires_in=assignment_timeout_seconds,\n    )\n</code></pre>"},{"location":"reference/api/routers/#labelers","title":"Labelers","text":""},{"location":"reference/api/routers/#ulabel.api.routers.labelers","title":"<code>labelers</code>","text":"<p>Router for labeler-related endpoints.</p> <p>Provides endpoints for searching labelers by username prefix and listing the projects assigned to a specific labeler.</p>"},{"location":"reference/api/routers/#ulabel.api.routers.labelers.LabelerAutocompleteItem","title":"<code>LabelerAutocompleteItem</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Schema for a single labeler autocomplete suggestion.</p>"},{"location":"reference/api/routers/#ulabel.api.routers.labelers.autocomplete_labelers","title":"<code>autocomplete_labelers(q=Query(min_length=1, description='Username prefix to search for'), limit=Query(default=10, ge=1, le=50), use_case=Depends(Provide[Container.search_labelers_use_case]))</code>  <code>async</code>","text":"<p>Search labelers by username prefix for autocomplete.</p> <p>Parameters:</p> Name Type Description Default <code>q</code> <code>str</code> <p>Username prefix to search for.</p> <code>Query(min_length=1, description='Username prefix to search for')</code> <code>limit</code> <code>int</code> <p>Maximum number of results to return.</p> <code>Query(default=10, ge=1, le=50)</code> <code>use_case</code> <code>SearchLabelersUseCase</code> <p>Injected search-labelers use case.</p> <code>Depends(Provide[search_labelers_use_case])</code> <p>Returns:</p> Type Description <code>list[LabelerAutocompleteItem]</code> <p>A list of matching labeler autocomplete items.</p> Source code in <code>src/ulabel/api/routers/labelers.py</code> <pre><code>@router.get(\n    \"/autocomplete\",\n    response_model=list[LabelerAutocompleteItem],\n    summary=\"Autocomplete labeler usernames\",\n    description=\"Search labelers by username prefix. Returns up to `limit` matches.\",\n)\n@inject\nasync def autocomplete_labelers(\n    q: str = Query(min_length=1, description=\"Username prefix to search for\"),\n    limit: int = Query(default=10, ge=1, le=50),\n    use_case: SearchLabelersUseCase = Depends(Provide[Container.search_labelers_use_case]),\n) -&gt; list[LabelerAutocompleteItem]:\n    \"\"\"Search labelers by username prefix for autocomplete.\n\n    Args:\n        q: Username prefix to search for.\n        limit: Maximum number of results to return.\n        use_case: Injected search-labelers use case.\n\n    Returns:\n        A list of matching labeler autocomplete items.\n    \"\"\"\n    results = await use_case.execute(prefix=q, limit=limit)\n    return [LabelerAutocompleteItem(id=u.id, username=u.username) for u in results]\n</code></pre>"},{"location":"reference/api/routers/#ulabel.api.routers.labelers.get_labeler_projects","title":"<code>get_labeler_projects(labeler_id, use_case=Depends(Provide[Container.get_labeler_projects_use_case]))</code>  <code>async</code>","text":"<p>List all projects assigned to a labeler.</p> <p>Parameters:</p> Name Type Description Default <code>labeler_id</code> <code>UUID</code> <p>The labeler's user ID.</p> required <code>use_case</code> <code>GetLabelerProjectsUseCase</code> <p>Injected get-labeler-projects use case.</p> <code>Depends(Provide[get_labeler_projects_use_case])</code> <p>Returns:</p> Type Description <code>list[ProjectSummary]</code> <p>A list of ProjectSummary objects for the labeler's projects.</p> Source code in <code>src/ulabel/api/routers/labelers.py</code> <pre><code>@router.get(\n    \"/{labeler_id}/projects\",\n    response_model=list[ProjectSummary],\n    summary=\"List labeler projects\",\n    description=\"\"\"\nReturns all projects the labeler is assigned to.\n\nThe user must exist and have the `labeler` role. If the user exists but has the\n`admin` role, a 403 is returned.\n\nUse this response to show the labeler which projects they can work on and which\nlabels are available in each one.\n\"\"\",\n    responses={\n        200: {\"description\": \"List of projects assigned to the labeler.\"},\n        403: {\n            \"description\": \"The user does not have the `labeler` role.\",\n            \"content\": {\n                \"application/json\": {\n                    \"example\": {\n                        \"error\": {\n                            \"code\": \"UNAUTHORIZED\",\n                            \"message\": \"User is not a labeler\",\n                            \"details\": [],\n                        }\n                    }\n                }\n            },\n        },\n        404: {\n            \"description\": \"Labeler not found.\",\n            \"content\": {\n                \"application/json\": {\n                    \"example\": {\n                        \"error\": {\n                            \"code\": \"USER_NOT_FOUND\",\n                            \"message\": \"Labeler not found\",\n                            \"details\": [],\n                        }\n                    }\n                }\n            },\n        },\n    },\n)\n@inject\nasync def get_labeler_projects(\n    labeler_id: UUID,\n    use_case: GetLabelerProjectsUseCase = Depends(Provide[Container.get_labeler_projects_use_case]),\n) -&gt; list[ProjectSummary]:\n    \"\"\"List all projects assigned to a labeler.\n\n    Args:\n        labeler_id: The labeler's user ID.\n        use_case: Injected get-labeler-projects use case.\n\n    Returns:\n        A list of ProjectSummary objects for the labeler's projects.\n    \"\"\"\n    projects = await use_case.execute(labeler_id=labeler_id)\n    return [\n        ProjectSummary(\n            id=p.id,\n            owner_id=p.owner.id,\n            name=p.name,\n            description=p.description,\n            labels=p.labels,\n            labeler_count=len(p.labeler_ids),\n            created_at=p.created_at,\n        )\n        for p in projects\n    ]\n</code></pre>"},{"location":"reference/api/routers/#exports","title":"Exports","text":""},{"location":"reference/api/routers/#ulabel.api.routers.exports","title":"<code>exports</code>","text":"<p>Router for label export endpoints.</p> <p>Provides an endpoint to export all label data for a project as a downloadable file via a presigned redirect URL.</p>"},{"location":"reference/api/routers/#ulabel.api.routers.exports.export_labels","title":"<code>export_labels(project_id, fmt=Query(default=(ExportFormat.JSON), alias='format'), use_case=Depends(Provide[Container.export_labels_use_case]))</code>  <code>async</code>","text":"<p>Export project labels and redirect to a presigned download URL.</p> <p>Parameters:</p> Name Type Description Default <code>project_id</code> <code>UUID</code> <p>The project whose labels to export.</p> required <code>fmt</code> <code>ExportFormat</code> <p>The desired export format (defaults to JSON).</p> <code>Query(default=JSON, alias='format')</code> <code>use_case</code> <code>ExportLabelsUseCase</code> <p>Injected export use case.</p> <code>Depends(Provide[export_labels_use_case])</code> <p>Returns:</p> Type Description <code>RedirectResponse</code> <p>A 307 redirect to the presigned URL for downloading the export file.</p> Source code in <code>src/ulabel/api/routers/exports.py</code> <pre><code>@router.get(\n    \"/{project_id}/export\",\n    status_code=status.HTTP_307_TEMPORARY_REDIRECT,\n    summary=\"Export label data\",\n    description=\"\"\"\nExports all label records for the project as a downloadable file.\n\nThe response is a **307 redirect** to a presigned MinIO URL where the file can be\ndownloaded directly. The file is cached; subsequent requests on the same day return\nthe cached version unless new labels have been submitted.\n\"\"\",\n    responses={\n        307: {\"description\": \"Redirect to presigned download URL.\"},\n        404: {\n            \"description\": \"Project not found or no labels exist yet.\",\n            \"content\": {\n                \"application/json\": {\n                    \"example\": {\n                        \"error\": {\n                            \"code\": \"PROJECT_NOT_FOUND\",\n                            \"message\": \"Project not found\",\n                            \"details\": [],\n                        }\n                    }\n                }\n            },\n        },\n    },\n)\n@inject\nasync def export_labels(\n    project_id: UUID,\n    fmt: ExportFormat = Query(default=ExportFormat.JSON, alias=\"format\"),\n    use_case: ExportLabelsUseCase = Depends(Provide[Container.export_labels_use_case]),\n) -&gt; RedirectResponse:\n    \"\"\"Export project labels and redirect to a presigned download URL.\n\n    Args:\n        project_id: The project whose labels to export.\n        fmt: The desired export format (defaults to JSON).\n        use_case: Injected export use case.\n\n    Returns:\n        A 307 redirect to the presigned URL for downloading the export file.\n    \"\"\"\n    result = await use_case.execute(project_id=project_id, fmt=fmt)\n    if result.cache_hit:\n        logger.info(\"Export cache hit: project=%s format=%s\", project_id, fmt.value)\n    else:\n        logger.info(\"Export generated: project=%s format=%s\", project_id, fmt.value)\n    return RedirectResponse(url=result.url, status_code=status.HTTP_307_TEMPORARY_REDIRECT)\n</code></pre>"},{"location":"reference/api/routers/#stats","title":"Stats","text":""},{"location":"reference/api/routers/#ulabel.api.routers.stats","title":"<code>stats</code>","text":"<p>Router for project statistics endpoints.</p> <p>Provides an endpoint to retrieve labelling statistics for a project, including image counts, class distribution, and per-labeler activity.</p>"},{"location":"reference/api/routers/#ulabel.api.routers.stats.get_project_stats","title":"<code>get_project_stats(project_id, use_case=Depends(Provide[Container.get_project_stats_use_case]))</code>  <code>async</code>","text":"<p>Retrieve labelling statistics for a project.</p> <p>Parameters:</p> Name Type Description Default <code>project_id</code> <code>UUID</code> <p>The project to get statistics for.</p> required <code>use_case</code> <code>GetProjectStatsUseCase</code> <p>Injected get-project-stats use case.</p> <code>Depends(Provide[get_project_stats_use_case])</code> <p>Returns:</p> Type Description <code>ProjectStatsResponse</code> <p>A ProjectStatsResponse with image counts, class distribution,</p> <code>ProjectStatsResponse</code> <p>per-labeler breakdowns, and daily activity data.</p> Source code in <code>src/ulabel/api/routers/stats.py</code> <pre><code>@router.get(\n    \"/{project_id}/stats\",\n    response_model=ProjectStatsResponse,\n    summary=\"Get project statistics\",\n    description=\"\"\"\nReturns labelling statistics for a project, including total/labeled image counts,\nclass distribution, per-labeler breakdowns, and daily activity.\n\"\"\",\n    responses={\n        404: {\n            \"description\": \"Project not found.\",\n            \"content\": {\n                \"application/json\": {\n                    \"example\": {\n                        \"error\": {\n                            \"code\": \"PROJECT_NOT_FOUND\",\n                            \"message\": \"Project not found\",\n                            \"details\": [],\n                        }\n                    }\n                }\n            },\n        },\n    },\n)\n@inject\nasync def get_project_stats(\n    project_id: UUID,\n    use_case: GetProjectStatsUseCase = Depends(Provide[Container.get_project_stats_use_case]),\n) -&gt; ProjectStatsResponse:\n    \"\"\"Retrieve labelling statistics for a project.\n\n    Args:\n        project_id: The project to get statistics for.\n        use_case: Injected get-project-stats use case.\n\n    Returns:\n        A ProjectStatsResponse with image counts, class distribution,\n        per-labeler breakdowns, and daily activity data.\n    \"\"\"\n    stats = await use_case.execute(project_id=project_id)\n\n    return ProjectStatsResponse(\n        total_images=stats.total_images,\n        labeled_images=stats.labeled_images,\n        class_distribution=stats.class_distribution,\n        labeler_class_counts=[\n            LabelerClassCountSchema(\n                labeler_id=lc.labeler_id, username=lc.username, counts=lc.counts\n            )\n            for lc in stats.labeler_class_counts\n        ],\n        labeler_daily_activity=[\n            LabelerDailyActivitySchema(\n                labeler_id=la.labeler_id,\n                username=la.username,\n                daily=[DailyCountSchema(date=d.date, counts=d.counts) for d in la.daily],\n            )\n            for la in stats.labeler_daily_activity\n        ],\n        daily_totals=[\n            DailyTotalSchema(\n                date=dt.date,\n                labelers=[\n                    DailyLabelerTotalSchema(\n                        labeler_id=dl.labeler_id, username=dl.username, count=dl.count\n                    )\n                    for dl in dt.labelers\n                ],\n            )\n            for dt in stats.daily_totals\n        ],\n    )\n</code></pre>"},{"location":"reference/api/routers/#tokens","title":"Tokens","text":""},{"location":"reference/api/routers/#ulabel.api.routers.tokens","title":"<code>tokens</code>","text":"<p>Router for authentication endpoints.</p> <p>Provides a simple sign-in endpoint that authenticates users by username and returns their identity and role.</p>"},{"location":"reference/api/routers/#ulabel.api.routers.tokens.login","title":"<code>login(request, use_case=Depends(Provide[Container.login_use_case]))</code>  <code>async</code>","text":"<p>Authenticate a user by username and return their claim.</p> <p>Parameters:</p> Name Type Description Default <code>request</code> <code>LoginRequest</code> <p>Contains the username to authenticate.</p> required <code>use_case</code> <code>LoginUseCase</code> <p>Injected login use case.</p> <code>Depends(Provide[login_use_case])</code> <p>Returns:</p> Type Description <code>Claim</code> <p>A Claim with the user's ID, username, and role.</p> Source code in <code>src/ulabel/api/routers/tokens.py</code> <pre><code>@router.post(\n    \"\",\n    response_model=Claim,\n    summary=\"Sign in\",\n    description=\"\"\"\nAuthenticates a user by username and returns their session information (ID and role).\n\n&gt; **Note:** no password is required. The system assumes the username is unique\n&gt; and was pre-created in the database.\n\"\"\",\n    responses={\n        200: {\"description\": \"Sign-in successful. Returns the user's ID and role.\"},\n        404: {\n            \"description\": \"User not found.\",\n            \"content\": {\n                \"application/json\": {\n                    \"example\": {\n                        \"error\": {\n                            \"code\": \"USER_NOT_FOUND\",\n                            \"message\": \"User not found\",\n                            \"details\": [],\n                        }\n                    }\n                }\n            },\n        },\n    },\n)\n@inject\nasync def login(\n    request: LoginRequest,\n    use_case: LoginUseCase = Depends(Provide[Container.login_use_case]),\n) -&gt; Claim:\n    \"\"\"Authenticate a user by username and return their claim.\n\n    Args:\n        request: Contains the username to authenticate.\n        use_case: Injected login use case.\n\n    Returns:\n        A Claim with the user's ID, username, and role.\n    \"\"\"\n    user = await use_case.execute(request.username)\n    logger.info(\"User logged in: id=%s username=%s role=%s\", user.id, user.username, user.role)\n    return Claim(username=user.username, id=user.id, role=user.role)\n</code></pre>"},{"location":"reference/api/schemas/","title":"API Schemas","text":"<p>Pydantic models for request/response validation and serialization.</p>"},{"location":"reference/api/schemas/#projects","title":"Projects","text":""},{"location":"reference/api/schemas/#ulabel.api.schemas.projects","title":"<code>projects</code>","text":"<p>Pydantic schemas for project-related requests and responses.</p>"},{"location":"reference/api/schemas/#ulabel.api.schemas.projects.CreateProjectRequest","title":"<code>CreateProjectRequest</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Request body for creating a new labelling project.</p>"},{"location":"reference/api/schemas/#ulabel.api.schemas.projects.AddLabelerRequest","title":"<code>AddLabelerRequest</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Request body for adding a labeler to a project.</p>"},{"location":"reference/api/schemas/#ulabel.api.schemas.projects.UpdateProjectRequest","title":"<code>UpdateProjectRequest</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Request body for updating a project's mutable fields.</p>"},{"location":"reference/api/schemas/#ulabel.api.schemas.projects.LabelerInfo","title":"<code>LabelerInfo</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Summary information for a labeler assigned to a project.</p>"},{"location":"reference/api/schemas/#ulabel.api.schemas.projects.ProjectSummary","title":"<code>ProjectSummary</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Lightweight project representation for list endpoints.</p>"},{"location":"reference/api/schemas/#ulabel.api.schemas.projects.ProjectDetail","title":"<code>ProjectDetail</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Full project representation with resolved labeler details.</p>"},{"location":"reference/api/schemas/#ulabel.api.schemas.projects.PaginatedProjectSummaryResponse","title":"<code>PaginatedProjectSummaryResponse</code>","text":"<p>               Bases: <code>PaginatedResponse[ProjectSummary]</code></p> <p>Paginated response containing a list of project summaries.</p>"},{"location":"reference/api/schemas/#images","title":"Images","text":""},{"location":"reference/api/schemas/#ulabel.api.schemas.images","title":"<code>images</code>","text":"<p>Pydantic schemas for image-related requests and responses.</p>"},{"location":"reference/api/schemas/#ulabel.api.schemas.images.AddImageRequest","title":"<code>AddImageRequest</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Request body for registering an existing storage object as a project image.</p>"},{"location":"reference/api/schemas/#ulabel.api.schemas.images.ImageResponse","title":"<code>ImageResponse</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Response body for an image resource.</p>"},{"location":"reference/api/schemas/#ulabel.api.schemas.images.SubmitLabelRequest","title":"<code>SubmitLabelRequest</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Request body for submitting a label for an assigned image.</p>"},{"location":"reference/api/schemas/#ulabel.api.schemas.images.LabelRecordResponse","title":"<code>LabelRecordResponse</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Response body for a label record.</p>"},{"location":"reference/api/schemas/#ulabel.api.schemas.images.SubmitLabelResponse","title":"<code>SubmitLabelResponse</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Response body for a submitted label, including labeler ranking.</p>"},{"location":"reference/api/schemas/#ulabel.api.schemas.images.ImportImagesRequest","title":"<code>ImportImagesRequest</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Request body for starting a bulk image import from storage.</p>"},{"location":"reference/api/schemas/#ulabel.api.schemas.images.ImportJobResponse","title":"<code>ImportJobResponse</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Response body for an import job with its current status.</p>"},{"location":"reference/api/schemas/#assignments","title":"Assignments","text":""},{"location":"reference/api/schemas/#ulabel.api.schemas.assignments","title":"<code>assignments</code>","text":"<p>Pydantic schemas for image assignment requests and responses.</p>"},{"location":"reference/api/schemas/#ulabel.api.schemas.assignments.CreateAssignmentRequest","title":"<code>CreateAssignmentRequest</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Request body for creating a new image assignment.</p>"},{"location":"reference/api/schemas/#ulabel.api.schemas.assignments.AssignmentResponse","title":"<code>AssignmentResponse</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Response body for a created image assignment with presigned URL.</p>"},{"location":"reference/api/schemas/#pagination","title":"Pagination","text":""},{"location":"reference/api/schemas/#ulabel.api.schemas.pagination","title":"<code>pagination</code>","text":"<p>Generic pagination schema for paginated API responses.</p>"},{"location":"reference/api/schemas/#ulabel.api.schemas.pagination.PaginatedResponse","title":"<code>PaginatedResponse</code>","text":"<p>               Bases: <code>BaseModel</code>, <code>Generic[T]</code></p> <p>Generic paginated response wrapper with items, total count, and offset.</p>"},{"location":"reference/api/schemas/#stats","title":"Stats","text":""},{"location":"reference/api/schemas/#ulabel.api.schemas.stats","title":"<code>stats</code>","text":"<p>Pydantic schemas for project statistics responses.</p>"},{"location":"reference/api/schemas/#ulabel.api.schemas.stats.LabelerClassCountSchema","title":"<code>LabelerClassCountSchema</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Per-labeler label class counts within a project.</p>"},{"location":"reference/api/schemas/#ulabel.api.schemas.stats.DailyCountSchema","title":"<code>DailyCountSchema</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Label counts by class for a single day.</p>"},{"location":"reference/api/schemas/#ulabel.api.schemas.stats.LabelerDailyActivitySchema","title":"<code>LabelerDailyActivitySchema</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Daily labelling activity breakdown for a single labeler.</p>"},{"location":"reference/api/schemas/#ulabel.api.schemas.stats.DailyLabelerTotalSchema","title":"<code>DailyLabelerTotalSchema</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Total label count for a single labeler on a single day.</p>"},{"location":"reference/api/schemas/#ulabel.api.schemas.stats.DailyTotalSchema","title":"<code>DailyTotalSchema</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Aggregate daily totals with per-labeler breakdowns.</p>"},{"location":"reference/api/schemas/#ulabel.api.schemas.stats.ProjectStatsResponse","title":"<code>ProjectStatsResponse</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Complete labelling statistics for a project.</p>"},{"location":"reference/api/schemas/#tokens","title":"Tokens","text":""},{"location":"reference/api/schemas/#ulabel.api.schemas.tokens","title":"<code>tokens</code>","text":"<p>Pydantic schemas for authentication requests and responses.</p>"},{"location":"reference/api/schemas/#ulabel.api.schemas.tokens.LoginRequest","title":"<code>LoginRequest</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Request body for user sign-in by username.</p>"},{"location":"reference/api/schemas/#ulabel.api.schemas.tokens.Claim","title":"<code>Claim</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Response body containing the authenticated user's identity and role.</p>"},{"location":"reference/api/schemas/#errors","title":"Errors","text":""},{"location":"reference/api/schemas/#ulabel.api.schemas.errors","title":"<code>errors</code>","text":"<p>Pydantic schemas for structured API error responses.</p>"},{"location":"reference/api/schemas/#ulabel.api.schemas.errors.ErrorBody","title":"<code>ErrorBody</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Inner error object containing code, message, and details.</p>"},{"location":"reference/api/schemas/#ulabel.api.schemas.errors.ErrorResponse","title":"<code>ErrorResponse</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Wrapper schema for API error responses.</p>"},{"location":"reference/application/use-cases/","title":"Use Cases","text":"<p>Application-layer services implementing business workflows.</p>"},{"location":"reference/application/use-cases/#project-management","title":"Project Management","text":""},{"location":"reference/application/use-cases/#create-project","title":"Create Project","text":""},{"location":"reference/application/use-cases/#ulabel.application.create_project","title":"<code>create_project</code>","text":"<p>Use case for creating a new labeling project.</p>"},{"location":"reference/application/use-cases/#ulabel.application.create_project.Unauthorized","title":"<code>Unauthorized</code>","text":"<p>               Bases: <code>DomainError</code></p> <p>Raised when a user lacks the required role for an operation.</p>"},{"location":"reference/application/use-cases/#ulabel.application.create_project.ProjectNameAlreadyExists","title":"<code>ProjectNameAlreadyExists</code>","text":"<p>               Bases: <code>DomainError</code></p> <p>Raised when a project name is already taken.</p>"},{"location":"reference/application/use-cases/#ulabel.application.create_project.CreateProjectUseCase","title":"<code>CreateProjectUseCase(uow)</code>","text":"<p>Creates a new labeling project owned by an admin user.</p> <p>Validates that the owner exists and has admin privileges, and that the project name is unique.</p> <p>Initialize the use case.</p> <p>Parameters:</p> Name Type Description Default <code>uow</code> <code>UnitOfWork</code> <p>Unit of Work for transactional repository access.</p> required Source code in <code>src/ulabel/application/create_project.py</code> <pre><code>def __init__(self, uow: UnitOfWork):\n    \"\"\"Initialize the use case.\n\n    Args:\n        uow: Unit of Work for transactional repository access.\n    \"\"\"\n    self._uow = uow\n</code></pre>"},{"location":"reference/application/use-cases/#ulabel.application.create_project.CreateProjectUseCase.execute","title":"<code>execute(owner_id, name, description, labels)</code>  <code>async</code>","text":"<p>Create a new labeling project.</p> <p>Parameters:</p> Name Type Description Default <code>owner_id</code> <code>UUID</code> <p>The ID of the admin user who will own the project.</p> required <code>name</code> <code>str</code> <p>The unique display name for the project.</p> required <code>description</code> <code>str</code> <p>A brief description of the project.</p> required <code>labels</code> <code>set[str]</code> <p>The set of allowed label values for the project.</p> required <p>Returns:</p> Type Description <code>Project</code> <p>The newly created project entity.</p> <p>Raises:</p> Type Description <code>UserNotFound</code> <p>If the owner user does not exist.</p> <code>Unauthorized</code> <p>If the owner is not an admin.</p> <code>ProjectNameAlreadyExists</code> <p>If a project with the given name already exists.</p> Source code in <code>src/ulabel/application/create_project.py</code> <pre><code>async def execute(\n    self,\n    owner_id: UUID,\n    name: str,\n    description: str,\n    labels: set[str],\n) -&gt; Project:\n    \"\"\"Create a new labeling project.\n\n    Args:\n        owner_id: The ID of the admin user who will own the project.\n        name: The unique display name for the project.\n        description: A brief description of the project.\n        labels: The set of allowed label values for the project.\n\n    Returns:\n        The newly created project entity.\n\n    Raises:\n        UserNotFound: If the owner user does not exist.\n        Unauthorized: If the owner is not an admin.\n        ProjectNameAlreadyExists: If a project with the given name already exists.\n    \"\"\"\n    async with self._uow as uow:\n        owner = await uow.user_repository.get_by_id(owner_id)\n        if owner is None:\n            raise UserNotFound(\"Owner not found\")\n        if owner.role != UserRole.ADMIN:\n            raise Unauthorized(\"Owner is not an admin\")\n\n        existing = await uow.project_repository.get_by_name(name)\n        if existing is not None:\n            raise ProjectNameAlreadyExists(\"Project name already exists\")\n\n        project = Project.create(\n            id=uuid4(),\n            owner=owner,\n            name=name,\n            description=description,\n            labels=labels,\n            created_at=datetime.now(timezone.utc),\n        )\n        await uow.project_repository.save(project)\n        await uow.commit()\n        return project\n</code></pre>"},{"location":"reference/application/use-cases/#update-project","title":"Update Project","text":""},{"location":"reference/application/use-cases/#ulabel.application.update_project","title":"<code>update_project</code>","text":"<p>Use case for updating an existing labeling project.</p>"},{"location":"reference/application/use-cases/#ulabel.application.update_project.UpdateProjectUseCase","title":"<code>UpdateProjectUseCase(uow)</code>","text":"<p>Updates project metadata and labeler assignments.</p> <p>Validates name uniqueness and labeler roles before persisting changes.</p> <p>Initialize the use case.</p> <p>Parameters:</p> Name Type Description Default <code>uow</code> <code>UnitOfWork</code> <p>Unit of Work for transactional repository access.</p> required Source code in <code>src/ulabel/application/update_project.py</code> <pre><code>def __init__(self, uow: UnitOfWork):\n    \"\"\"Initialize the use case.\n\n    Args:\n        uow: Unit of Work for transactional repository access.\n    \"\"\"\n    self._uow = uow\n</code></pre>"},{"location":"reference/application/use-cases/#ulabel.application.update_project.UpdateProjectUseCase.execute","title":"<code>execute(project_id, *, name=None, description=None, labeler_ids=None)</code>  <code>async</code>","text":"<p>Update a project's name, description, and/or labeler assignments.</p> <p>Parameters:</p> Name Type Description Default <code>project_id</code> <code>UUID</code> <p>The ID of the project to update.</p> required <code>name</code> <code>str | None</code> <p>New project name, or None to keep the current name.</p> <code>None</code> <code>description</code> <code>str | None</code> <p>New description, or None to keep the current description.</p> <code>None</code> <code>labeler_ids</code> <code>set[UUID] | None</code> <p>New set of labeler IDs, or None to keep the current set.</p> <code>None</code> <p>Returns:</p> Type Description <code>Project</code> <p>The updated project entity.</p> <p>Raises:</p> Type Description <code>ProjectNotFound</code> <p>If the project does not exist.</p> <code>ProjectNameAlreadyExists</code> <p>If the new name conflicts with another project.</p> <code>UserNotFound</code> <p>If a labeler ID does not correspond to an existing user.</p> <code>Unauthorized</code> <p>If a labeler ID belongs to a non-labeler user.</p> Source code in <code>src/ulabel/application/update_project.py</code> <pre><code>async def execute(\n    self,\n    project_id: UUID,\n    *,\n    name: str | None = None,\n    description: str | None = None,\n    labeler_ids: set[UUID] | None = None,\n) -&gt; Project:\n    \"\"\"Update a project's name, description, and/or labeler assignments.\n\n    Args:\n        project_id: The ID of the project to update.\n        name: New project name, or None to keep the current name.\n        description: New description, or None to keep the current description.\n        labeler_ids: New set of labeler IDs, or None to keep the current set.\n\n    Returns:\n        The updated project entity.\n\n    Raises:\n        ProjectNotFound: If the project does not exist.\n        ProjectNameAlreadyExists: If the new name conflicts with another project.\n        UserNotFound: If a labeler ID does not correspond to an existing user.\n        Unauthorized: If a labeler ID belongs to a non-labeler user.\n    \"\"\"\n    async with self._uow as uow:\n        project = await uow.project_repository.get_by_id(project_id)\n        if project is None:\n            raise ProjectNotFound(\"Project not found\")\n\n        if name is not None and name != project.name:\n            existing = await uow.project_repository.get_by_name(name)\n            if existing is not None:\n                raise ProjectNameAlreadyExists(\"Project name already exists\")\n\n        project.update(name=name, description=description)\n\n        if labeler_ids is not None:\n            users = await uow.user_repository.get_by_ids(labeler_ids)\n            if len(users) != len(labeler_ids):\n                raise UserNotFound(\"Labeler not found\")\n            if any(u.role != UserRole.LABELER for u in users):\n                raise Unauthorized(\"User is not a labeler\")\n            project.set_labelers(labeler_ids)\n\n        await uow.project_repository.save(project)\n        await uow.commit()\n        return project\n</code></pre>"},{"location":"reference/application/use-cases/#list-projects","title":"List Projects","text":""},{"location":"reference/application/use-cases/#ulabel.application.list_projects","title":"<code>list_projects</code>","text":"<p>Use case for listing labeling projects with pagination and optional filtering.</p>"},{"location":"reference/application/use-cases/#ulabel.application.list_projects.ListProjectsUseCase","title":"<code>ListProjectsUseCase(uow)</code>","text":"<p>Lists projects with pagination and optional name filtering.</p> <p>Initialize the use case.</p> <p>Parameters:</p> Name Type Description Default <code>uow</code> <code>UnitOfWork</code> <p>Unit of Work for transactional repository access.</p> required Source code in <code>src/ulabel/application/list_projects.py</code> <pre><code>def __init__(self, uow: UnitOfWork):\n    \"\"\"Initialize the use case.\n\n    Args:\n        uow: Unit of Work for transactional repository access.\n    \"\"\"\n    self._uow = uow\n</code></pre>"},{"location":"reference/application/use-cases/#ulabel.application.list_projects.ListProjectsUseCase.execute","title":"<code>execute(limit, offset, name=None)</code>  <code>async</code>","text":"<p>List projects with pagination.</p> <p>Parameters:</p> Name Type Description Default <code>limit</code> <code>int</code> <p>Maximum number of projects to return.</p> required <code>offset</code> <code>int</code> <p>Number of projects to skip.</p> required <code>name</code> <code>str | None</code> <p>Optional name filter for partial matching.</p> <code>None</code> <p>Returns:</p> Type Description <code>PaginatedResult[Project]</code> <p>A paginated result containing the matching projects.</p> Source code in <code>src/ulabel/application/list_projects.py</code> <pre><code>async def execute(\n    self,\n    limit: int,\n    offset: int,\n    name: str | None = None,\n) -&gt; PaginatedResult[Project]:\n    \"\"\"List projects with pagination.\n\n    Args:\n        limit: Maximum number of projects to return.\n        offset: Number of projects to skip.\n        name: Optional name filter for partial matching.\n\n    Returns:\n        A paginated result containing the matching projects.\n    \"\"\"\n    async with self._uow as uow:\n        return await uow.project_repository.get_all(limit=limit, offset=offset, name=name)\n</code></pre>"},{"location":"reference/application/use-cases/#labeler-management","title":"Labeler Management","text":""},{"location":"reference/application/use-cases/#add-labeler-to-project","title":"Add Labeler to Project","text":""},{"location":"reference/application/use-cases/#ulabel.application.add_labeler_to_project","title":"<code>add_labeler_to_project</code>","text":"<p>Use case for assigning a labeler to a labeling project.</p>"},{"location":"reference/application/use-cases/#ulabel.application.add_labeler_to_project.ProjectNotFound","title":"<code>ProjectNotFound</code>","text":"<p>               Bases: <code>DomainError</code></p> <p>Raised when a referenced project does not exist.</p>"},{"location":"reference/application/use-cases/#ulabel.application.add_labeler_to_project.AddLabelerToProjectUseCase","title":"<code>AddLabelerToProjectUseCase(uow)</code>","text":"<p>Adds a labeler user to a project so they can receive assignments.</p> <p>Validates that both the project and labeler exist, and that the user holds the labeler role.</p> <p>Initialize the use case.</p> <p>Parameters:</p> Name Type Description Default <code>uow</code> <code>UnitOfWork</code> <p>Unit of Work for transactional repository access.</p> required Source code in <code>src/ulabel/application/add_labeler_to_project.py</code> <pre><code>def __init__(self, uow: UnitOfWork):\n    \"\"\"Initialize the use case.\n\n    Args:\n        uow: Unit of Work for transactional repository access.\n    \"\"\"\n    self._uow = uow\n</code></pre>"},{"location":"reference/application/use-cases/#ulabel.application.add_labeler_to_project.AddLabelerToProjectUseCase.execute","title":"<code>execute(project_id, labeler_id)</code>  <code>async</code>","text":"<p>Add a labeler to a project.</p> <p>Parameters:</p> Name Type Description Default <code>project_id</code> <code>UUID</code> <p>The ID of the target project.</p> required <code>labeler_id</code> <code>UUID</code> <p>The ID of the user to add as a labeler.</p> required <p>Returns:</p> Type Description <code>Project</code> <p>The updated project entity with the labeler added.</p> <p>Raises:</p> Type Description <code>ProjectNotFound</code> <p>If the project does not exist.</p> <code>UserNotFound</code> <p>If the labeler user does not exist.</p> <code>Unauthorized</code> <p>If the user does not have the labeler role.</p> Source code in <code>src/ulabel/application/add_labeler_to_project.py</code> <pre><code>async def execute(self, project_id: UUID, labeler_id: UUID) -&gt; Project:\n    \"\"\"Add a labeler to a project.\n\n    Args:\n        project_id: The ID of the target project.\n        labeler_id: The ID of the user to add as a labeler.\n\n    Returns:\n        The updated project entity with the labeler added.\n\n    Raises:\n        ProjectNotFound: If the project does not exist.\n        UserNotFound: If the labeler user does not exist.\n        Unauthorized: If the user does not have the labeler role.\n    \"\"\"\n    async with self._uow as uow:\n        project = await uow.project_repository.get_by_id(project_id)\n        if project is None:\n            raise ProjectNotFound(\"Project not found\")\n\n        labeler = await uow.user_repository.get_by_id(labeler_id)\n        if labeler is None:\n            raise UserNotFound(\"Labeler not found\")\n\n        if labeler.role != UserRole.LABELER:\n            raise Unauthorized(\"User is not a labeler\")\n\n        project.add_labeler(labeler_id)\n        await uow.project_repository.save(project)\n        await uow.commit()\n        return project\n</code></pre>"},{"location":"reference/application/use-cases/#search-labelers","title":"Search Labelers","text":""},{"location":"reference/application/use-cases/#ulabel.application.search_labelers","title":"<code>search_labelers</code>","text":"<p>Use case for searching labeler users by username prefix.</p>"},{"location":"reference/application/use-cases/#ulabel.application.search_labelers.SearchLabelersUseCase","title":"<code>SearchLabelersUseCase(uow)</code>","text":"<p>Searches for users with the labeler role by username prefix.</p> <p>Useful for autocomplete when assigning labelers to projects.</p> <p>Initialize the use case.</p> <p>Parameters:</p> Name Type Description Default <code>uow</code> <code>UnitOfWork</code> <p>Unit of Work for transactional repository access.</p> required Source code in <code>src/ulabel/application/search_labelers.py</code> <pre><code>def __init__(self, uow: UnitOfWork):\n    \"\"\"Initialize the use case.\n\n    Args:\n        uow: Unit of Work for transactional repository access.\n    \"\"\"\n    self._uow = uow\n</code></pre>"},{"location":"reference/application/use-cases/#ulabel.application.search_labelers.SearchLabelersUseCase.execute","title":"<code>execute(prefix, limit=10)</code>  <code>async</code>","text":"<p>Search for labelers whose username starts with the given prefix.</p> <p>Parameters:</p> Name Type Description Default <code>prefix</code> <code>str</code> <p>The username prefix to search for.</p> required <code>limit</code> <code>int</code> <p>Maximum number of results to return.</p> <code>10</code> <p>Returns:</p> Type Description <code>list[User]</code> <p>A list of matching labeler users.</p> Source code in <code>src/ulabel/application/search_labelers.py</code> <pre><code>async def execute(self, prefix: str, limit: int = 10) -&gt; list[User]:\n    \"\"\"Search for labelers whose username starts with the given prefix.\n\n    Args:\n        prefix: The username prefix to search for.\n        limit: Maximum number of results to return.\n\n    Returns:\n        A list of matching labeler users.\n    \"\"\"\n    async with self._uow as uow:\n        return await uow.user_repository.search_by_username_prefix(\n            prefix, role=UserRole.LABELER, limit=limit\n        )\n</code></pre>"},{"location":"reference/application/use-cases/#get-labeler-projects","title":"Get Labeler Projects","text":""},{"location":"reference/application/use-cases/#ulabel.application.get_labeler_projects","title":"<code>get_labeler_projects</code>","text":"<p>Use case for retrieving all projects assigned to a labeler.</p>"},{"location":"reference/application/use-cases/#ulabel.application.get_labeler_projects.GetLabelerProjectsUseCase","title":"<code>GetLabelerProjectsUseCase(uow)</code>","text":"<p>Retrieves the list of projects a labeler is assigned to.</p> <p>Validates that the user exists and holds the labeler role.</p> <p>Initialize the use case.</p> <p>Parameters:</p> Name Type Description Default <code>uow</code> <code>UnitOfWork</code> <p>Unit of Work for transactional repository access.</p> required Source code in <code>src/ulabel/application/get_labeler_projects.py</code> <pre><code>def __init__(self, uow: UnitOfWork):\n    \"\"\"Initialize the use case.\n\n    Args:\n        uow: Unit of Work for transactional repository access.\n    \"\"\"\n    self._uow = uow\n</code></pre>"},{"location":"reference/application/use-cases/#ulabel.application.get_labeler_projects.GetLabelerProjectsUseCase.execute","title":"<code>execute(labeler_id)</code>  <code>async</code>","text":"<p>Get all projects for a labeler.</p> <p>Parameters:</p> Name Type Description Default <code>labeler_id</code> <code>UUID</code> <p>The ID of the labeler user.</p> required <p>Returns:</p> Type Description <code>list[Project]</code> <p>A list of projects the labeler is assigned to.</p> <p>Raises:</p> Type Description <code>UserNotFound</code> <p>If the labeler user does not exist.</p> <code>Unauthorized</code> <p>If the user does not have the labeler role.</p> Source code in <code>src/ulabel/application/get_labeler_projects.py</code> <pre><code>async def execute(self, labeler_id: UUID) -&gt; list[Project]:\n    \"\"\"Get all projects for a labeler.\n\n    Args:\n        labeler_id: The ID of the labeler user.\n\n    Returns:\n        A list of projects the labeler is assigned to.\n\n    Raises:\n        UserNotFound: If the labeler user does not exist.\n        Unauthorized: If the user does not have the labeler role.\n    \"\"\"\n    async with self._uow as uow:\n        labeler = await uow.user_repository.get_by_id(labeler_id)\n        if labeler is None:\n            raise UserNotFound(\"Labeler not found\")\n        if labeler.role != UserRole.LABELER:\n            raise Unauthorized(\"User is not a labeler\")\n        return await uow.project_repository.get_by_labeler_id(labeler_id)\n</code></pre>"},{"location":"reference/application/use-cases/#login","title":"Login","text":""},{"location":"reference/application/use-cases/#ulabel.application.login","title":"<code>login</code>","text":"<p>Use case for authenticating a user by username.</p>"},{"location":"reference/application/use-cases/#ulabel.application.login.UserNotFound","title":"<code>UserNotFound</code>","text":"<p>               Bases: <code>DomainError</code></p> <p>Raised when a user with the given identifier does not exist.</p>"},{"location":"reference/application/use-cases/#ulabel.application.login.LoginUseCase","title":"<code>LoginUseCase(uow)</code>","text":"<p>Authenticates a user by looking up their username.</p> <p>Returns the user entity if found, otherwise raises an error.</p> <p>Initialize the use case.</p> <p>Parameters:</p> Name Type Description Default <code>uow</code> <code>UnitOfWork</code> <p>Unit of Work for transactional repository access.</p> required Source code in <code>src/ulabel/application/login.py</code> <pre><code>def __init__(self, uow: UnitOfWork):\n    \"\"\"Initialize the use case.\n\n    Args:\n        uow: Unit of Work for transactional repository access.\n    \"\"\"\n    self._uow = uow\n</code></pre>"},{"location":"reference/application/use-cases/#ulabel.application.login.LoginUseCase.execute","title":"<code>execute(username)</code>  <code>async</code>","text":"<p>Log in a user by username.</p> <p>Parameters:</p> Name Type Description Default <code>username</code> <code>str</code> <p>The username to authenticate.</p> required <p>Returns:</p> Type Description <code>User</code> <p>The authenticated user entity.</p> <p>Raises:</p> Type Description <code>UserNotFound</code> <p>If no user with the given username exists.</p> Source code in <code>src/ulabel/application/login.py</code> <pre><code>async def execute(self, username: str) -&gt; User:\n    \"\"\"Log in a user by username.\n\n    Args:\n        username: The username to authenticate.\n\n    Returns:\n        The authenticated user entity.\n\n    Raises:\n        UserNotFound: If no user with the given username exists.\n    \"\"\"\n    async with self._uow as uow:\n        user = await uow.user_repository.get_by_username(username)\n        if user is None:\n            raise UserNotFound(\"User not found\")\n        return user\n</code></pre>"},{"location":"reference/application/use-cases/#image-management","title":"Image Management","text":""},{"location":"reference/application/use-cases/#add-image-to-project","title":"Add Image to Project","text":""},{"location":"reference/application/use-cases/#ulabel.application.add_image_to_project","title":"<code>add_image_to_project</code>","text":"<p>Use case for adding an image reference to a labeling project.</p>"},{"location":"reference/application/use-cases/#ulabel.application.add_image_to_project.AddImageToProjectUseCase","title":"<code>AddImageToProjectUseCase(uow)</code>","text":"<p>Registers an already-uploaded image in a project by its storage key.</p> <p>Validates that the target project exists before persisting the image.</p> <p>Initialize the use case.</p> <p>Parameters:</p> Name Type Description Default <code>uow</code> <code>UnitOfWork</code> <p>Unit of Work for transactional repository access.</p> required Source code in <code>src/ulabel/application/add_image_to_project.py</code> <pre><code>def __init__(self, uow: UnitOfWork):\n    \"\"\"Initialize the use case.\n\n    Args:\n        uow: Unit of Work for transactional repository access.\n    \"\"\"\n    self._uow = uow\n</code></pre>"},{"location":"reference/application/use-cases/#ulabel.application.add_image_to_project.AddImageToProjectUseCase.execute","title":"<code>execute(project_id, storage_key)</code>  <code>async</code>","text":"<p>Add an image to a project.</p> <p>Parameters:</p> Name Type Description Default <code>project_id</code> <code>UUID</code> <p>The ID of the target project.</p> required <code>storage_key</code> <code>str</code> <p>The object storage key where the image data resides.</p> required <p>Returns:</p> Type Description <code>Image</code> <p>The newly created image entity.</p> <p>Raises:</p> Type Description <code>ProjectNotFound</code> <p>If the project does not exist.</p> Source code in <code>src/ulabel/application/add_image_to_project.py</code> <pre><code>async def execute(self, project_id: UUID, storage_key: str) -&gt; Image:\n    \"\"\"Add an image to a project.\n\n    Args:\n        project_id: The ID of the target project.\n        storage_key: The object storage key where the image data resides.\n\n    Returns:\n        The newly created image entity.\n\n    Raises:\n        ProjectNotFound: If the project does not exist.\n    \"\"\"\n    async with self._uow as uow:\n        project = await uow.project_repository.get_by_id(project_id)\n        if project is None:\n            raise ProjectNotFound(f\"Project '{project_id}' not found\")\n\n        image = Image.create(id=uuid4(), project_id=project_id, storage_key=storage_key)\n        await uow.image_repository.save(image)\n        await uow.commit()\n        return image\n</code></pre>"},{"location":"reference/application/use-cases/#upload-image-to-project","title":"Upload Image to Project","text":""},{"location":"reference/application/use-cases/#ulabel.application.upload_image_to_project","title":"<code>upload_image_to_project</code>","text":"<p>Use case for uploading an image file to a labeling project.</p>"},{"location":"reference/application/use-cases/#ulabel.application.upload_image_to_project.UploadImageToProjectUseCase","title":"<code>UploadImageToProjectUseCase(uow, storage_service)</code>","text":"<p>Uploads image data to object storage and registers it in a project.</p> <p>Handles both the storage upload and the creation of the image entity in a single operation.</p> <p>Initialize the use case.</p> <p>Parameters:</p> Name Type Description Default <code>uow</code> <code>UnitOfWork</code> <p>Unit of Work for transactional repository access.</p> required <code>storage_service</code> <code>StorageService</code> <p>Service for uploading files to object storage.</p> required Source code in <code>src/ulabel/application/upload_image_to_project.py</code> <pre><code>def __init__(self, uow: UnitOfWork, storage_service: StorageService):\n    \"\"\"Initialize the use case.\n\n    Args:\n        uow: Unit of Work for transactional repository access.\n        storage_service: Service for uploading files to object storage.\n    \"\"\"\n    self._uow = uow\n    self.storage_service = storage_service\n</code></pre>"},{"location":"reference/application/use-cases/#ulabel.application.upload_image_to_project.UploadImageToProjectUseCase.execute","title":"<code>execute(project_id, data, content_type)</code>  <code>async</code>","text":"<p>Upload an image to a project.</p> <p>Parameters:</p> Name Type Description Default <code>project_id</code> <code>UUID</code> <p>The project to add the image to.</p> required <code>data</code> <code>bytes</code> <p>The raw image bytes.</p> required <code>content_type</code> <code>str</code> <p>The MIME type of the image.</p> required <p>Returns:</p> Type Description <code>Image</code> <p>The newly created image entity.</p> <p>Raises:</p> Type Description <code>ProjectNotFound</code> <p>If the project does not exist.</p> Source code in <code>src/ulabel/application/upload_image_to_project.py</code> <pre><code>async def execute(self, project_id: UUID, data: bytes, content_type: str) -&gt; Image:\n    \"\"\"Upload an image to a project.\n\n    Args:\n        project_id: The project to add the image to.\n        data: The raw image bytes.\n        content_type: The MIME type of the image.\n\n    Returns:\n        The newly created image entity.\n\n    Raises:\n        ProjectNotFound: If the project does not exist.\n    \"\"\"\n    async with self._uow as uow:\n        project = await uow.project_repository.get_by_id(project_id)\n        if project is None:\n            raise ProjectNotFound(f\"Project '{project_id}' not found\")\n\n        image_id = uuid4()\n        storage_key = f\"{project_id}/{image_id}\"\n        await self.storage_service.upload_file(\n            key=storage_key,\n            data=data,\n            content_type=content_type,\n            size=len(data),\n        )\n        image = Image.create(id=image_id, project_id=project_id, storage_key=storage_key)\n        await uow.image_repository.save(image)\n        await uow.commit()\n        return image\n</code></pre>"},{"location":"reference/application/use-cases/#import-images-from-storage","title":"Import Images from Storage","text":""},{"location":"reference/application/use-cases/#ulabel.application.import_images_from_storage","title":"<code>import_images_from_storage</code>","text":"<p>Use case for bulk-importing images from object storage into a project.</p>"},{"location":"reference/application/use-cases/#ulabel.application.import_images_from_storage.ImportImagesFromStorageUseCase","title":"<code>ImportImagesFromStorageUseCase(uow, storage_service)</code>","text":"<p>Imports images from an object storage prefix into a project in chunks.</p> <p>Creates an import job that can be polled for progress while the actual import runs asynchronously in the background.</p> <p>Initialize the use case.</p> <p>Parameters:</p> Name Type Description Default <code>uow</code> <code>UnitOfWork</code> <p>Unit of Work for transactional repository access.</p> required <code>storage_service</code> <code>StorageService</code> <p>Service for listing objects in storage.</p> required Source code in <code>src/ulabel/application/import_images_from_storage.py</code> <pre><code>def __init__(self, uow: UnitOfWork, storage_service: StorageService):\n    \"\"\"Initialize the use case.\n\n    Args:\n        uow: Unit of Work for transactional repository access.\n        storage_service: Service for listing objects in storage.\n    \"\"\"\n    self._uow = uow\n    self._storage_service = storage_service\n</code></pre>"},{"location":"reference/application/use-cases/#ulabel.application.import_images_from_storage.ImportImagesFromStorageUseCase.execute","title":"<code>execute(project_id, prefix)</code>  <code>async</code>","text":"<p>Start a new import job for a project.</p> <p>Validates the project exists, creates and persists the import job, and returns it immediately. The caller is responsible for scheduling <code>run_import</code> as a background task.</p> <p>Parameters:</p> Name Type Description Default <code>project_id</code> <code>UUID</code> <p>The project to import images into.</p> required <code>prefix</code> <code>str</code> <p>The object storage prefix to list images from.</p> required <p>Returns:</p> Type Description <code>ImportJob</code> <p>The newly created import job in running status.</p> <p>Raises:</p> Type Description <code>ProjectNotFound</code> <p>If the project does not exist.</p> Source code in <code>src/ulabel/application/import_images_from_storage.py</code> <pre><code>async def execute(self, project_id: UUID, prefix: str) -&gt; ImportJob:\n    \"\"\"Start a new import job for a project.\n\n    Validates the project exists, creates and persists the import job,\n    and returns it immediately. The caller is responsible for scheduling\n    ``run_import`` as a background task.\n\n    Args:\n        project_id: The project to import images into.\n        prefix: The object storage prefix to list images from.\n\n    Returns:\n        The newly created import job in running status.\n\n    Raises:\n        ProjectNotFound: If the project does not exist.\n    \"\"\"\n    async with self._uow as uow:\n        project = await uow.project_repository.get_by_id(project_id)\n        if project is None:\n            raise ProjectNotFound(\"Project not found\")\n\n        job = ImportJob.create(id=uuid4(), project_id=project_id, prefix=prefix)\n        await uow.import_job_repository.save(job)\n        await uow.commit()\n        return job\n</code></pre>"},{"location":"reference/application/use-cases/#ulabel.application.import_images_from_storage.ImportImagesFromStorageUseCase.run_import","title":"<code>run_import(job_id)</code>  <code>async</code>","text":"<p>Execute the import, saving images in chunks and updating job progress.</p> <p>Loads the job from the repository, iterates over storage objects, and persists images in batches. Updates the job status in the repository after each chunk and on completion or failure.</p> <p>Parameters:</p> Name Type Description Default <code>job_id</code> <code>UUID</code> <p>The ID of the import job to execute.</p> required Source code in <code>src/ulabel/application/import_images_from_storage.py</code> <pre><code>async def run_import(self, job_id: UUID) -&gt; None:\n    \"\"\"Execute the import, saving images in chunks and updating job progress.\n\n    Loads the job from the repository, iterates over storage objects,\n    and persists images in batches. Updates the job status in the\n    repository after each chunk and on completion or failure.\n\n    Args:\n        job_id: The ID of the import job to execute.\n    \"\"\"\n    async with self._uow as uow:\n        job = await uow.import_job_repository.get_by_id(job_id)\n        if job is None:\n            return\n\n        try:\n            chunk: list[Image] = []\n            async for storage_key in self._storage_service.list_objects(job.prefix):\n                chunk.append(\n                    Image.create(\n                        id=uuid4(),\n                        project_id=job.project_id,\n                        storage_key=storage_key,\n                    )\n                )\n                if len(chunk) &gt;= _CHUNK_SIZE:\n                    await uow.image_repository.save_bulk(chunk)\n                    job.mark_progress(len(chunk))\n                    await uow.import_job_repository.save(job)\n                    await uow.commit()\n                    chunk = []\n            if chunk:\n                await uow.image_repository.save_bulk(chunk)\n                job.mark_progress(len(chunk))\n            job.mark_done()\n        except Exception as e:\n            job.mark_failed(str(e))\n\n        await uow.import_job_repository.save(job)\n        await uow.commit()\n</code></pre>"},{"location":"reference/application/use-cases/#expire-images-task","title":"Expire Images Task","text":""},{"location":"reference/application/use-cases/#ulabel.application.expire_images_task","title":"<code>expire_images_task</code>","text":"<p>Background task that expires stale in-progress image assignments.</p>"},{"location":"reference/application/use-cases/#ulabel.application.expire_images_task.ExpireImagesTask","title":"<code>ExpireImagesTask(uow, timeout, interval, now=lambda: datetime.now(timezone.utc))</code>","text":"<p>Periodically checks for and expires in-progress images that have exceeded their timeout.</p> <p>Runs as a continuous background loop, reverting abandoned assignments so the images become available for reassignment.</p> <p>Initialize the task.</p> <p>Parameters:</p> Name Type Description Default <code>uow</code> <code>UnitOfWork</code> <p>Unit of Work for transactional repository access.</p> required <code>timeout</code> <code>timedelta</code> <p>Duration after which an in-progress image is considered expired.</p> required <code>interval</code> <code>timedelta</code> <p>Time between expiration check cycles.</p> required <code>now</code> <code>Callable[[], datetime]</code> <p>Callable returning the current UTC datetime.</p> <code>lambda: now(utc)</code> Source code in <code>src/ulabel/application/expire_images_task.py</code> <pre><code>def __init__(\n    self,\n    uow: UnitOfWork,\n    timeout: timedelta,\n    interval: timedelta,\n    now: Callable[[], datetime] = lambda: datetime.now(timezone.utc),\n):\n    \"\"\"Initialize the task.\n\n    Args:\n        uow: Unit of Work for transactional repository access.\n        timeout: Duration after which an in-progress image is considered expired.\n        interval: Time between expiration check cycles.\n        now: Callable returning the current UTC datetime.\n    \"\"\"\n    self._uow = uow\n    self._timeout = timeout\n    self._interval = interval\n    self._now = now\n</code></pre>"},{"location":"reference/application/use-cases/#ulabel.application.expire_images_task.ExpireImagesTask.run","title":"<code>run()</code>  <code>async</code>","text":"<p>Run the expiration loop indefinitely.</p> Source code in <code>src/ulabel/application/expire_images_task.py</code> <pre><code>async def run(self) -&gt; None:\n    \"\"\"Run the expiration loop indefinitely.\"\"\"\n    while True:\n        await self.tick()\n        await asyncio.sleep(self._interval.total_seconds())\n</code></pre>"},{"location":"reference/application/use-cases/#ulabel.application.expire_images_task.ExpireImagesTask.tick","title":"<code>tick()</code>  <code>async</code>","text":"<p>Execute a single expiration cycle, expiring all overdue images.</p> Source code in <code>src/ulabel/application/expire_images_task.py</code> <pre><code>async def tick(self) -&gt; None:\n    \"\"\"Execute a single expiration cycle, expiring all overdue images.\"\"\"\n    cutoff = self._now() - self._timeout\n    async with self._uow as uow:\n        expired = await uow.image_repository.expire_in_progress(cutoff)\n        if expired:\n            logger.info(\"Expired %d stale assignments\", len(expired))\n        await uow.commit()\n</code></pre>"},{"location":"reference/application/use-cases/#labeling-workflow","title":"Labeling Workflow","text":""},{"location":"reference/application/use-cases/#create-assignment","title":"Create Assignment","text":""},{"location":"reference/application/use-cases/#ulabel.application.create_assignment","title":"<code>create_assignment</code>","text":"<p>Use case for creating an image labeling assignment for a labeler.</p>"},{"location":"reference/application/use-cases/#ulabel.application.create_assignment.LabelerNotInProject","title":"<code>LabelerNotInProject</code>","text":"<p>               Bases: <code>DomainError</code></p> <p>Raised when a labeler is not a member of the target project.</p>"},{"location":"reference/application/use-cases/#ulabel.application.create_assignment.NoImageAvailable","title":"<code>NoImageAvailable</code>","text":"<p>               Bases: <code>DomainError</code></p> <p>Raised when no pending images are available for assignment.</p>"},{"location":"reference/application/use-cases/#ulabel.application.create_assignment.CreateAssignmentUseCase","title":"<code>CreateAssignmentUseCase(uow, now=lambda: datetime.now(timezone.utc))</code>","text":"<p>Assigns the next pending image in a project to a labeler.</p> <p>Validates project membership and image availability before creating the assignment.</p> <p>Initialize the use case.</p> <p>Parameters:</p> Name Type Description Default <code>uow</code> <code>UnitOfWork</code> <p>Unit of Work for transactional repository access.</p> required <code>now</code> <code>Callable[[], datetime]</code> <p>Callable returning the current UTC datetime.</p> <code>lambda: now(utc)</code> Source code in <code>src/ulabel/application/create_assignment.py</code> <pre><code>def __init__(\n    self,\n    uow: UnitOfWork,\n    now: Callable[[], datetime] = lambda: datetime.now(timezone.utc),\n):\n    \"\"\"Initialize the use case.\n\n    Args:\n        uow: Unit of Work for transactional repository access.\n        now: Callable returning the current UTC datetime.\n    \"\"\"\n    self._uow = uow\n    self.now = now\n</code></pre>"},{"location":"reference/application/use-cases/#ulabel.application.create_assignment.CreateAssignmentUseCase.execute","title":"<code>execute(project_id, labeler_id)</code>  <code>async</code>","text":"<p>Create a new assignment for the labeler.</p> <p>Parameters:</p> Name Type Description Default <code>project_id</code> <code>UUID</code> <p>The project to assign an image from.</p> required <code>labeler_id</code> <code>UUID</code> <p>The labeler to assign the image to.</p> required <p>Returns:</p> Type Description <code>Image</code> <p>The image entity with the new assignment set.</p> <p>Raises:</p> Type Description <code>ProjectNotFound</code> <p>If the project does not exist.</p> <code>LabelerNotInProject</code> <p>If the labeler is not a member of the project.</p> <code>NoImageAvailable</code> <p>If there are no pending images to assign.</p> Source code in <code>src/ulabel/application/create_assignment.py</code> <pre><code>async def execute(self, project_id: UUID, labeler_id: UUID) -&gt; Image:\n    \"\"\"Create a new assignment for the labeler.\n\n    Args:\n        project_id: The project to assign an image from.\n        labeler_id: The labeler to assign the image to.\n\n    Returns:\n        The image entity with the new assignment set.\n\n    Raises:\n        ProjectNotFound: If the project does not exist.\n        LabelerNotInProject: If the labeler is not a member of the project.\n        NoImageAvailable: If there are no pending images to assign.\n    \"\"\"\n    async with self._uow as uow:\n        project = await uow.project_repository.get_by_id(project_id)\n        if project is None:\n            raise ProjectNotFound(\"Project not found\")\n        if labeler_id not in project.labeler_ids:\n            raise LabelerNotInProject(\"Labeler is not in this project\")\n\n        image = await uow.image_repository.assign_next_pending(\n            project_id=project_id,\n            labeler_id=labeler_id,\n            assigned_at=self.now(),\n        )\n        if image is None:\n            raise NoImageAvailable(\"No pending images available\")\n\n        await uow.commit()\n        return image\n</code></pre>"},{"location":"reference/application/use-cases/#submit-label","title":"Submit Label","text":""},{"location":"reference/application/use-cases/#ulabel.application.submit_label","title":"<code>submit_label</code>","text":"<p>Use case for submitting a label for an assigned image.</p>"},{"location":"reference/application/use-cases/#ulabel.application.submit_label.ImageNotFound","title":"<code>ImageNotFound</code>","text":"<p>               Bases: <code>DomainError</code></p> <p>Raised when the image does not exist or does not belong to the project.</p>"},{"location":"reference/application/use-cases/#ulabel.application.submit_label.ImageNotInProgress","title":"<code>ImageNotInProgress</code>","text":"<p>               Bases: <code>DomainError</code></p> <p>Raised when the image is not in the in-progress state.</p>"},{"location":"reference/application/use-cases/#ulabel.application.submit_label.AssignmentMismatch","title":"<code>AssignmentMismatch</code>","text":"<p>               Bases: <code>DomainError</code></p> <p>Raised when the provided assignment ID does not match the image's assignment.</p>"},{"location":"reference/application/use-cases/#ulabel.application.submit_label.LabelerMismatch","title":"<code>LabelerMismatch</code>","text":"<p>               Bases: <code>DomainError</code></p> <p>Raised when the submitting labeler does not match the assigned labeler.</p>"},{"location":"reference/application/use-cases/#ulabel.application.submit_label.InvalidLabel","title":"<code>InvalidLabel</code>","text":"<p>               Bases: <code>DomainError</code></p> <p>Raised when the submitted label value is not in the project's allowed labels.</p>"},{"location":"reference/application/use-cases/#ulabel.application.submit_label.SubmitLabelUseCase","title":"<code>SubmitLabelUseCase(uow)</code>","text":"<p>Records a label for an assigned image and returns updated labeler stats.</p> <p>Validates the full assignment chain (project, image, assignment, labeler, label value) before persisting the label and marking the image as complete.</p> <p>Initialize the use case.</p> <p>Parameters:</p> Name Type Description Default <code>uow</code> <code>UnitOfWork</code> <p>Unit of Work for transactional repository access.</p> required Source code in <code>src/ulabel/application/submit_label.py</code> <pre><code>def __init__(self, uow: UnitOfWork):\n    \"\"\"Initialize the use case.\n\n    Args:\n        uow: Unit of Work for transactional repository access.\n    \"\"\"\n    self._uow = uow\n</code></pre>"},{"location":"reference/application/use-cases/#ulabel.application.submit_label.SubmitLabelUseCase.execute","title":"<code>execute(project_id, image_id, labeler_id, assignment_id, label)</code>  <code>async</code>","text":"<p>Submit a label for an assigned image.</p> <p>Parameters:</p> Name Type Description Default <code>project_id</code> <code>UUID</code> <p>The project the image belongs to.</p> required <code>image_id</code> <code>UUID</code> <p>The image being labeled.</p> required <code>labeler_id</code> <code>UUID</code> <p>The labeler submitting the label.</p> required <code>assignment_id</code> <code>UUID</code> <p>The assignment ID to verify ownership.</p> required <code>label</code> <code>str</code> <p>The label value to assign.</p> required <p>Returns:</p> Type Description <code>tuple[LabelRecord, LabelerSubmitStats]</code> <p>A tuple of the created label record and the labeler's updated stats.</p> <p>Raises:</p> Type Description <code>ProjectNotFound</code> <p>If the project does not exist.</p> <code>ImageNotFound</code> <p>If the image does not exist or is in another project.</p> <code>ImageNotInProgress</code> <p>If the image is not currently assigned.</p> <code>AssignmentMismatch</code> <p>If the assignment ID does not match.</p> <code>LabelerMismatch</code> <p>If the labeler does not match the assignment.</p> <code>InvalidLabel</code> <p>If the label is not in the project's allowed set.</p> Source code in <code>src/ulabel/application/submit_label.py</code> <pre><code>async def execute(\n    self,\n    project_id: UUID,\n    image_id: UUID,\n    labeler_id: UUID,\n    assignment_id: UUID,\n    label: str,\n) -&gt; tuple[LabelRecord, LabelerSubmitStats]:\n    \"\"\"Submit a label for an assigned image.\n\n    Args:\n        project_id: The project the image belongs to.\n        image_id: The image being labeled.\n        labeler_id: The labeler submitting the label.\n        assignment_id: The assignment ID to verify ownership.\n        label: The label value to assign.\n\n    Returns:\n        A tuple of the created label record and the labeler's updated stats.\n\n    Raises:\n        ProjectNotFound: If the project does not exist.\n        ImageNotFound: If the image does not exist or is in another project.\n        ImageNotInProgress: If the image is not currently assigned.\n        AssignmentMismatch: If the assignment ID does not match.\n        LabelerMismatch: If the labeler does not match the assignment.\n        InvalidLabel: If the label is not in the project's allowed set.\n    \"\"\"\n    async with self._uow as uow:\n        project = await uow.project_repository.get_by_id(project_id)\n        if project is None:\n            raise ProjectNotFound(\"Project not found\")\n\n        image = await uow.image_repository.get_by_id(image_id)\n        if image is None or image.project_id != project_id:\n            raise ImageNotFound(\"Image not found\")\n\n        if image.status != ImageStatus.IN_PROGRESS:\n            raise ImageNotInProgress(\"Image is not in progress\")\n\n        if image.assignment_id != assignment_id:\n            raise AssignmentMismatch(\"Assignment ID mismatch\")\n\n        if image.labeler_id != labeler_id:\n            raise LabelerMismatch(\"Labeler mismatch\")\n\n        if label not in project.labels:\n            raise InvalidLabel(\"Invalid label\")\n\n        label_record = LabelRecord.create(\n            id=uuid4(),\n            project_id=project_id,\n            image_id=image_id,\n            labeler_id=labeler_id,\n            label=label,\n        )\n        await uow.label_repository.save(label_record)\n\n        image.complete()\n        await uow.image_repository.save(image)\n\n        stats = await uow.stats_repository.get_labeler_ranking(project_id, labeler_id)\n\n        await uow.commit()\n\n        return label_record, stats\n</code></pre>"},{"location":"reference/application/use-cases/#reporting","title":"Reporting","text":""},{"location":"reference/application/use-cases/#get-project-stats","title":"Get Project Stats","text":""},{"location":"reference/application/use-cases/#ulabel.application.get_project_stats","title":"<code>get_project_stats</code>","text":"<p>Use case for retrieving statistics and analytics for a labeling project.</p>"},{"location":"reference/application/use-cases/#ulabel.application.get_project_stats.LabelerClassCount","title":"<code>LabelerClassCount(labeler_id, username, counts)</code>  <code>dataclass</code>","text":"<p>Label counts per class for a single labeler.</p>"},{"location":"reference/application/use-cases/#ulabel.application.get_project_stats.DailyCount","title":"<code>DailyCount(date, counts)</code>  <code>dataclass</code>","text":"<p>Label counts per class for a single day.</p>"},{"location":"reference/application/use-cases/#ulabel.application.get_project_stats.LabelerDailyActivity","title":"<code>LabelerDailyActivity(labeler_id, username, daily)</code>  <code>dataclass</code>","text":"<p>Daily labeling activity breakdown for a single labeler.</p>"},{"location":"reference/application/use-cases/#ulabel.application.get_project_stats.DailyLabelerTotal","title":"<code>DailyLabelerTotal(labeler_id, username, count)</code>  <code>dataclass</code>","text":"<p>Total label count for a labeler on a given day.</p>"},{"location":"reference/application/use-cases/#ulabel.application.get_project_stats.DailyTotal","title":"<code>DailyTotal(date, labelers)</code>  <code>dataclass</code>","text":"<p>Aggregated labeler totals for a single day.</p>"},{"location":"reference/application/use-cases/#ulabel.application.get_project_stats.ProjectStats","title":"<code>ProjectStats(total_images, labeled_images, class_distribution, labeler_class_counts, labeler_daily_activity, daily_totals)</code>  <code>dataclass</code>","text":"<p>Aggregate statistics for a labeling project.</p>"},{"location":"reference/application/use-cases/#ulabel.application.get_project_stats.GetProjectStatsUseCase","title":"<code>GetProjectStatsUseCase(uow)</code>","text":"<p>Computes and returns comprehensive statistics for a project.</p> <p>Aggregates image counts, class distributions, per-labeler breakdowns, and daily activity data.</p> <p>Initialize the use case.</p> <p>Parameters:</p> Name Type Description Default <code>uow</code> <code>UnitOfWork</code> <p>Unit of Work for transactional repository access.</p> required Source code in <code>src/ulabel/application/get_project_stats.py</code> <pre><code>def __init__(self, uow: UnitOfWork):\n    \"\"\"Initialize the use case.\n\n    Args:\n        uow: Unit of Work for transactional repository access.\n    \"\"\"\n    self._uow = uow\n</code></pre>"},{"location":"reference/application/use-cases/#ulabel.application.get_project_stats.GetProjectStatsUseCase.execute","title":"<code>execute(project_id)</code>  <code>async</code>","text":"<p>Get statistics for a project.</p> <p>Parameters:</p> Name Type Description Default <code>project_id</code> <code>UUID</code> <p>The ID of the project to get stats for.</p> required <p>Returns:</p> Type Description <code>ProjectStats</code> <p>Aggregated project statistics.</p> <p>Raises:</p> Type Description <code>ProjectNotFound</code> <p>If the project does not exist.</p> Source code in <code>src/ulabel/application/get_project_stats.py</code> <pre><code>async def execute(self, project_id: UUID) -&gt; ProjectStats:\n    \"\"\"Get statistics for a project.\n\n    Args:\n        project_id: The ID of the project to get stats for.\n\n    Returns:\n        Aggregated project statistics.\n\n    Raises:\n        ProjectNotFound: If the project does not exist.\n    \"\"\"\n    async with self._uow as uow:\n        project = await uow.project_repository.get_by_id(project_id)\n        if project is None:\n            raise ProjectNotFound(\"Project not found\")\n\n        image_counts: ImageCounts = await uow.stats_repository.get_image_counts(project_id)\n        labeler_class_rows: list[\n            LabelerClassRow\n        ] = await uow.stats_repository.get_labeler_class_counts(project_id)\n        daily_rows: list[DailyLabelRow] = await uow.stats_repository.get_daily_label_counts(\n            project_id\n        )\n\n        # class_distribution: aggregate from labeler_class_rows\n        class_distribution: dict[str, int] = defaultdict(int)\n        for lc_row in labeler_class_rows:\n            class_distribution[lc_row.label] += lc_row.count\n\n        # labeler_class_counts: group by labeler\n        labeler_map: dict[UUID, LabelerClassCount] = {}\n        for lc_row in labeler_class_rows:\n            if lc_row.labeler_id not in labeler_map:\n                labeler_map[lc_row.labeler_id] = LabelerClassCount(\n                    labeler_id=lc_row.labeler_id, username=lc_row.username, counts={}\n                )\n            labeler_map[lc_row.labeler_id].counts[lc_row.label] = lc_row.count\n\n        # labeler_daily_activity: group by labeler -&gt; day -&gt; label\n        daily_by_labeler: dict[UUID, dict[date, dict[str, int]]] = defaultdict(\n            lambda: defaultdict(lambda: defaultdict(int))\n        )\n        labeler_usernames: dict[UUID, str] = {}\n        for daily_row in daily_rows:\n            daily_by_labeler[daily_row.labeler_id][daily_row.day][daily_row.label] += (\n                daily_row.count\n            )\n            labeler_usernames[daily_row.labeler_id] = daily_row.username\n\n        labeler_daily_activity = [\n            LabelerDailyActivity(\n                labeler_id=lid,\n                username=labeler_usernames[lid],\n                daily=[\n                    DailyCount(date=day, counts=dict(labels))\n                    for day, labels in sorted(days.items())\n                ],\n            )\n            for lid, days in daily_by_labeler.items()\n        ]\n\n        # daily_totals: group by day -&gt; labeler\n        day_labeler: dict[date, dict[UUID, DailyLabelerTotal]] = defaultdict(dict)\n        for daily_row in daily_rows:\n            key = daily_row.labeler_id\n            if key not in day_labeler[daily_row.day]:\n                day_labeler[daily_row.day][key] = DailyLabelerTotal(\n                    labeler_id=daily_row.labeler_id, username=daily_row.username, count=0\n                )\n            day_labeler[daily_row.day][key].count += daily_row.count\n\n        daily_totals = [\n            DailyTotal(date=day, labelers=list(labelers.values()))\n            for day, labelers in sorted(day_labeler.items())\n        ]\n\n        return ProjectStats(\n            total_images=image_counts.total,\n            labeled_images=image_counts.labeled,\n            class_distribution=dict(class_distribution),\n            labeler_class_counts=list(labeler_map.values()),\n            labeler_daily_activity=labeler_daily_activity,\n            daily_totals=daily_totals,\n        )\n</code></pre>"},{"location":"reference/application/use-cases/#export-labels","title":"Export Labels","text":""},{"location":"reference/application/use-cases/#ulabel.application.export_labels","title":"<code>export_labels</code>","text":"<p>Use case for exporting labeled data from a project as CSV or JSON.</p>"},{"location":"reference/application/use-cases/#ulabel.application.export_labels.ExportFormat","title":"<code>ExportFormat</code>","text":"<p>               Bases: <code>str</code>, <code>Enum</code></p> <p>Supported export file formats.</p>"},{"location":"reference/application/use-cases/#ulabel.application.export_labels.NoLabelsFound","title":"<code>NoLabelsFound</code>","text":"<p>               Bases: <code>DomainError</code></p> <p>Raised when a project has no labels to export.</p>"},{"location":"reference/application/use-cases/#ulabel.application.export_labels.ExportResult","title":"<code>ExportResult(url, cache_hit)</code>  <code>dataclass</code>","text":"<p>Result of an export operation.</p>"},{"location":"reference/application/use-cases/#ulabel.application.export_labels.ExportLabelsUseCase","title":"<code>ExportLabelsUseCase(uow, storage_service, presigned_url_expiry)</code>","text":"<p>Generates and uploads a label export file, returning a presigned download URL.</p> <p>Caches exports in object storage and reuses them when the label count has not changed since the last export.</p> <p>Initialize the use case.</p> <p>Parameters:</p> Name Type Description Default <code>uow</code> <code>UnitOfWork</code> <p>Unit of Work for transactional repository access.</p> required <code>storage_service</code> <code>StorageService</code> <p>Service for file upload and presigned URL generation.</p> required <code>presigned_url_expiry</code> <code>timedelta</code> <p>How long presigned download URLs remain valid.</p> required Source code in <code>src/ulabel/application/export_labels.py</code> <pre><code>def __init__(\n    self,\n    uow: UnitOfWork,\n    storage_service: StorageService,\n    presigned_url_expiry: timedelta,\n):\n    \"\"\"Initialize the use case.\n\n    Args:\n        uow: Unit of Work for transactional repository access.\n        storage_service: Service for file upload and presigned URL generation.\n        presigned_url_expiry: How long presigned download URLs remain valid.\n    \"\"\"\n    self._uow = uow\n    self._storage_service = storage_service\n    self._presigned_url_expiry = presigned_url_expiry\n</code></pre>"},{"location":"reference/application/use-cases/#ulabel.application.export_labels.ExportLabelsUseCase.execute","title":"<code>execute(project_id, fmt)</code>  <code>async</code>","text":"<p>Export all labels for a project in the specified format.</p> <p>Parameters:</p> Name Type Description Default <code>project_id</code> <code>UUID</code> <p>The project whose labels to export.</p> required <code>fmt</code> <code>ExportFormat</code> <p>The desired output format.</p> required <p>Returns:</p> Type Description <code>ExportResult</code> <p>An ExportResult with the presigned URL and cache-hit flag.</p> <p>Raises:</p> Type Description <code>ProjectNotFound</code> <p>If the project does not exist.</p> <code>NoLabelsFound</code> <p>If the project has no labels to export.</p> Source code in <code>src/ulabel/application/export_labels.py</code> <pre><code>async def execute(self, project_id: UUID, fmt: ExportFormat) -&gt; ExportResult:\n    \"\"\"Export all labels for a project in the specified format.\n\n    Args:\n        project_id: The project whose labels to export.\n        fmt: The desired output format.\n\n    Returns:\n        An ExportResult with the presigned URL and cache-hit flag.\n\n    Raises:\n        ProjectNotFound: If the project does not exist.\n        NoLabelsFound: If the project has no labels to export.\n    \"\"\"\n    async with self._uow as uow:\n        project = await uow.project_repository.get_by_id(project_id)\n        if project is None:\n            raise ProjectNotFound(\"Project not found\")\n\n        label_count = await uow.label_repository.count_by_project(project_id)\n        if label_count == 0:\n            raise NoLabelsFound(\"No labels to export\")\n\n        filename = f\"{project_id}.{fmt.value}\"\n        storage_key = f\"exports/{project_id}/{filename}\"\n\n        metadata = await self._storage_service.head_object(storage_key)\n        if metadata is not None and metadata.get(\"label_count\") == str(label_count):\n            url = await self._storage_service.get_presigned_url(\n                storage_key,\n                expires_in=self._presigned_url_expiry,\n            )\n            return ExportResult(url=url, cache_hit=True)\n\n        rows = uow.label_repository.get_export_data(project_id)\n\n        if fmt == ExportFormat.CSV:\n            chunks = self._generate_csv(rows)\n            content_type = \"text/csv\"\n        else:\n            chunks = self._generate_json(rows)\n            content_type = \"application/json\"\n\n        await self._storage_service.upload_file_streaming(\n            key=storage_key,\n            chunks=chunks,\n            content_type=content_type,\n            metadata={\"label_count\": str(label_count)},\n        )\n\n        url = await self._storage_service.get_presigned_url(\n            storage_key,\n            expires_in=self._presigned_url_expiry,\n        )\n        return ExportResult(url=url, cache_hit=False)\n</code></pre>"},{"location":"reference/domain/entities/","title":"Domain Entities","text":"<p>Core domain models representing the business concepts of the uLabel platform.</p>"},{"location":"reference/domain/entities/#project","title":"Project","text":""},{"location":"reference/domain/entities/#ulabel.domain.projects","title":"<code>projects</code>","text":"<p>Domain model for labeling projects.</p>"},{"location":"reference/domain/entities/#ulabel.domain.projects.Project","title":"<code>Project(id, owner, name, description, labels, created_at=None, labeler_ids=set())</code>  <code>dataclass</code>","text":"<p>A labeling project that groups images, labels, and labelers.</p> <p>Attributes:</p> Name Type Description <code>id</code> <code>UUID</code> <p>Unique identifier for the project.</p> <code>owner</code> <code>User</code> <p>The admin user who owns the project.</p> <code>name</code> <code>str</code> <p>Human-readable project name.</p> <code>description</code> <code>str</code> <p>Free-text description of the project.</p> <code>labels</code> <code>set[str]</code> <p>Set of allowed label values for this project.</p> <code>created_at</code> <code>datetime | None</code> <p>Timestamp when the project was created.</p> <code>labeler_ids</code> <code>set[UUID]</code> <p>Set of user identifiers assigned as labelers.</p>"},{"location":"reference/domain/entities/#ulabel.domain.projects.Project.create","title":"<code>create(id, owner, name, description, labels, created_at=None)</code>  <code>classmethod</code>","text":"<p>Create a new project, defaulting created_at to the current UTC time.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <code>UUID</code> <p>Unique identifier for the project.</p> required <code>owner</code> <code>User</code> <p>The admin user who owns the project.</p> required <code>name</code> <code>str</code> <p>Human-readable project name.</p> required <code>description</code> <code>str</code> <p>Free-text description.</p> required <code>labels</code> <code>set[str]</code> <p>Set of allowed label values.</p> required <code>created_at</code> <code>datetime | None</code> <p>Optional creation timestamp; defaults to now (UTC).</p> <code>None</code> <p>Returns:</p> Type Description <code>Project</code> <p>A new Project instance.</p> Source code in <code>src/ulabel/domain/projects.py</code> <pre><code>@classmethod\ndef create(\n    cls,\n    id: UUID,\n    owner: User,\n    name: str,\n    description: str,\n    labels: set[str],\n    created_at: datetime | None = None,\n) -&gt; \"Project\":\n    \"\"\"Create a new project, defaulting created_at to the current UTC time.\n\n    Args:\n        id: Unique identifier for the project.\n        owner: The admin user who owns the project.\n        name: Human-readable project name.\n        description: Free-text description.\n        labels: Set of allowed label values.\n        created_at: Optional creation timestamp; defaults to now (UTC).\n\n    Returns:\n        A new Project instance.\n    \"\"\"\n    if created_at is None:\n        created_at = datetime.now(timezone.utc)\n    return cls(\n        id=id,\n        owner=owner,\n        name=name,\n        description=description,\n        labels=labels,\n        created_at=created_at,\n    )\n</code></pre>"},{"location":"reference/domain/entities/#ulabel.domain.projects.Project.add_labeler","title":"<code>add_labeler(labeler_id)</code>","text":"<p>Add a labeler to the project.</p> <p>Parameters:</p> Name Type Description Default <code>labeler_id</code> <code>UUID</code> <p>Identifier of the labeler to add.</p> required Source code in <code>src/ulabel/domain/projects.py</code> <pre><code>def add_labeler(self, labeler_id: UUID) -&gt; None:\n    \"\"\"Add a labeler to the project.\n\n    Args:\n        labeler_id: Identifier of the labeler to add.\n    \"\"\"\n    self.labeler_ids.add(labeler_id)\n</code></pre>"},{"location":"reference/domain/entities/#ulabel.domain.projects.Project.update","title":"<code>update(*, name=None, description=None)</code>","text":"<p>Update mutable project fields.</p> <p>Only the provided (non-None) fields are updated.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str | None</code> <p>New project name, if changing.</p> <code>None</code> <code>description</code> <code>str | None</code> <p>New project description, if changing.</p> <code>None</code> Source code in <code>src/ulabel/domain/projects.py</code> <pre><code>def update(self, *, name: str | None = None, description: str | None = None) -&gt; None:\n    \"\"\"Update mutable project fields.\n\n    Only the provided (non-None) fields are updated.\n\n    Args:\n        name: New project name, if changing.\n        description: New project description, if changing.\n    \"\"\"\n    if name is not None:\n        self.name = name\n    if description is not None:\n        self.description = description\n</code></pre>"},{"location":"reference/domain/entities/#ulabel.domain.projects.Project.set_labelers","title":"<code>set_labelers(labeler_ids)</code>","text":"<p>Replace the full set of assigned labelers.</p> <p>Parameters:</p> Name Type Description Default <code>labeler_ids</code> <code>set[UUID]</code> <p>The new set of labeler identifiers.</p> required Source code in <code>src/ulabel/domain/projects.py</code> <pre><code>def set_labelers(self, labeler_ids: set[UUID]) -&gt; None:\n    \"\"\"Replace the full set of assigned labelers.\n\n    Args:\n        labeler_ids: The new set of labeler identifiers.\n    \"\"\"\n    self.labeler_ids = labeler_ids\n</code></pre>"},{"location":"reference/domain/entities/#image","title":"Image","text":""},{"location":"reference/domain/entities/#ulabel.domain.images","title":"<code>images</code>","text":"<p>Domain model for images within labeling projects.</p>"},{"location":"reference/domain/entities/#ulabel.domain.images.ImageStatus","title":"<code>ImageStatus</code>","text":"<p>               Bases: <code>StrEnum</code></p> <p>Possible lifecycle states of an image in the labeling workflow.</p>"},{"location":"reference/domain/entities/#ulabel.domain.images.Image","title":"<code>Image(id, project_id, storage_key, status=ImageStatus.PENDING, labeler_id=None, assigned_at=None, assignment_id=None)</code>  <code>dataclass</code>","text":"<p>An image that belongs to a project and moves through the labeling workflow.</p> <p>Attributes:</p> Name Type Description <code>id</code> <code>UUID</code> <p>Unique identifier for the image.</p> <code>project_id</code> <code>UUID</code> <p>Identifier of the project this image belongs to.</p> <code>storage_key</code> <code>str</code> <p>Object-storage key used to locate the image file.</p> <code>status</code> <code>ImageStatus</code> <p>Current lifecycle status of the image.</p> <code>labeler_id</code> <code>UUID | None</code> <p>Identifier of the labeler currently assigned, if any.</p> <code>assigned_at</code> <code>datetime | None</code> <p>Timestamp when the image was assigned to a labeler.</p> <code>assignment_id</code> <code>UUID | None</code> <p>Unique identifier for the current assignment session.</p>"},{"location":"reference/domain/entities/#ulabel.domain.images.Image.create","title":"<code>create(id, project_id, storage_key)</code>  <code>classmethod</code>","text":"<p>Create a new image in PENDING status.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <code>UUID</code> <p>Unique identifier for the image.</p> required <code>project_id</code> <code>UUID</code> <p>Identifier of the owning project.</p> required <code>storage_key</code> <code>str</code> <p>Object-storage key for the image file.</p> required <p>Returns:</p> Type Description <code>Image</code> <p>A new Image instance with default PENDING status.</p> Source code in <code>src/ulabel/domain/images.py</code> <pre><code>@classmethod\ndef create(cls, id: UUID, project_id: UUID, storage_key: str) -&gt; \"Image\":\n    \"\"\"Create a new image in PENDING status.\n\n    Args:\n        id: Unique identifier for the image.\n        project_id: Identifier of the owning project.\n        storage_key: Object-storage key for the image file.\n\n    Returns:\n        A new Image instance with default PENDING status.\n    \"\"\"\n    return cls(id=id, project_id=project_id, storage_key=storage_key)\n</code></pre>"},{"location":"reference/domain/entities/#ulabel.domain.images.Image.assign","title":"<code>assign(labeler_id, assigned_at)</code>","text":"<p>Assign the image to a labeler and transition to IN_PROGRESS.</p> <p>Parameters:</p> Name Type Description Default <code>labeler_id</code> <code>UUID</code> <p>Identifier of the labeler receiving the assignment.</p> required <code>assigned_at</code> <code>datetime</code> <p>Timestamp of the assignment.</p> required Source code in <code>src/ulabel/domain/images.py</code> <pre><code>def assign(self, labeler_id: UUID, assigned_at: datetime) -&gt; None:\n    \"\"\"Assign the image to a labeler and transition to IN_PROGRESS.\n\n    Args:\n        labeler_id: Identifier of the labeler receiving the assignment.\n        assigned_at: Timestamp of the assignment.\n    \"\"\"\n    self.labeler_id = labeler_id\n    self.assigned_at = assigned_at\n    self.assignment_id = uuid4()\n    self.status = ImageStatus.IN_PROGRESS\n</code></pre>"},{"location":"reference/domain/entities/#ulabel.domain.images.Image.complete","title":"<code>complete()</code>","text":"<p>Mark the image as fully labeled by transitioning to DONE.</p> Source code in <code>src/ulabel/domain/images.py</code> <pre><code>def complete(self) -&gt; None:\n    \"\"\"Mark the image as fully labeled by transitioning to DONE.\"\"\"\n    self.status = ImageStatus.DONE\n</code></pre>"},{"location":"reference/domain/entities/#ulabel.domain.images.Image.expire","title":"<code>expire()</code>","text":"<p>Reset the image back to PENDING, clearing its assignment data.</p> Source code in <code>src/ulabel/domain/images.py</code> <pre><code>def expire(self) -&gt; None:\n    \"\"\"Reset the image back to PENDING, clearing its assignment data.\"\"\"\n    self.status = ImageStatus.PENDING\n    self.labeler_id = None\n    self.assigned_at = None\n    self.assignment_id = None\n</code></pre>"},{"location":"reference/domain/entities/#label","title":"Label","text":""},{"location":"reference/domain/entities/#ulabel.domain.labels","title":"<code>labels</code>","text":"<p>Domain models for labels, export data, and labeler statistics.</p>"},{"location":"reference/domain/entities/#ulabel.domain.labels.LabelRecord","title":"<code>LabelRecord(id, project_id, image_id, labeler_id, label)</code>  <code>dataclass</code>","text":"<p>A label applied by a labeler to an image within a project.</p> <p>Attributes:</p> Name Type Description <code>id</code> <code>UUID</code> <p>Unique identifier for this label record.</p> <code>project_id</code> <code>UUID</code> <p>Identifier of the project the label belongs to.</p> <code>image_id</code> <code>UUID</code> <p>Identifier of the labeled image.</p> <code>labeler_id</code> <code>UUID</code> <p>Identifier of the labeler who created the label.</p> <code>label</code> <code>str</code> <p>The label value assigned to the image.</p>"},{"location":"reference/domain/entities/#ulabel.domain.labels.LabelRecord.create","title":"<code>create(id, project_id, image_id, labeler_id, label)</code>  <code>classmethod</code>","text":"<p>Create a new label record.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <code>UUID</code> <p>Unique identifier for the record.</p> required <code>project_id</code> <code>UUID</code> <p>Identifier of the owning project.</p> required <code>image_id</code> <code>UUID</code> <p>Identifier of the labeled image.</p> required <code>labeler_id</code> <code>UUID</code> <p>Identifier of the labeler.</p> required <code>label</code> <code>str</code> <p>The label value.</p> required <p>Returns:</p> Type Description <code>LabelRecord</code> <p>A new LabelRecord instance.</p> Source code in <code>src/ulabel/domain/labels.py</code> <pre><code>@classmethod\ndef create(\n    cls, id: UUID, project_id: UUID, image_id: UUID, labeler_id: UUID, label: str\n) -&gt; \"LabelRecord\":\n    \"\"\"Create a new label record.\n\n    Args:\n        id: Unique identifier for the record.\n        project_id: Identifier of the owning project.\n        image_id: Identifier of the labeled image.\n        labeler_id: Identifier of the labeler.\n        label: The label value.\n\n    Returns:\n        A new LabelRecord instance.\n    \"\"\"\n    return cls(\n        id=id, project_id=project_id, image_id=image_id, labeler_id=labeler_id, label=label\n    )\n</code></pre>"},{"location":"reference/domain/entities/#ulabel.domain.labels.LabelExportRow","title":"<code>LabelExportRow(image_id, storage_key, value)</code>  <code>dataclass</code>","text":"<p>A flattened row used when exporting labels for a project.</p> <p>Attributes:</p> Name Type Description <code>image_id</code> <code>UUID</code> <p>Identifier of the image.</p> <code>storage_key</code> <code>str</code> <p>Object-storage key of the image file.</p> <code>value</code> <code>str</code> <p>The label value assigned to the image.</p>"},{"location":"reference/domain/entities/#ulabel.domain.labels.LabelerSubmitStats","title":"<code>LabelerSubmitStats(labeler_count, ranking, total_labelers)</code>  <code>dataclass</code>","text":"<p>Statistics about a labeler's submissions within a project.</p> <p>Attributes:</p> Name Type Description <code>labeler_count</code> <code>int</code> <p>Number of labels submitted by the labeler.</p> <code>ranking</code> <code>int</code> <p>The labeler's rank among all labelers in the project.</p> <code>total_labelers</code> <code>int</code> <p>Total number of labelers in the project.</p>"},{"location":"reference/domain/entities/#user","title":"User","text":""},{"location":"reference/domain/entities/#ulabel.domain.users","title":"<code>users</code>","text":"<p>Domain model for users and their roles.</p>"},{"location":"reference/domain/entities/#ulabel.domain.users.UserRole","title":"<code>UserRole</code>","text":"<p>               Bases: <code>StrEnum</code></p> <p>Available roles that a user can hold in the system.</p>"},{"location":"reference/domain/entities/#ulabel.domain.users.User","title":"<code>User(id, username, role)</code>  <code>dataclass</code>","text":"<p>A user of the labeling platform.</p> <p>Attributes:</p> Name Type Description <code>id</code> <code>UUID</code> <p>Unique identifier for the user.</p> <code>username</code> <code>str</code> <p>The user's display name / login handle.</p> <code>role</code> <code>UserRole</code> <p>The user's role determining their permissions.</p>"},{"location":"reference/domain/entities/#ulabel.domain.users.User.create_admin","title":"<code>create_admin(id, username)</code>  <code>classmethod</code>","text":"<p>Create a user with the ADMIN role.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <code>UUID</code> <p>Unique identifier for the user.</p> required <code>username</code> <code>str</code> <p>The user's display name.</p> required <p>Returns:</p> Type Description <code>User</code> <p>A new User instance with ADMIN role.</p> Source code in <code>src/ulabel/domain/users.py</code> <pre><code>@classmethod\ndef create_admin(cls, id: UUID, username: str) -&gt; \"User\":\n    \"\"\"Create a user with the ADMIN role.\n\n    Args:\n        id: Unique identifier for the user.\n        username: The user's display name.\n\n    Returns:\n        A new User instance with ADMIN role.\n    \"\"\"\n    return cls(id=id, username=username, role=UserRole.ADMIN)\n</code></pre>"},{"location":"reference/domain/entities/#ulabel.domain.users.User.create_labeler","title":"<code>create_labeler(id, username)</code>  <code>classmethod</code>","text":"<p>Create a user with the LABELER role.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <code>UUID</code> <p>Unique identifier for the user.</p> required <code>username</code> <code>str</code> <p>The user's display name.</p> required <p>Returns:</p> Type Description <code>User</code> <p>A new User instance with LABELER role.</p> Source code in <code>src/ulabel/domain/users.py</code> <pre><code>@classmethod\ndef create_labeler(cls, id: UUID, username: str) -&gt; \"User\":\n    \"\"\"Create a user with the LABELER role.\n\n    Args:\n        id: Unique identifier for the user.\n        username: The user's display name.\n\n    Returns:\n        A new User instance with LABELER role.\n    \"\"\"\n    return cls(id=id, username=username, role=UserRole.LABELER)\n</code></pre>"},{"location":"reference/domain/entities/#pagination","title":"Pagination","text":""},{"location":"reference/domain/entities/#ulabel.domain.pagination","title":"<code>pagination</code>","text":"<p>Generic pagination wrapper for domain query results.</p>"},{"location":"reference/domain/entities/#ulabel.domain.pagination.PaginatedResult","title":"<code>PaginatedResult(items, total)</code>  <code>dataclass</code>","text":"<p>               Bases: <code>Generic[T]</code></p> <p>A page of results together with the total count.</p> <p>Attributes:</p> Name Type Description <code>items</code> <code>list[T]</code> <p>The items in the current page.</p> <code>total</code> <code>int</code> <p>The total number of items across all pages.</p>"},{"location":"reference/domain/errors/","title":"Domain Errors","text":"<p>Custom exception classes for domain-level error handling.</p>"},{"location":"reference/domain/errors/#ulabel.domain.errors","title":"<code>errors</code>","text":"<p>Domain-level exceptions for the ulabel application.</p>"},{"location":"reference/domain/errors/#ulabel.domain.errors.DomainError","title":"<code>DomainError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Base exception for all domain-specific errors.</p> <p>All custom exceptions raised within the domain layer should inherit from this class to allow consumers to catch domain errors uniformly.</p>"},{"location":"reference/domain/errors/#ulabel.domain.errors.StorageFull","title":"<code>StorageFull</code>","text":"<p>               Bases: <code>DomainError</code></p> <p>Raised when the storage backend has no free space.</p>"},{"location":"reference/domain/ports/","title":"Domain Ports","text":"<p>Abstract repository interfaces defining the contracts between the domain and infrastructure layers.</p>"},{"location":"reference/domain/ports/#project-repository","title":"Project Repository","text":""},{"location":"reference/domain/ports/#ulabel.domain.ports.project_repository","title":"<code>project_repository</code>","text":"<p>Port interface for project persistence.</p>"},{"location":"reference/domain/ports/#ulabel.domain.ports.project_repository.ProjectRepository","title":"<code>ProjectRepository</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Abstract repository for storing and retrieving projects.</p>"},{"location":"reference/domain/ports/#ulabel.domain.ports.project_repository.ProjectRepository.get_by_id","title":"<code>get_by_id(project_id)</code>  <code>abstractmethod</code> <code>async</code>","text":"<p>Retrieve a project by its unique identifier.</p> <p>Parameters:</p> Name Type Description Default <code>project_id</code> <code>UUID</code> <p>The unique identifier of the project.</p> required <p>Returns:</p> Type Description <code>Project | None</code> <p>The project if found, None otherwise.</p> Source code in <code>src/ulabel/domain/ports/project_repository.py</code> <pre><code>@abstractmethod\nasync def get_by_id(self, project_id: UUID) -&gt; Project | None:\n    \"\"\"Retrieve a project by its unique identifier.\n\n    Args:\n        project_id: The unique identifier of the project.\n\n    Returns:\n        The project if found, None otherwise.\n    \"\"\"\n    raise NotImplementedError\n</code></pre>"},{"location":"reference/domain/ports/#ulabel.domain.ports.project_repository.ProjectRepository.get_by_name","title":"<code>get_by_name(name)</code>  <code>abstractmethod</code> <code>async</code>","text":"<p>Retrieve a project by its name.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The exact project name to search for.</p> required <p>Returns:</p> Type Description <code>Project | None</code> <p>The project if found, None otherwise.</p> Source code in <code>src/ulabel/domain/ports/project_repository.py</code> <pre><code>@abstractmethod\nasync def get_by_name(self, name: str) -&gt; Project | None:\n    \"\"\"Retrieve a project by its name.\n\n    Args:\n        name: The exact project name to search for.\n\n    Returns:\n        The project if found, None otherwise.\n    \"\"\"\n    raise NotImplementedError\n</code></pre>"},{"location":"reference/domain/ports/#ulabel.domain.ports.project_repository.ProjectRepository.get_by_labeler_id","title":"<code>get_by_labeler_id(labeler_id)</code>  <code>abstractmethod</code> <code>async</code>","text":"<p>Retrieve all projects assigned to a given labeler.</p> <p>Parameters:</p> Name Type Description Default <code>labeler_id</code> <code>UUID</code> <p>The identifier of the labeler.</p> required <p>Returns:</p> Type Description <code>list[Project]</code> <p>A list of projects the labeler is assigned to.</p> Source code in <code>src/ulabel/domain/ports/project_repository.py</code> <pre><code>@abstractmethod\nasync def get_by_labeler_id(self, labeler_id: UUID) -&gt; list[Project]:\n    \"\"\"Retrieve all projects assigned to a given labeler.\n\n    Args:\n        labeler_id: The identifier of the labeler.\n\n    Returns:\n        A list of projects the labeler is assigned to.\n    \"\"\"\n    raise NotImplementedError\n</code></pre>"},{"location":"reference/domain/ports/#ulabel.domain.ports.project_repository.ProjectRepository.get_all","title":"<code>get_all(limit, offset, *, name=None)</code>  <code>abstractmethod</code> <code>async</code>","text":"<p>Retrieve a paginated list of projects, optionally filtered by name.</p> <p>Parameters:</p> Name Type Description Default <code>limit</code> <code>int</code> <p>Maximum number of projects to return.</p> required <code>offset</code> <code>int</code> <p>Number of projects to skip.</p> required <code>name</code> <code>str | None</code> <p>Optional name filter (substring or prefix match).</p> <code>None</code> <p>Returns:</p> Type Description <code>PaginatedResult[Project]</code> <p>A paginated result containing the matching projects.</p> Source code in <code>src/ulabel/domain/ports/project_repository.py</code> <pre><code>@abstractmethod\nasync def get_all(\n    self, limit: int, offset: int, *, name: str | None = None\n) -&gt; PaginatedResult[Project]:\n    \"\"\"Retrieve a paginated list of projects, optionally filtered by name.\n\n    Args:\n        limit: Maximum number of projects to return.\n        offset: Number of projects to skip.\n        name: Optional name filter (substring or prefix match).\n\n    Returns:\n        A paginated result containing the matching projects.\n    \"\"\"\n    raise NotImplementedError\n</code></pre>"},{"location":"reference/domain/ports/#ulabel.domain.ports.project_repository.ProjectRepository.save","title":"<code>save(project)</code>  <code>abstractmethod</code> <code>async</code>","text":"<p>Persist a project (insert or update).</p> <p>Parameters:</p> Name Type Description Default <code>project</code> <code>Project</code> <p>The project entity to save.</p> required Source code in <code>src/ulabel/domain/ports/project_repository.py</code> <pre><code>@abstractmethod\nasync def save(self, project: Project) -&gt; None:\n    \"\"\"Persist a project (insert or update).\n\n    Args:\n        project: The project entity to save.\n    \"\"\"\n    raise NotImplementedError\n</code></pre>"},{"location":"reference/domain/ports/#image-repository","title":"Image Repository","text":""},{"location":"reference/domain/ports/#ulabel.domain.ports.image_repository","title":"<code>image_repository</code>","text":"<p>Port interface for image persistence.</p>"},{"location":"reference/domain/ports/#ulabel.domain.ports.image_repository.ImageRepository","title":"<code>ImageRepository</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Abstract repository for storing and retrieving images.</p>"},{"location":"reference/domain/ports/#ulabel.domain.ports.image_repository.ImageRepository.get_by_id","title":"<code>get_by_id(image_id)</code>  <code>abstractmethod</code> <code>async</code>","text":"<p>Retrieve an image by its unique identifier.</p> <p>Parameters:</p> Name Type Description Default <code>image_id</code> <code>UUID</code> <p>The unique identifier of the image.</p> required <p>Returns:</p> Type Description <code>Image | None</code> <p>The image if found, None otherwise.</p> Source code in <code>src/ulabel/domain/ports/image_repository.py</code> <pre><code>@abstractmethod\nasync def get_by_id(self, image_id: UUID) -&gt; Image | None:\n    \"\"\"Retrieve an image by its unique identifier.\n\n    Args:\n        image_id: The unique identifier of the image.\n\n    Returns:\n        The image if found, None otherwise.\n    \"\"\"\n    raise NotImplementedError\n</code></pre>"},{"location":"reference/domain/ports/#ulabel.domain.ports.image_repository.ImageRepository.save","title":"<code>save(image)</code>  <code>abstractmethod</code> <code>async</code>","text":"<p>Persist a single image (insert or update).</p> <p>Parameters:</p> Name Type Description Default <code>image</code> <code>Image</code> <p>The image entity to save.</p> required Source code in <code>src/ulabel/domain/ports/image_repository.py</code> <pre><code>@abstractmethod\nasync def save(self, image: Image) -&gt; None:\n    \"\"\"Persist a single image (insert or update).\n\n    Args:\n        image: The image entity to save.\n    \"\"\"\n    raise NotImplementedError\n</code></pre>"},{"location":"reference/domain/ports/#ulabel.domain.ports.image_repository.ImageRepository.get_next_pending","title":"<code>get_next_pending(project_id)</code>  <code>abstractmethod</code> <code>async</code>","text":"<p>Retrieve the next image with PENDING status in a project.</p> <p>Parameters:</p> Name Type Description Default <code>project_id</code> <code>UUID</code> <p>The project to search within.</p> required <p>Returns:</p> Type Description <code>Image | None</code> <p>The next pending image, or None if all images are assigned or done.</p> Source code in <code>src/ulabel/domain/ports/image_repository.py</code> <pre><code>@abstractmethod\nasync def get_next_pending(self, project_id: UUID) -&gt; Image | None:\n    \"\"\"Retrieve the next image with PENDING status in a project.\n\n    Args:\n        project_id: The project to search within.\n\n    Returns:\n        The next pending image, or None if all images are assigned or done.\n    \"\"\"\n    raise NotImplementedError\n</code></pre>"},{"location":"reference/domain/ports/#ulabel.domain.ports.image_repository.ImageRepository.assign_next_pending","title":"<code>assign_next_pending(project_id, labeler_id, assigned_at)</code>  <code>abstractmethod</code> <code>async</code>","text":"<p>Atomically claim the next pending image and assign it to a labeler.</p> <p>Selects the next pending image and transitions it to IN_PROGRESS within a single transaction to prevent race conditions.</p> <p>Parameters:</p> Name Type Description Default <code>project_id</code> <code>UUID</code> <p>The project to search within.</p> required <code>labeler_id</code> <code>UUID</code> <p>The labeler to assign the image to.</p> required <code>assigned_at</code> <code>datetime</code> <p>The assignment timestamp.</p> required <p>Returns:</p> Type Description <code>Image | None</code> <p>The assigned image, or None if no pending images exist.</p> Source code in <code>src/ulabel/domain/ports/image_repository.py</code> <pre><code>@abstractmethod\nasync def assign_next_pending(\n    self, project_id: UUID, labeler_id: UUID, assigned_at: datetime\n) -&gt; Image | None:\n    \"\"\"Atomically claim the next pending image and assign it to a labeler.\n\n    Selects the next pending image and transitions it to IN_PROGRESS\n    within a single transaction to prevent race conditions.\n\n    Args:\n        project_id: The project to search within.\n        labeler_id: The labeler to assign the image to.\n        assigned_at: The assignment timestamp.\n\n    Returns:\n        The assigned image, or None if no pending images exist.\n    \"\"\"\n    raise NotImplementedError\n</code></pre>"},{"location":"reference/domain/ports/#ulabel.domain.ports.image_repository.ImageRepository.expire_in_progress","title":"<code>expire_in_progress(cutoff)</code>  <code>abstractmethod</code> <code>async</code>","text":"<p>Atomically expire all in-progress images assigned before the cutoff time.</p> <p>Selects expired images and resets them to PENDING within a single transaction to prevent race conditions with concurrent label submissions.</p> <p>Parameters:</p> Name Type Description Default <code>cutoff</code> <code>datetime</code> <p>Images assigned before this timestamp are considered expired.</p> required <p>Returns:</p> Type Description <code>list[Image]</code> <p>A list of images that were expired.</p> Source code in <code>src/ulabel/domain/ports/image_repository.py</code> <pre><code>@abstractmethod\nasync def expire_in_progress(self, cutoff: datetime) -&gt; list[Image]:\n    \"\"\"Atomically expire all in-progress images assigned before the cutoff time.\n\n    Selects expired images and resets them to PENDING within a single\n    transaction to prevent race conditions with concurrent label submissions.\n\n    Args:\n        cutoff: Images assigned before this timestamp are considered expired.\n\n    Returns:\n        A list of images that were expired.\n    \"\"\"\n    raise NotImplementedError\n</code></pre>"},{"location":"reference/domain/ports/#ulabel.domain.ports.image_repository.ImageRepository.get_expired_in_progress","title":"<code>get_expired_in_progress(cutoff)</code>  <code>abstractmethod</code> <code>async</code>","text":"<p>Retrieve all in-progress images assigned before the cutoff time.</p> <p>Parameters:</p> Name Type Description Default <code>cutoff</code> <code>datetime</code> <p>Images assigned before this timestamp are considered expired.</p> required <p>Returns:</p> Type Description <code>list[Image]</code> <p>A list of expired in-progress images.</p> Source code in <code>src/ulabel/domain/ports/image_repository.py</code> <pre><code>@abstractmethod\nasync def get_expired_in_progress(self, cutoff: datetime) -&gt; list[Image]:\n    \"\"\"Retrieve all in-progress images assigned before the cutoff time.\n\n    Args:\n        cutoff: Images assigned before this timestamp are considered expired.\n\n    Returns:\n        A list of expired in-progress images.\n    \"\"\"\n    raise NotImplementedError\n</code></pre>"},{"location":"reference/domain/ports/#ulabel.domain.ports.image_repository.ImageRepository.save_bulk","title":"<code>save_bulk(images)</code>  <code>abstractmethod</code> <code>async</code>","text":"<p>Persist multiple images in a single batch operation.</p> <p>Parameters:</p> Name Type Description Default <code>images</code> <code>list[Image]</code> <p>The list of image entities to save.</p> required Source code in <code>src/ulabel/domain/ports/image_repository.py</code> <pre><code>@abstractmethod\nasync def save_bulk(self, images: list[Image]) -&gt; None:\n    \"\"\"Persist multiple images in a single batch operation.\n\n    Args:\n        images: The list of image entities to save.\n    \"\"\"\n    raise NotImplementedError\n</code></pre>"},{"location":"reference/domain/ports/#label-repository","title":"Label Repository","text":""},{"location":"reference/domain/ports/#ulabel.domain.ports.label_repository","title":"<code>label_repository</code>","text":"<p>Port interface for label persistence.</p>"},{"location":"reference/domain/ports/#ulabel.domain.ports.label_repository.LabelRepository","title":"<code>LabelRepository</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Abstract repository for storing and querying label records.</p>"},{"location":"reference/domain/ports/#ulabel.domain.ports.label_repository.LabelRepository.save","title":"<code>save(label_record)</code>  <code>abstractmethod</code> <code>async</code>","text":"<p>Persist a single label record.</p> <p>Parameters:</p> Name Type Description Default <code>label_record</code> <code>LabelRecord</code> <p>The label record to save.</p> required Source code in <code>src/ulabel/domain/ports/label_repository.py</code> <pre><code>@abstractmethod\nasync def save(self, label_record: LabelRecord) -&gt; None:\n    \"\"\"Persist a single label record.\n\n    Args:\n        label_record: The label record to save.\n    \"\"\"\n    raise NotImplementedError\n</code></pre>"},{"location":"reference/domain/ports/#ulabel.domain.ports.label_repository.LabelRepository.count_by_project","title":"<code>count_by_project(project_id)</code>  <code>abstractmethod</code> <code>async</code>","text":"<p>Count the total number of labels in a project.</p> <p>Parameters:</p> Name Type Description Default <code>project_id</code> <code>UUID</code> <p>The project to count labels for.</p> required <p>Returns:</p> Type Description <code>int</code> <p>The total label count.</p> Source code in <code>src/ulabel/domain/ports/label_repository.py</code> <pre><code>@abstractmethod\nasync def count_by_project(self, project_id: UUID) -&gt; int:\n    \"\"\"Count the total number of labels in a project.\n\n    Args:\n        project_id: The project to count labels for.\n\n    Returns:\n        The total label count.\n    \"\"\"\n    raise NotImplementedError\n</code></pre>"},{"location":"reference/domain/ports/#ulabel.domain.ports.label_repository.LabelRepository.get_export_data","title":"<code>get_export_data(project_id)</code>  <code>abstractmethod</code>","text":"<p>Retrieve flattened label rows for export as an async stream.</p> <p>Parameters:</p> Name Type Description Default <code>project_id</code> <code>UUID</code> <p>The project to export labels from.</p> required <p>Yields:</p> Type Description <code>AsyncIterator[LabelExportRow]</code> <p>Export rows containing image and label information.</p> Source code in <code>src/ulabel/domain/ports/label_repository.py</code> <pre><code>@abstractmethod\ndef get_export_data(self, project_id: UUID) -&gt; AsyncIterator[LabelExportRow]:\n    \"\"\"Retrieve flattened label rows for export as an async stream.\n\n    Args:\n        project_id: The project to export labels from.\n\n    Yields:\n        Export rows containing image and label information.\n    \"\"\"\n    raise NotImplementedError\n</code></pre>"},{"location":"reference/domain/ports/#user-repository","title":"User Repository","text":""},{"location":"reference/domain/ports/#ulabel.domain.ports.user_repository","title":"<code>user_repository</code>","text":"<p>Port interface for user persistence.</p>"},{"location":"reference/domain/ports/#ulabel.domain.ports.user_repository.UserRepository","title":"<code>UserRepository</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Abstract repository for storing and retrieving users.</p>"},{"location":"reference/domain/ports/#ulabel.domain.ports.user_repository.UserRepository.get_by_username","title":"<code>get_by_username(username)</code>  <code>abstractmethod</code> <code>async</code>","text":"<p>Retrieve a user by their username.</p> <p>Parameters:</p> Name Type Description Default <code>username</code> <code>str</code> <p>The exact username to search for.</p> required <p>Returns:</p> Type Description <code>User | None</code> <p>The user if found, None otherwise.</p> Source code in <code>src/ulabel/domain/ports/user_repository.py</code> <pre><code>@abstractmethod\nasync def get_by_username(self, username: str) -&gt; User | None:\n    \"\"\"Retrieve a user by their username.\n\n    Args:\n        username: The exact username to search for.\n\n    Returns:\n        The user if found, None otherwise.\n    \"\"\"\n    raise NotImplementedError\n</code></pre>"},{"location":"reference/domain/ports/#ulabel.domain.ports.user_repository.UserRepository.get_by_id","title":"<code>get_by_id(user_id)</code>  <code>abstractmethod</code> <code>async</code>","text":"<p>Retrieve a user by their unique identifier.</p> <p>Parameters:</p> Name Type Description Default <code>user_id</code> <code>UUID</code> <p>The unique identifier of the user.</p> required <p>Returns:</p> Type Description <code>User | None</code> <p>The user if found, None otherwise.</p> Source code in <code>src/ulabel/domain/ports/user_repository.py</code> <pre><code>@abstractmethod\nasync def get_by_id(self, user_id: UUID) -&gt; User | None:\n    \"\"\"Retrieve a user by their unique identifier.\n\n    Args:\n        user_id: The unique identifier of the user.\n\n    Returns:\n        The user if found, None otherwise.\n    \"\"\"\n    raise NotImplementedError\n</code></pre>"},{"location":"reference/domain/ports/#ulabel.domain.ports.user_repository.UserRepository.get_by_ids","title":"<code>get_by_ids(user_ids)</code>  <code>abstractmethod</code> <code>async</code>","text":"<p>Retrieve multiple users by their unique identifiers in a single query.</p> <p>Parameters:</p> Name Type Description Default <code>user_ids</code> <code>set[UUID]</code> <p>The set of user IDs to look up.</p> required <p>Returns:</p> Type Description <code>list[User]</code> <p>A list of found users. Missing IDs are silently skipped.</p> Source code in <code>src/ulabel/domain/ports/user_repository.py</code> <pre><code>@abstractmethod\nasync def get_by_ids(self, user_ids: set[UUID]) -&gt; list[User]:\n    \"\"\"Retrieve multiple users by their unique identifiers in a single query.\n\n    Args:\n        user_ids: The set of user IDs to look up.\n\n    Returns:\n        A list of found users. Missing IDs are silently skipped.\n    \"\"\"\n    raise NotImplementedError\n</code></pre>"},{"location":"reference/domain/ports/#ulabel.domain.ports.user_repository.UserRepository.search_by_username_prefix","title":"<code>search_by_username_prefix(prefix, *, role=None, limit=10)</code>  <code>abstractmethod</code> <code>async</code>","text":"<p>Search for users whose username starts with the given prefix.</p> <p>Parameters:</p> Name Type Description Default <code>prefix</code> <code>str</code> <p>The username prefix to match.</p> required <code>role</code> <code>str | None</code> <p>Optional role filter to narrow results.</p> <code>None</code> <code>limit</code> <code>int</code> <p>Maximum number of users to return.</p> <code>10</code> <p>Returns:</p> Type Description <code>list[User]</code> <p>A list of matching users.</p> Source code in <code>src/ulabel/domain/ports/user_repository.py</code> <pre><code>@abstractmethod\nasync def search_by_username_prefix(\n    self, prefix: str, *, role: str | None = None, limit: int = 10\n) -&gt; list[User]:\n    \"\"\"Search for users whose username starts with the given prefix.\n\n    Args:\n        prefix: The username prefix to match.\n        role: Optional role filter to narrow results.\n        limit: Maximum number of users to return.\n\n    Returns:\n        A list of matching users.\n    \"\"\"\n    raise NotImplementedError\n</code></pre>"},{"location":"reference/domain/ports/#stats-repository","title":"Stats Repository","text":""},{"location":"reference/domain/ports/#ulabel.domain.ports.stats_repository","title":"<code>stats_repository</code>","text":"<p>Port interface for project statistics queries.</p>"},{"location":"reference/domain/ports/#ulabel.domain.ports.stats_repository.ImageCounts","title":"<code>ImageCounts(total, labeled)</code>  <code>dataclass</code>","text":"<p>Aggregate image counts for a project.</p> <p>Attributes:</p> Name Type Description <code>total</code> <code>int</code> <p>Total number of images in the project.</p> <code>labeled</code> <code>int</code> <p>Number of images that have been labeled.</p>"},{"location":"reference/domain/ports/#ulabel.domain.ports.stats_repository.LabelerClassRow","title":"<code>LabelerClassRow(labeler_id, username, label, count)</code>  <code>dataclass</code>","text":"<p>Per-labeler, per-label count used in statistics dashboards.</p> <p>Attributes:</p> Name Type Description <code>labeler_id</code> <code>UUID</code> <p>Identifier of the labeler.</p> <code>username</code> <code>str</code> <p>Display name of the labeler.</p> <code>label</code> <code>str</code> <p>The label value.</p> <code>count</code> <code>int</code> <p>Number of times this labeler applied this label.</p>"},{"location":"reference/domain/ports/#ulabel.domain.ports.stats_repository.DailyLabelRow","title":"<code>DailyLabelRow(labeler_id, username, day, label, count)</code>  <code>dataclass</code>","text":"<p>Per-labeler, per-day, per-label count for time-series statistics.</p> <p>Attributes:</p> Name Type Description <code>labeler_id</code> <code>UUID</code> <p>Identifier of the labeler.</p> <code>username</code> <code>str</code> <p>Display name of the labeler.</p> <code>day</code> <code>date</code> <p>The date of the aggregation.</p> <code>label</code> <code>str</code> <p>The label value.</p> <code>count</code> <code>int</code> <p>Number of labels submitted on that day.</p>"},{"location":"reference/domain/ports/#ulabel.domain.ports.stats_repository.StatsRepository","title":"<code>StatsRepository</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Abstract repository for querying project and labeler statistics.</p>"},{"location":"reference/domain/ports/#ulabel.domain.ports.stats_repository.StatsRepository.get_image_counts","title":"<code>get_image_counts(project_id)</code>  <code>abstractmethod</code> <code>async</code>","text":"<p>Get total and labeled image counts for a project.</p> <p>Parameters:</p> Name Type Description Default <code>project_id</code> <code>UUID</code> <p>The project to query.</p> required <p>Returns:</p> Type Description <code>ImageCounts</code> <p>Aggregate image counts.</p> Source code in <code>src/ulabel/domain/ports/stats_repository.py</code> <pre><code>@abstractmethod\nasync def get_image_counts(self, project_id: UUID) -&gt; ImageCounts:\n    \"\"\"Get total and labeled image counts for a project.\n\n    Args:\n        project_id: The project to query.\n\n    Returns:\n        Aggregate image counts.\n    \"\"\"\n    raise NotImplementedError\n</code></pre>"},{"location":"reference/domain/ports/#ulabel.domain.ports.stats_repository.StatsRepository.get_labeler_class_counts","title":"<code>get_labeler_class_counts(project_id)</code>  <code>abstractmethod</code> <code>async</code>","text":"<p>Get label counts broken down by labeler and label value.</p> <p>Parameters:</p> Name Type Description Default <code>project_id</code> <code>UUID</code> <p>The project to query.</p> required <p>Returns:</p> Type Description <code>list[LabelerClassRow]</code> <p>A list of per-labeler, per-label count rows.</p> Source code in <code>src/ulabel/domain/ports/stats_repository.py</code> <pre><code>@abstractmethod\nasync def get_labeler_class_counts(self, project_id: UUID) -&gt; list[LabelerClassRow]:\n    \"\"\"Get label counts broken down by labeler and label value.\n\n    Args:\n        project_id: The project to query.\n\n    Returns:\n        A list of per-labeler, per-label count rows.\n    \"\"\"\n    raise NotImplementedError\n</code></pre>"},{"location":"reference/domain/ports/#ulabel.domain.ports.stats_repository.StatsRepository.get_daily_label_counts","title":"<code>get_daily_label_counts(project_id)</code>  <code>abstractmethod</code> <code>async</code>","text":"<p>Get label counts broken down by labeler, day, and label value.</p> <p>Parameters:</p> Name Type Description Default <code>project_id</code> <code>UUID</code> <p>The project to query.</p> required <p>Returns:</p> Type Description <code>list[DailyLabelRow]</code> <p>A list of daily label count rows.</p> Source code in <code>src/ulabel/domain/ports/stats_repository.py</code> <pre><code>@abstractmethod\nasync def get_daily_label_counts(self, project_id: UUID) -&gt; list[DailyLabelRow]:\n    \"\"\"Get label counts broken down by labeler, day, and label value.\n\n    Args:\n        project_id: The project to query.\n\n    Returns:\n        A list of daily label count rows.\n    \"\"\"\n    raise NotImplementedError\n</code></pre>"},{"location":"reference/domain/ports/#ulabel.domain.ports.stats_repository.StatsRepository.get_labeler_ranking","title":"<code>get_labeler_ranking(project_id, labeler_id)</code>  <code>abstractmethod</code> <code>async</code>","text":"<p>Get submission statistics and ranking for a specific labeler.</p> <p>Parameters:</p> Name Type Description Default <code>project_id</code> <code>UUID</code> <p>The project to query.</p> required <code>labeler_id</code> <code>UUID</code> <p>The labeler to get stats for.</p> required <p>Returns:</p> Type Description <code>LabelerSubmitStats</code> <p>The labeler's submission count, ranking, and total labeler count.</p> Source code in <code>src/ulabel/domain/ports/stats_repository.py</code> <pre><code>@abstractmethod\nasync def get_labeler_ranking(self, project_id: UUID, labeler_id: UUID) -&gt; LabelerSubmitStats:\n    \"\"\"Get submission statistics and ranking for a specific labeler.\n\n    Args:\n        project_id: The project to query.\n        labeler_id: The labeler to get stats for.\n\n    Returns:\n        The labeler's submission count, ranking, and total labeler count.\n    \"\"\"\n    raise NotImplementedError\n</code></pre>"},{"location":"reference/domain/ports/#unit-of-work","title":"Unit of Work","text":""},{"location":"reference/domain/ports/#ulabel.domain.ports.unit_of_work","title":"<code>unit_of_work</code>","text":"<p>Port interface for the Unit of Work pattern.</p>"},{"location":"reference/domain/ports/#ulabel.domain.ports.unit_of_work.UnitOfWork","title":"<code>UnitOfWork</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Abstract Unit of Work that manages a shared transaction across repositories.</p> <p>Use as an async context manager to open a session shared by all repositories. Call <code>commit()</code> to persist changes; exiting without committing rolls back automatically.</p>"},{"location":"reference/domain/ports/#ulabel.domain.ports.unit_of_work.UnitOfWork.commit","title":"<code>commit()</code>  <code>abstractmethod</code> <code>async</code>","text":"<p>Commit the current transaction.</p> Source code in <code>src/ulabel/domain/ports/unit_of_work.py</code> <pre><code>@abstractmethod\nasync def commit(self) -&gt; None:\n    \"\"\"Commit the current transaction.\"\"\"\n    raise NotImplementedError\n</code></pre>"},{"location":"reference/domain/ports/#ulabel.domain.ports.unit_of_work.UnitOfWork.rollback","title":"<code>rollback()</code>  <code>abstractmethod</code> <code>async</code>","text":"<p>Roll back the current transaction.</p> Source code in <code>src/ulabel/domain/ports/unit_of_work.py</code> <pre><code>@abstractmethod\nasync def rollback(self) -&gt; None:\n    \"\"\"Roll back the current transaction.\"\"\"\n    raise NotImplementedError\n</code></pre>"},{"location":"reference/domain/ports/#storage-service","title":"Storage Service","text":""},{"location":"reference/domain/ports/#ulabel.domain.ports.storage_service","title":"<code>storage_service</code>","text":"<p>Port interface for object storage operations.</p>"},{"location":"reference/domain/ports/#ulabel.domain.ports.storage_service.StorageService","title":"<code>StorageService</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Abstract service for interacting with an object storage backend.</p>"},{"location":"reference/domain/ports/#ulabel.domain.ports.storage_service.StorageService.get_presigned_url","title":"<code>get_presigned_url(storage_key, expires_in)</code>  <code>abstractmethod</code> <code>async</code>","text":"<p>Generate a time-limited presigned URL for downloading an object.</p> <p>Parameters:</p> Name Type Description Default <code>storage_key</code> <code>str</code> <p>The key identifying the stored object.</p> required <code>expires_in</code> <code>timedelta</code> <p>Duration after which the URL expires.</p> required <p>Returns:</p> Type Description <code>str</code> <p>A presigned URL string.</p> Source code in <code>src/ulabel/domain/ports/storage_service.py</code> <pre><code>@abstractmethod\nasync def get_presigned_url(self, storage_key: str, expires_in: timedelta) -&gt; str:\n    \"\"\"Generate a time-limited presigned URL for downloading an object.\n\n    Args:\n        storage_key: The key identifying the stored object.\n        expires_in: Duration after which the URL expires.\n\n    Returns:\n        A presigned URL string.\n    \"\"\"\n    raise NotImplementedError\n</code></pre>"},{"location":"reference/domain/ports/#ulabel.domain.ports.storage_service.StorageService.upload_file","title":"<code>upload_file(key, data, content_type, size, metadata=None)</code>  <code>abstractmethod</code> <code>async</code>","text":"<p>Upload a file to object storage.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>The storage key for the uploaded object.</p> required <code>data</code> <code>bytes</code> <p>Raw file bytes.</p> required <code>content_type</code> <code>str</code> <p>MIME type of the file.</p> required <code>size</code> <code>int</code> <p>Size of the file in bytes.</p> required <code>metadata</code> <code>dict[str, str] | None</code> <p>Optional metadata to attach to the object.</p> <code>None</code> Source code in <code>src/ulabel/domain/ports/storage_service.py</code> <pre><code>@abstractmethod\nasync def upload_file(\n    self,\n    key: str,\n    data: bytes,\n    content_type: str,\n    size: int,\n    metadata: dict[str, str] | None = None,\n) -&gt; None:\n    \"\"\"Upload a file to object storage.\n\n    Args:\n        key: The storage key for the uploaded object.\n        data: Raw file bytes.\n        content_type: MIME type of the file.\n        size: Size of the file in bytes.\n        metadata: Optional metadata to attach to the object.\n    \"\"\"\n    raise NotImplementedError\n</code></pre>"},{"location":"reference/domain/ports/#ulabel.domain.ports.storage_service.StorageService.upload_file_streaming","title":"<code>upload_file_streaming(key, chunks, content_type, metadata=None)</code>  <code>abstractmethod</code> <code>async</code>","text":"<p>Upload a file to object storage by streaming chunks.</p> <p>Uses multipart upload internally. The caller provides an async iterator of byte chunks; the implementation buffers them into parts of at least 5 MB before uploading each part.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>The storage key for the uploaded object.</p> required <code>chunks</code> <code>AsyncIterator[bytes]</code> <p>Async iterator yielding byte chunks.</p> required <code>content_type</code> <code>str</code> <p>MIME type of the file.</p> required <code>metadata</code> <code>dict[str, str] | None</code> <p>Optional metadata to attach to the object.</p> <code>None</code> Source code in <code>src/ulabel/domain/ports/storage_service.py</code> <pre><code>@abstractmethod\nasync def upload_file_streaming(\n    self,\n    key: str,\n    chunks: AsyncIterator[bytes],\n    content_type: str,\n    metadata: dict[str, str] | None = None,\n) -&gt; None:\n    \"\"\"Upload a file to object storage by streaming chunks.\n\n    Uses multipart upload internally. The caller provides an async\n    iterator of byte chunks; the implementation buffers them into\n    parts of at least 5 MB before uploading each part.\n\n    Args:\n        key: The storage key for the uploaded object.\n        chunks: Async iterator yielding byte chunks.\n        content_type: MIME type of the file.\n        metadata: Optional metadata to attach to the object.\n    \"\"\"\n    raise NotImplementedError\n</code></pre>"},{"location":"reference/domain/ports/#ulabel.domain.ports.storage_service.StorageService.head_object","title":"<code>head_object(key)</code>  <code>abstractmethod</code> <code>async</code>","text":"<p>Retrieve metadata for an object without downloading its contents.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>The storage key of the object.</p> required <p>Returns:</p> Type Description <code>dict[str, str] | None</code> <p>A dictionary of object metadata, or None if the object does not exist.</p> Source code in <code>src/ulabel/domain/ports/storage_service.py</code> <pre><code>@abstractmethod\nasync def head_object(self, key: str) -&gt; dict[str, str] | None:\n    \"\"\"Retrieve metadata for an object without downloading its contents.\n\n    Args:\n        key: The storage key of the object.\n\n    Returns:\n        A dictionary of object metadata, or None if the object does not exist.\n    \"\"\"\n    raise NotImplementedError\n</code></pre>"},{"location":"reference/domain/ports/#ulabel.domain.ports.storage_service.StorageService.list_objects","title":"<code>list_objects(prefix)</code>  <code>abstractmethod</code>","text":"<p>List object keys matching a given prefix.</p> <p>Parameters:</p> Name Type Description Default <code>prefix</code> <code>str</code> <p>The key prefix to filter by.</p> required <p>Returns:</p> Type Description <code>AsyncIterator[str]</code> <p>An async iterator yielding matching object keys.</p> Source code in <code>src/ulabel/domain/ports/storage_service.py</code> <pre><code>@abstractmethod\ndef list_objects(self, prefix: str) -&gt; AsyncIterator[str]:\n    \"\"\"List object keys matching a given prefix.\n\n    Args:\n        prefix: The key prefix to filter by.\n\n    Returns:\n        An async iterator yielding matching object keys.\n    \"\"\"\n    raise NotImplementedError\n</code></pre>"},{"location":"reference/domain/ports/#ulabel.domain.ports.storage_service.StorageService.ensure_bucket","title":"<code>ensure_bucket()</code>  <code>abstractmethod</code> <code>async</code>","text":"<p>Ensure the storage bucket exists, creating it if necessary.</p> Source code in <code>src/ulabel/domain/ports/storage_service.py</code> <pre><code>@abstractmethod\nasync def ensure_bucket(self) -&gt; None:\n    \"\"\"Ensure the storage bucket exists, creating it if necessary.\"\"\"\n    raise NotImplementedError\n</code></pre>"},{"location":"reference/infrastructure/observability/","title":"Observability","text":"<p>OpenTelemetry-based observability infrastructure for tracing, metrics, and logging.</p>"},{"location":"reference/infrastructure/observability/#tracing","title":"Tracing","text":""},{"location":"reference/infrastructure/observability/#ulabel.infrastructure.observability.tracing","title":"<code>tracing</code>","text":"<p>OpenTelemetry distributed tracing configuration.</p> <p>Sets up a TracerProvider with a configurable sampler that supports both ratio-based sampling and forced sampling via HTTP header. Also instruments FastAPI, SQLAlchemy, logging, and aiohttp.</p>"},{"location":"reference/infrastructure/observability/#ulabel.infrastructure.observability.tracing.ForceTraceSampler","title":"<code>ForceTraceSampler(ratio, force_trace_header)</code>","text":"<p>               Bases: <code>Sampler</code></p> <p>Sampler that delegates to a ratio-based sampler but forces sampling when the configured header is present in the span attributes.</p> <p>Initialize the sampler.</p> <p>Parameters:</p> Name Type Description Default <code>ratio</code> <code>float</code> <p>Base sampling ratio (0.0 to 1.0).</p> required <code>force_trace_header</code> <code>str</code> <p>HTTP header name that forces sampling when set to \"true\".</p> required Source code in <code>src/ulabel/infrastructure/observability/tracing.py</code> <pre><code>def __init__(self, ratio: float, force_trace_header: str) -&gt; None:\n    \"\"\"Initialize the sampler.\n\n    Args:\n        ratio: Base sampling ratio (0.0 to 1.0).\n        force_trace_header: HTTP header name that forces sampling when set to \"true\".\n    \"\"\"\n    self._delegate = TraceIdRatioBased(ratio)\n    self._force_key = f\"http.request.header.{force_trace_header.lower()}\"\n</code></pre>"},{"location":"reference/infrastructure/observability/#ulabel.infrastructure.observability.tracing.ForceTraceSampler.should_sample","title":"<code>should_sample(parent_context, trace_id, name, kind=None, attributes=None, links=None, trace_state=None)</code>","text":"<p>Decide whether to sample a span.</p> <p>Forces sampling if the configured header is present and set to \"true\"; otherwise delegates to the ratio-based sampler.</p> <p>Parameters:</p> Name Type Description Default <code>parent_context</code> <code>Context | None</code> <p>The parent span context, if any.</p> required <code>trace_id</code> <code>int</code> <p>The trace identifier.</p> required <code>name</code> <code>str</code> <p>The span name.</p> required <code>kind</code> <code>SpanKind | None</code> <p>The span kind.</p> <code>None</code> <code>attributes</code> <code>Attributes | None</code> <p>Span attributes that may contain HTTP headers.</p> <code>None</code> <code>links</code> <code>Sequence[Link] | None</code> <p>Links to other spans.</p> <code>None</code> <p>Returns:</p> Type Description <code>SamplingResult</code> <p>A SamplingResult indicating whether to record and sample.</p> Source code in <code>src/ulabel/infrastructure/observability/tracing.py</code> <pre><code>def should_sample(\n    self,\n    parent_context: Context | None,\n    trace_id: int,\n    name: str,\n    kind: SpanKind | None = None,\n    attributes: Attributes | None = None,\n    links: Sequence[Link] | None = None,\n    trace_state: TraceState | None = None,\n) -&gt; SamplingResult:\n    \"\"\"Decide whether to sample a span.\n\n    Forces sampling if the configured header is present and set to\n    \"true\"; otherwise delegates to the ratio-based sampler.\n\n    Args:\n        parent_context: The parent span context, if any.\n        trace_id: The trace identifier.\n        name: The span name.\n        kind: The span kind.\n        attributes: Span attributes that may contain HTTP headers.\n        links: Links to other spans.\n\n    Returns:\n        A SamplingResult indicating whether to record and sample.\n    \"\"\"\n    if attributes:\n        header_values = attributes.get(self._force_key)\n        if header_values:\n            values = (\n                header_values if isinstance(header_values, (list, tuple)) else [header_values]\n            )\n            if any(str(v).lower() == \"true\" for v in values):\n                return SamplingResult(Decision.RECORD_AND_SAMPLE, attributes)\n\n    return self._delegate.should_sample(parent_context, trace_id, name, kind, attributes, links)\n</code></pre>"},{"location":"reference/infrastructure/observability/#ulabel.infrastructure.observability.tracing.ForceTraceSampler.get_description","title":"<code>get_description()</code>","text":"<p>Return a human-readable description of this sampler.</p> <p>Returns:</p> Type Description <code>str</code> <p>A string describing the sampler and its delegate.</p> Source code in <code>src/ulabel/infrastructure/observability/tracing.py</code> <pre><code>def get_description(self) -&gt; str:\n    \"\"\"Return a human-readable description of this sampler.\n\n    Returns:\n        A string describing the sampler and its delegate.\n    \"\"\"\n    return f\"ForceTraceSampler(delegate={self._delegate.get_description()})\"\n</code></pre>"},{"location":"reference/infrastructure/observability/#ulabel.infrastructure.observability.tracing.setup_tracing","title":"<code>setup_tracing(service_name, endpoint, enabled, sample_ratio, force_trace_header)</code>","text":"<p>Initialize OpenTelemetry tracing with OTLP gRPC export.</p> <p>Parameters:</p> Name Type Description Default <code>service_name</code> <code>str</code> <p>The service name for the trace resource.</p> required <code>endpoint</code> <code>str</code> <p>OTLP collector endpoint URL.</p> required <code>enabled</code> <code>str</code> <p>Whether tracing is enabled (\"true\"/\"false\").</p> required <code>sample_ratio</code> <code>str</code> <p>Ratio of traces to sample (e.g., \"0.1\" for 10%).</p> required <code>force_trace_header</code> <code>str</code> <p>HTTP header name that forces sampling.</p> required <p>Returns:</p> Type Description <code>TracerProvider | None</code> <p>The configured TracerProvider, or None if tracing is disabled.</p> Source code in <code>src/ulabel/infrastructure/observability/tracing.py</code> <pre><code>def setup_tracing(\n    service_name: str,\n    endpoint: str,\n    enabled: str,\n    sample_ratio: str,\n    force_trace_header: str,\n) -&gt; TracerProvider | None:\n    \"\"\"Initialize OpenTelemetry tracing with OTLP gRPC export.\n\n    Args:\n        service_name: The service name for the trace resource.\n        endpoint: OTLP collector endpoint URL.\n        enabled: Whether tracing is enabled (\"true\"/\"false\").\n        sample_ratio: Ratio of traces to sample (e.g., \"0.1\" for 10%).\n        force_trace_header: HTTP header name that forces sampling.\n\n    Returns:\n        The configured TracerProvider, or None if tracing is disabled.\n    \"\"\"\n    if enabled.lower() != \"true\":\n        logger.info(\"Tracing disabled\")\n        return None\n\n    ratio = float(sample_ratio)\n    resource = Resource.create({\"service.name\": service_name})\n\n    sampler = ForceTraceSampler(ratio, force_trace_header)\n    provider = TracerProvider(resource=resource, sampler=sampler)\n\n    exporter = OTLPSpanExporter(endpoint=endpoint, insecure=True)\n    provider.add_span_processor(BatchSpanProcessor(exporter))\n\n    trace.set_tracer_provider(provider)\n    logger.info(\"Tracing enabled: endpoint=%s, sample_ratio=%s\", endpoint, ratio)\n    return provider\n</code></pre>"},{"location":"reference/infrastructure/observability/#ulabel.infrastructure.observability.tracing.instrument_fastapi","title":"<code>instrument_fastapi(app)</code>","text":"<p>Add OpenTelemetry tracing middleware to the FastAPI app.</p> <p>Must be called at module level, before the ASGI lifespan builds the middleware stack. The middleware resolves the global TracerProvider at request time, so the provider can be set up later in the lifespan.</p> <p>Parameters:</p> Name Type Description Default <code>app</code> <code>FastAPI</code> <p>The FastAPI application to instrument.</p> required Source code in <code>src/ulabel/infrastructure/observability/tracing.py</code> <pre><code>def instrument_fastapi(app: FastAPI) -&gt; None:\n    \"\"\"Add OpenTelemetry tracing middleware to the FastAPI app.\n\n    Must be called at module level, before the ASGI lifespan builds\n    the middleware stack. The middleware resolves the global TracerProvider\n    at request time, so the provider can be set up later in the lifespan.\n\n    Args:\n        app: The FastAPI application to instrument.\n    \"\"\"\n    FastAPIInstrumentor.instrument_app(app)\n</code></pre>"},{"location":"reference/infrastructure/observability/#ulabel.infrastructure.observability.tracing.instrument_libraries","title":"<code>instrument_libraries(engine)</code>","text":"<p>Instrument libraries that do not depend on the middleware stack.</p> <p>Safe to call inside the ASGI lifespan.</p> <p>Parameters:</p> Name Type Description Default <code>engine</code> <code>AsyncEngine</code> <p>The async SQLAlchemy engine to instrument.</p> required Source code in <code>src/ulabel/infrastructure/observability/tracing.py</code> <pre><code>def instrument_libraries(engine: AsyncEngine) -&gt; None:\n    \"\"\"Instrument libraries that do not depend on the middleware stack.\n\n    Safe to call inside the ASGI lifespan.\n\n    Args:\n        engine: The async SQLAlchemy engine to instrument.\n    \"\"\"\n    SQLAlchemyInstrumentor().instrument(engine=engine.sync_engine)\n    LoggingInstrumentor().instrument(set_logging_format=True)\n    AioHttpClientInstrumentor().instrument()\n    logger.info(\"Instrumentation complete: SQLAlchemy, Logging, AioHTTP\")\n</code></pre>"},{"location":"reference/infrastructure/observability/#ulabel.infrastructure.observability.tracing.shutdown_tracing","title":"<code>shutdown_tracing(provider)</code>","text":"<p>Gracefully shut down the tracer provider and flush pending spans.</p> <p>Parameters:</p> Name Type Description Default <code>provider</code> <code>TracerProvider | None</code> <p>The TracerProvider to shut down, or None if tracing is disabled.</p> required Source code in <code>src/ulabel/infrastructure/observability/tracing.py</code> <pre><code>def shutdown_tracing(provider: TracerProvider | None) -&gt; None:\n    \"\"\"Gracefully shut down the tracer provider and flush pending spans.\n\n    Args:\n        provider: The TracerProvider to shut down, or None if tracing is disabled.\n    \"\"\"\n    if provider is not None:\n        provider.shutdown()\n        logger.info(\"Tracing shut down\")\n</code></pre>"},{"location":"reference/infrastructure/observability/#metrics","title":"Metrics","text":""},{"location":"reference/infrastructure/observability/#ulabel.infrastructure.observability.metrics","title":"<code>metrics</code>","text":"<p>Prometheus metrics collection and HTTP middleware.</p> <p>Defines counters, histograms, and gauges for HTTP request tracking, and provides a pure ASGI middleware that records metrics per request. Uses OpenMetrics exposition format with exemplars linking metrics to traces.</p>"},{"location":"reference/infrastructure/observability/#ulabel.infrastructure.observability.metrics.PrometheusMiddleware","title":"<code>PrometheusMiddleware(app)</code>","text":"<p>Pure ASGI middleware that records Prometheus metrics for each HTTP request.</p> Source code in <code>src/ulabel/infrastructure/observability/metrics.py</code> <pre><code>def __init__(self, app: ASGIApp) -&gt; None:\n    self.app = app\n</code></pre>"},{"location":"reference/infrastructure/observability/#ulabel.infrastructure.observability.metrics.metrics_route","title":"<code>metrics_route(request)</code>  <code>async</code>","text":"<p>Serve Prometheus metrics in OpenMetrics exposition format.</p> <p>Uses OpenMetrics format to support exemplars linking metrics to traces.</p> <p>Parameters:</p> Name Type Description Default <code>request</code> <code>Request</code> <p>The incoming HTTP request.</p> required <p>Returns:</p> Type Description <code>Response</code> <p>A response with all collected Prometheus metrics in OpenMetrics format.</p> Source code in <code>src/ulabel/infrastructure/observability/metrics.py</code> <pre><code>async def metrics_route(request: Request) -&gt; Response:\n    \"\"\"Serve Prometheus metrics in OpenMetrics exposition format.\n\n    Uses OpenMetrics format to support exemplars linking metrics to traces.\n\n    Args:\n        request: The incoming HTTP request.\n\n    Returns:\n        A response with all collected Prometheus metrics in OpenMetrics format.\n    \"\"\"\n    return Response(\n        content=generate_latest(REGISTRY),  # type: ignore[no-untyped-call]\n        media_type=CONTENT_TYPE_LATEST,\n    )\n</code></pre>"},{"location":"reference/infrastructure/observability/#logging","title":"Logging","text":""},{"location":"reference/infrastructure/observability/#ulabel.infrastructure.observability.logging","title":"<code>logging</code>","text":"<p>Structured logging configuration for the uLabel service.</p> <p>Supports both JSON (using orjson) and plain-text log formats, with OpenTelemetry trace/span ID injection.</p>"},{"location":"reference/infrastructure/observability/#ulabel.infrastructure.observability.logging.JsonFormatter","title":"<code>JsonFormatter(service_name)</code>","text":"<p>               Bases: <code>Formatter</code></p> <p>High-performance JSON log formatter using orjson.</p> <p>Initialize with the service name to include in each log entry.</p> <p>Parameters:</p> Name Type Description Default <code>service_name</code> <code>str</code> <p>Name of the service for log identification.</p> required Source code in <code>src/ulabel/infrastructure/observability/logging.py</code> <pre><code>def __init__(self, service_name: str) -&gt; None:\n    \"\"\"Initialize with the service name to include in each log entry.\n\n    Args:\n        service_name: Name of the service for log identification.\n    \"\"\"\n    super().__init__()\n    self.service_name = service_name\n</code></pre>"},{"location":"reference/infrastructure/observability/#ulabel.infrastructure.observability.logging.JsonFormatter.format","title":"<code>format(record)</code>","text":"<p>Format a log record as a JSON string.</p> <p>Parameters:</p> Name Type Description Default <code>record</code> <code>LogRecord</code> <p>The log record to format.</p> required <p>Returns:</p> Type Description <code>str</code> <p>A JSON-encoded string with timestamp, level, message, and trace IDs.</p> Source code in <code>src/ulabel/infrastructure/observability/logging.py</code> <pre><code>def format(self, record: logging.LogRecord) -&gt; str:\n    \"\"\"Format a log record as a JSON string.\n\n    Args:\n        record: The log record to format.\n\n    Returns:\n        A JSON-encoded string with timestamp, level, message, and trace IDs.\n    \"\"\"\n    log_entry = {\n        \"timestamp\": datetime.fromtimestamp(record.created, tz=timezone.utc).isoformat(),\n        \"level\": record.levelname,\n        \"logger\": record.name,\n        \"message\": record.getMessage(),\n        \"service\": self.service_name,\n        \"trace_id\": getattr(record, \"otelTraceID\", \"\"),\n        \"span_id\": getattr(record, \"otelSpanID\", \"\"),\n    }\n    if record.exc_info and record.exc_info[1] is not None:\n        log_entry[\"exception\"] = self.formatException(record.exc_info)\n    return orjson.dumps(log_entry).decode()\n</code></pre>"},{"location":"reference/infrastructure/observability/#ulabel.infrastructure.observability.logging.configure_logging","title":"<code>configure_logging(log_level, log_format, service_name)</code>","text":"<p>Configure the root logger with the specified level and format.</p> <p>Parameters:</p> Name Type Description Default <code>log_level</code> <code>str</code> <p>Logging level (e.g., \"INFO\", \"DEBUG\").</p> required <code>log_format</code> <code>str</code> <p>Output format, either \"json\" or \"text\".</p> required <code>service_name</code> <code>str</code> <p>Service name for JSON log entries.</p> required Source code in <code>src/ulabel/infrastructure/observability/logging.py</code> <pre><code>def configure_logging(log_level: str, log_format: str, service_name: str) -&gt; None:\n    \"\"\"Configure the root logger with the specified level and format.\n\n    Args:\n        log_level: Logging level (e.g., \"INFO\", \"DEBUG\").\n        log_format: Output format, either \"json\" or \"text\".\n        service_name: Service name for JSON log entries.\n    \"\"\"\n    root = logging.getLogger()\n    root.setLevel(log_level.upper())\n\n    # Remove existing handlers to avoid duplicates\n    root.handlers.clear()\n\n    handler = logging.StreamHandler(sys.stdout)\n\n    if log_format == \"json\":\n        handler.setFormatter(JsonFormatter(service_name))\n    else:\n        handler.setFormatter(\n            logging.Formatter(\n                \"%(asctime)s %(levelname)-8s %(name)s [%(otelTraceID)s] %(message)s\",\n                defaults={\"otelTraceID\": \"\"},\n            )\n        )\n\n    root.addHandler(handler)\n\n    # Suppress noisy loggers\n    logging.getLogger(\"uvicorn.access\").setLevel(logging.WARNING)\n</code></pre>"},{"location":"reference/infrastructure/repositories/","title":"Repository Implementations","text":"<p>Concrete implementations of the domain repository ports.</p>"},{"location":"reference/infrastructure/repositories/#unit-of-work","title":"Unit of Work","text":""},{"location":"reference/infrastructure/repositories/#sqlalchemy-unit-of-work","title":"SQLAlchemy Unit of Work","text":""},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.unit_of_work","title":"<code>unit_of_work</code>","text":"<p>SQLAlchemy implementation of the Unit of Work pattern.</p> <p>Opens a single database session shared by all repositories for the duration of one logical operation, ensuring atomicity and reducing connection overhead.</p>"},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.unit_of_work.SqlAlchemyUnitOfWork","title":"<code>SqlAlchemyUnitOfWork(sessionmaker)</code>","text":"<p>               Bases: <code>UnitOfWork</code></p> <p>SQLAlchemy-backed Unit of Work.</p> <p>Creates a single <code>AsyncSession</code> on entry and constructs all repositories sharing that session. Supports being entered multiple times (each entry creates a fresh session).</p> Source code in <code>src/ulabel/infrastructure/unit_of_work.py</code> <pre><code>def __init__(self, sessionmaker: async_sessionmaker[AsyncSession]):\n    self._sessionmaker = sessionmaker\n</code></pre>"},{"location":"reference/infrastructure/repositories/#in-memory-unit-of-work","title":"In-Memory Unit of Work","text":""},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.in_memory.unit_of_work","title":"<code>unit_of_work</code>","text":"<p>In-memory implementation of the Unit of Work for testing.</p>"},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.in_memory.unit_of_work.InMemoryUnitOfWork","title":"<code>InMemoryUnitOfWork(project_repository, image_repository, label_repository, stats_repository, user_repository, import_job_repository)</code>","text":"<p>               Bases: <code>UnitOfWork</code></p> <p>In-memory Unit of Work backed by pre-built repository instances.</p> <p><code>commit()</code> and <code>rollback()</code> are no-ops since in-memory repositories mutate state immediately.</p> Source code in <code>src/ulabel/infrastructure/repositories/in_memory/unit_of_work.py</code> <pre><code>def __init__(\n    self,\n    project_repository: ProjectRepository,\n    image_repository: ImageRepository,\n    label_repository: LabelRepository,\n    stats_repository: StatsRepository,\n    user_repository: UserRepository,\n    import_job_repository: ImportJobRepository,\n):\n    self.project_repository = project_repository\n    self.image_repository = image_repository\n    self.label_repository = label_repository\n    self.stats_repository = stats_repository\n    self.user_repository = user_repository\n    self.import_job_repository = import_job_repository\n</code></pre>"},{"location":"reference/infrastructure/repositories/#sqlalchemy-repositories","title":"SQLAlchemy Repositories","text":""},{"location":"reference/infrastructure/repositories/#project-repository","title":"Project Repository","text":""},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.sql.project_repository","title":"<code>project_repository</code>","text":"<p>SQLAlchemy implementation of the project repository.</p> <p>Provides PostgreSQL-backed persistence for projects with eager loading of related labels and labelers, and full upsert support.</p>"},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.sql.project_repository.SqlAlchemyProjectRepository","title":"<code>SqlAlchemyProjectRepository(session)</code>","text":"<p>               Bases: <code>ProjectRepository</code></p> <p>PostgreSQL-backed project repository using SQLAlchemy.</p> <p>Initialize with a shared async session.</p> <p>Parameters:</p> Name Type Description Default <code>session</code> <code>AsyncSession</code> <p>The async database session managed by the Unit of Work.</p> required Source code in <code>src/ulabel/infrastructure/repositories/sql/project_repository.py</code> <pre><code>def __init__(self, session: AsyncSession):\n    \"\"\"Initialize with a shared async session.\n\n    Args:\n        session: The async database session managed by the Unit of Work.\n    \"\"\"\n    self._session = session\n</code></pre>"},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.sql.project_repository.SqlAlchemyProjectRepository.get_by_id","title":"<code>get_by_id(project_id)</code>  <code>async</code>","text":"<p>Retrieve a project by its ID with all relations loaded.</p> <p>Parameters:</p> Name Type Description Default <code>project_id</code> <code>UUID</code> <p>The project's unique identifier.</p> required <p>Returns:</p> Type Description <code>Project | None</code> <p>The domain Project if found, otherwise None.</p> Source code in <code>src/ulabel/infrastructure/repositories/sql/project_repository.py</code> <pre><code>async def get_by_id(self, project_id: UUID) -&gt; Project | None:\n    \"\"\"Retrieve a project by its ID with all relations loaded.\n\n    Args:\n        project_id: The project's unique identifier.\n\n    Returns:\n        The domain Project if found, otherwise None.\n    \"\"\"\n    result = await self._session.execute(\n        select(ProjectModel).where(ProjectModel.id == project_id).options(*_load_options())\n    )\n    model = result.unique().scalar_one_or_none()\n    return model.to_domain() if model else None\n</code></pre>"},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.sql.project_repository.SqlAlchemyProjectRepository.get_by_name","title":"<code>get_by_name(name)</code>  <code>async</code>","text":"<p>Retrieve a project by its unique name.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The project name to look up.</p> required <p>Returns:</p> Type Description <code>Project | None</code> <p>The domain Project if found, otherwise None.</p> Source code in <code>src/ulabel/infrastructure/repositories/sql/project_repository.py</code> <pre><code>async def get_by_name(self, name: str) -&gt; Project | None:\n    \"\"\"Retrieve a project by its unique name.\n\n    Args:\n        name: The project name to look up.\n\n    Returns:\n        The domain Project if found, otherwise None.\n    \"\"\"\n    result = await self._session.execute(\n        select(ProjectModel).where(ProjectModel.name == name).options(*_load_options())\n    )\n    model = result.unique().scalar_one_or_none()\n    return model.to_domain() if model else None\n</code></pre>"},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.sql.project_repository.SqlAlchemyProjectRepository.get_by_labeler_id","title":"<code>get_by_labeler_id(labeler_id)</code>  <code>async</code>","text":"<p>Retrieve all projects that a labeler is assigned to.</p> <p>Parameters:</p> Name Type Description Default <code>labeler_id</code> <code>UUID</code> <p>The labeler's user ID.</p> required <p>Returns:</p> Type Description <code>list[Project]</code> <p>A list of domain Projects the labeler belongs to.</p> Source code in <code>src/ulabel/infrastructure/repositories/sql/project_repository.py</code> <pre><code>async def get_by_labeler_id(self, labeler_id: UUID) -&gt; list[Project]:\n    \"\"\"Retrieve all projects that a labeler is assigned to.\n\n    Args:\n        labeler_id: The labeler's user ID.\n\n    Returns:\n        A list of domain Projects the labeler belongs to.\n    \"\"\"\n    result = await self._session.execute(\n        select(ProjectModel)\n        .join(ProjectLabelerModel, ProjectModel.id == ProjectLabelerModel.project_id)\n        .where(ProjectLabelerModel.labeler_id == labeler_id)\n        .options(*_load_options())\n    )\n    return [row.to_domain() for row in result.unique().scalars()]\n</code></pre>"},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.sql.project_repository.SqlAlchemyProjectRepository.get_all","title":"<code>get_all(limit, offset, *, name=None)</code>  <code>async</code>","text":"<p>Retrieve a paginated list of projects, optionally filtered by name.</p> <p>Parameters:</p> Name Type Description Default <code>limit</code> <code>int</code> <p>Maximum number of projects to return.</p> required <code>offset</code> <code>int</code> <p>Number of projects to skip.</p> required <code>name</code> <code>str | None</code> <p>Optional case-insensitive name filter (contains match).</p> <code>None</code> <p>Returns:</p> Type Description <code>PaginatedResult[Project]</code> <p>A PaginatedResult with projects ordered by creation date (newest first).</p> Source code in <code>src/ulabel/infrastructure/repositories/sql/project_repository.py</code> <pre><code>async def get_all(\n    self, limit: int, offset: int, *, name: str | None = None\n) -&gt; PaginatedResult[Project]:\n    \"\"\"Retrieve a paginated list of projects, optionally filtered by name.\n\n    Args:\n        limit: Maximum number of projects to return.\n        offset: Number of projects to skip.\n        name: Optional case-insensitive name filter (contains match).\n\n    Returns:\n        A PaginatedResult with projects ordered by creation date (newest first).\n    \"\"\"\n    filters = []\n    if name is not None:\n        filters.append(ProjectModel.name.ilike(f\"%{name}%\"))\n\n    count_result = await self._session.execute(\n        select(func.count()).select_from(ProjectModel).where(*filters)\n    )\n    total = count_result.scalar_one()\n\n    result = await self._session.execute(\n        select(ProjectModel)\n        .where(*filters)\n        .options(*_load_options())\n        .order_by(ProjectModel.created_at.desc())\n        .limit(limit)\n        .offset(offset)\n    )\n    items = [row.to_domain() for row in result.unique().scalars()]\n    return PaginatedResult(items=items, total=total)\n</code></pre>"},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.sql.project_repository.SqlAlchemyProjectRepository.save","title":"<code>save(project)</code>  <code>async</code>","text":"<p>Save or update a project with its labels and labelers.</p> <p>Uses upsert for the project row and replaces all label and labeler associations on each save.</p> <p>Parameters:</p> Name Type Description Default <code>project</code> <code>Project</code> <p>The domain Project to persist.</p> required Source code in <code>src/ulabel/infrastructure/repositories/sql/project_repository.py</code> <pre><code>async def save(self, project: Project) -&gt; None:\n    \"\"\"Save or update a project with its labels and labelers.\n\n    Uses upsert for the project row and replaces all label and\n    labeler associations on each save.\n\n    Args:\n        project: The domain Project to persist.\n    \"\"\"\n    # Upsert project row\n    await self._session.execute(\n        insert(ProjectModel)\n        .values(\n            id=project.id,\n            owner_id=project.owner.id,\n            name=project.name,\n            description=project.description,\n        )\n        .on_conflict_do_update(\n            index_elements=[\"id\"],\n            set_={\"name\": project.name, \"description\": project.description},\n        )\n    )\n\n    # Replace labels: delete existing, insert current\n    await self._session.execute(\n        delete(ProjectLabelModel).where(ProjectLabelModel.project_id == project.id)\n    )\n    if project.labels:\n        await self._session.execute(\n            insert(ProjectLabelModel),\n            [{\"project_id\": project.id, \"label\": label} for label in project.labels],\n        )\n\n    # Replace labelers: delete existing, insert current\n    await self._session.execute(\n        delete(ProjectLabelerModel).where(ProjectLabelerModel.project_id == project.id)\n    )\n    if project.labeler_ids:\n        await self._session.execute(\n            insert(ProjectLabelerModel),\n            [{\"project_id\": project.id, \"labeler_id\": lid} for lid in project.labeler_ids],\n        )\n</code></pre>"},{"location":"reference/infrastructure/repositories/#image-repository","title":"Image Repository","text":""},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.sql.image_repository","title":"<code>image_repository</code>","text":"<p>SQLAlchemy implementation of the image repository.</p> <p>Provides PostgreSQL-backed persistence for images using upsert semantics and row-level locking for concurrent assignment safety.</p>"},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.sql.image_repository.SqlAlchemyImageRepository","title":"<code>SqlAlchemyImageRepository(session)</code>","text":"<p>               Bases: <code>ImageRepository</code></p> <p>PostgreSQL-backed image repository using SQLAlchemy.</p> <p>Initialize with a shared async session.</p> <p>Parameters:</p> Name Type Description Default <code>session</code> <code>AsyncSession</code> <p>The async database session managed by the Unit of Work.</p> required Source code in <code>src/ulabel/infrastructure/repositories/sql/image_repository.py</code> <pre><code>def __init__(self, session: AsyncSession):\n    \"\"\"Initialize with a shared async session.\n\n    Args:\n        session: The async database session managed by the Unit of Work.\n    \"\"\"\n    self._session = session\n</code></pre>"},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.sql.image_repository.SqlAlchemyImageRepository.get_by_id","title":"<code>get_by_id(image_id)</code>  <code>async</code>","text":"<p>Retrieve an image by its ID.</p> <p>Parameters:</p> Name Type Description Default <code>image_id</code> <code>UUID</code> <p>The image's unique identifier.</p> required <p>Returns:</p> Type Description <code>Image | None</code> <p>The domain Image if found, otherwise None.</p> Source code in <code>src/ulabel/infrastructure/repositories/sql/image_repository.py</code> <pre><code>async def get_by_id(self, image_id: UUID) -&gt; Image | None:\n    \"\"\"Retrieve an image by its ID.\n\n    Args:\n        image_id: The image's unique identifier.\n\n    Returns:\n        The domain Image if found, otherwise None.\n    \"\"\"\n    result = await self._session.execute(select(ImageModel).where(ImageModel.id == image_id))\n    model = result.scalar_one_or_none()\n    return model.to_domain() if model else None\n</code></pre>"},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.sql.image_repository.SqlAlchemyImageRepository.save","title":"<code>save(image)</code>  <code>async</code>","text":"<p>Save or update an image using upsert semantics.</p> <p>Parameters:</p> Name Type Description Default <code>image</code> <code>Image</code> <p>The domain Image to persist.</p> required Source code in <code>src/ulabel/infrastructure/repositories/sql/image_repository.py</code> <pre><code>async def save(self, image: Image) -&gt; None:\n    \"\"\"Save or update an image using upsert semantics.\n\n    Args:\n        image: The domain Image to persist.\n    \"\"\"\n    stmt = (\n        insert(ImageModel)\n        .values(\n            id=image.id,\n            project_id=image.project_id,\n            storage_key=image.storage_key,\n            status=image.status.value,\n            labeler_id=image.labeler_id,\n            assigned_at=image.assigned_at,\n            assignment_id=image.assignment_id,\n        )\n        .on_conflict_do_update(\n            index_elements=[\"id\"],\n            set_={\n                \"status\": image.status.value,\n                \"labeler_id\": image.labeler_id,\n                \"assigned_at\": image.assigned_at,\n                \"assignment_id\": image.assignment_id,\n            },\n        )\n    )\n    await self._session.execute(stmt)\n</code></pre>"},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.sql.image_repository.SqlAlchemyImageRepository.get_next_pending","title":"<code>get_next_pending(project_id)</code>  <code>async</code>","text":"<p>Get the next pending image for a project using row-level locking.</p> <p>Uses <code>SELECT ... FOR UPDATE SKIP LOCKED</code> to safely handle concurrent assignment requests.</p> <p>Parameters:</p> Name Type Description Default <code>project_id</code> <code>UUID</code> <p>The project to find a pending image in.</p> required <p>Returns:</p> Type Description <code>Image | None</code> <p>The next pending Image, or None if no images are available.</p> Source code in <code>src/ulabel/infrastructure/repositories/sql/image_repository.py</code> <pre><code>async def get_next_pending(self, project_id: UUID) -&gt; Image | None:\n    \"\"\"Get the next pending image for a project using row-level locking.\n\n    Uses ``SELECT ... FOR UPDATE SKIP LOCKED`` to safely handle\n    concurrent assignment requests.\n\n    Args:\n        project_id: The project to find a pending image in.\n\n    Returns:\n        The next pending Image, or None if no images are available.\n    \"\"\"\n    result = await self._session.execute(\n        select(ImageModel)\n        .where(\n            ImageModel.project_id == project_id,\n            ImageModel.status == ImageStatus.PENDING.value,\n        )\n        .order_by(ImageModel.id)\n        .limit(1)\n        .with_for_update(skip_locked=True)\n    )\n    model = result.scalar_one_or_none()\n    return model.to_domain() if model else None\n</code></pre>"},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.sql.image_repository.SqlAlchemyImageRepository.assign_next_pending","title":"<code>assign_next_pending(project_id, labeler_id, assigned_at)</code>  <code>async</code>","text":"<p>Atomically claim the next pending image within a single transaction.</p> <p>Uses <code>SELECT ... FOR UPDATE SKIP LOCKED</code> followed by an update, all within the same session, so the row lock is held until commit.</p> <p>Parameters:</p> Name Type Description Default <code>project_id</code> <code>UUID</code> <p>The project to find a pending image in.</p> required <code>labeler_id</code> <code>UUID</code> <p>The labeler to assign the image to.</p> required <code>assigned_at</code> <code>datetime</code> <p>The assignment timestamp.</p> required <p>Returns:</p> Type Description <code>Image | None</code> <p>The assigned Image, or None if no pending images are available.</p> Source code in <code>src/ulabel/infrastructure/repositories/sql/image_repository.py</code> <pre><code>async def assign_next_pending(\n    self, project_id: UUID, labeler_id: UUID, assigned_at: datetime\n) -&gt; Image | None:\n    \"\"\"Atomically claim the next pending image within a single transaction.\n\n    Uses ``SELECT ... FOR UPDATE SKIP LOCKED`` followed by an update,\n    all within the same session, so the row lock is held until commit.\n\n    Args:\n        project_id: The project to find a pending image in.\n        labeler_id: The labeler to assign the image to.\n        assigned_at: The assignment timestamp.\n\n    Returns:\n        The assigned Image, or None if no pending images are available.\n    \"\"\"\n    result = await self._session.execute(\n        select(ImageModel)\n        .where(\n            ImageModel.project_id == project_id,\n            ImageModel.status == ImageStatus.PENDING.value,\n        )\n        .order_by(ImageModel.id)\n        .limit(1)\n        .with_for_update(skip_locked=True)\n    )\n    model = result.scalar_one_or_none()\n    if model is None:\n        return None\n    image = model.to_domain()\n    image.assign(labeler_id=labeler_id, assigned_at=assigned_at)\n    stmt = (\n        insert(ImageModel)\n        .values(\n            id=image.id,\n            project_id=image.project_id,\n            storage_key=image.storage_key,\n            status=image.status.value,\n            labeler_id=image.labeler_id,\n            assigned_at=image.assigned_at,\n            assignment_id=image.assignment_id,\n        )\n        .on_conflict_do_update(\n            index_elements=[\"id\"],\n            set_={\n                \"status\": image.status.value,\n                \"labeler_id\": image.labeler_id,\n                \"assigned_at\": image.assigned_at,\n                \"assignment_id\": image.assignment_id,\n            },\n        )\n    )\n    await self._session.execute(stmt)\n    return image\n</code></pre>"},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.sql.image_repository.SqlAlchemyImageRepository.expire_in_progress","title":"<code>expire_in_progress(cutoff)</code>  <code>async</code>","text":"<p>Atomically expire stale in-progress images within a single transaction.</p> <p>Uses <code>SELECT ... FOR UPDATE SKIP LOCKED</code> to lock rows, then resets each to PENDING via upsert, all within the same session.</p> <p>Parameters:</p> Name Type Description Default <code>cutoff</code> <code>datetime</code> <p>Images assigned before this time are considered expired.</p> required <p>Returns:</p> Type Description <code>list[Image]</code> <p>A list of images that were expired.</p> Source code in <code>src/ulabel/infrastructure/repositories/sql/image_repository.py</code> <pre><code>async def expire_in_progress(self, cutoff: datetime) -&gt; list[Image]:\n    \"\"\"Atomically expire stale in-progress images within a single transaction.\n\n    Uses ``SELECT ... FOR UPDATE SKIP LOCKED`` to lock rows, then\n    resets each to PENDING via upsert, all within the same session.\n\n    Args:\n        cutoff: Images assigned before this time are considered expired.\n\n    Returns:\n        A list of images that were expired.\n    \"\"\"\n    result = await self._session.execute(\n        select(ImageModel)\n        .where(\n            ImageModel.status == ImageStatus.IN_PROGRESS.value,\n            ImageModel.assigned_at &lt; cutoff,\n        )\n        .with_for_update(skip_locked=True)\n    )\n    models = list(result.scalars())\n    if not models:\n        return []\n    expired: list[Image] = []\n    for model in models:\n        image = model.to_domain()\n        image.expire()\n        stmt = (\n            insert(ImageModel)\n            .values(\n                id=image.id,\n                project_id=image.project_id,\n                storage_key=image.storage_key,\n                status=image.status.value,\n                labeler_id=image.labeler_id,\n                assigned_at=image.assigned_at,\n                assignment_id=image.assignment_id,\n            )\n            .on_conflict_do_update(\n                index_elements=[\"id\"],\n                set_={\n                    \"status\": image.status.value,\n                    \"labeler_id\": image.labeler_id,\n                    \"assigned_at\": image.assigned_at,\n                    \"assignment_id\": image.assignment_id,\n                },\n            )\n        )\n        await self._session.execute(stmt)\n        expired.append(image)\n    return expired\n</code></pre>"},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.sql.image_repository.SqlAlchemyImageRepository.get_expired_in_progress","title":"<code>get_expired_in_progress(cutoff)</code>  <code>async</code>","text":"<p>Find all in-progress images assigned before the cutoff time.</p> <p>Parameters:</p> Name Type Description Default <code>cutoff</code> <code>datetime</code> <p>Images assigned before this time are considered expired.</p> required <p>Returns:</p> Type Description <code>list[Image]</code> <p>A list of expired in-progress Images.</p> Source code in <code>src/ulabel/infrastructure/repositories/sql/image_repository.py</code> <pre><code>async def get_expired_in_progress(self, cutoff: datetime) -&gt; list[Image]:\n    \"\"\"Find all in-progress images assigned before the cutoff time.\n\n    Args:\n        cutoff: Images assigned before this time are considered expired.\n\n    Returns:\n        A list of expired in-progress Images.\n    \"\"\"\n    result = await self._session.execute(\n        select(ImageModel).where(\n            ImageModel.status == ImageStatus.IN_PROGRESS.value,\n            ImageModel.assigned_at &lt; cutoff,\n        )\n    )\n    return [m.to_domain() for m in result.scalars()]\n</code></pre>"},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.sql.image_repository.SqlAlchemyImageRepository.save_bulk","title":"<code>save_bulk(images)</code>  <code>async</code>","text":"<p>Bulk-insert images in chunks, skipping duplicates.</p> <p>Parameters:</p> Name Type Description Default <code>images</code> <code>list[Image]</code> <p>The list of domain Images to insert.</p> required Source code in <code>src/ulabel/infrastructure/repositories/sql/image_repository.py</code> <pre><code>async def save_bulk(self, images: list[Image]) -&gt; None:\n    \"\"\"Bulk-insert images in chunks, skipping duplicates.\n\n    Args:\n        images: The list of domain Images to insert.\n    \"\"\"\n    if not images:\n        return\n    chunk_size = 1000\n    for i in range(0, len(images), chunk_size):\n        chunk = images[i : i + chunk_size]\n        await self._session.execute(\n            insert(ImageModel)\n            .values(\n                [\n                    {\n                        \"id\": img.id,\n                        \"project_id\": img.project_id,\n                        \"storage_key\": img.storage_key,\n                        \"status\": img.status.value,\n                        \"labeler_id\": img.labeler_id,\n                        \"assigned_at\": img.assigned_at,\n                        \"assignment_id\": img.assignment_id,\n                    }\n                    for img in chunk\n                ]\n            )\n            .on_conflict_do_nothing()\n        )\n</code></pre>"},{"location":"reference/infrastructure/repositories/#label-repository","title":"Label Repository","text":""},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.sql.label_repository","title":"<code>label_repository</code>","text":"<p>SQLAlchemy implementation of the label repository.</p> <p>Provides PostgreSQL-backed persistence for label records, including saving, counting, and exporting label data.</p>"},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.sql.label_repository.SqlAlchemyLabelRepository","title":"<code>SqlAlchemyLabelRepository(session)</code>","text":"<p>               Bases: <code>LabelRepository</code></p> <p>PostgreSQL-backed label repository using SQLAlchemy.</p> <p>Initialize with a shared async session.</p> <p>Parameters:</p> Name Type Description Default <code>session</code> <code>AsyncSession</code> <p>The async database session managed by the Unit of Work.</p> required Source code in <code>src/ulabel/infrastructure/repositories/sql/label_repository.py</code> <pre><code>def __init__(self, session: AsyncSession):\n    \"\"\"Initialize with a shared async session.\n\n    Args:\n        session: The async database session managed by the Unit of Work.\n    \"\"\"\n    self._session = session\n</code></pre>"},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.sql.label_repository.SqlAlchemyLabelRepository.save","title":"<code>save(label_record)</code>  <code>async</code>","text":"<p>Persist a label record, ignoring conflicts on duplicate image IDs.</p> <p>Parameters:</p> Name Type Description Default <code>label_record</code> <code>LabelRecord</code> <p>The domain LabelRecord to save.</p> required Source code in <code>src/ulabel/infrastructure/repositories/sql/label_repository.py</code> <pre><code>async def save(self, label_record: LabelRecord) -&gt; None:\n    \"\"\"Persist a label record, ignoring conflicts on duplicate image IDs.\n\n    Args:\n        label_record: The domain LabelRecord to save.\n    \"\"\"\n    stmt = (\n        insert(LabelRecordModel)\n        .values(\n            id=label_record.id,\n            project_id=label_record.project_id,\n            image_id=label_record.image_id,\n            labeler_id=label_record.labeler_id,\n            label=label_record.label,\n        )\n        .on_conflict_do_nothing()\n    )\n    await self._session.execute(stmt)\n</code></pre>"},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.sql.label_repository.SqlAlchemyLabelRepository.count_by_project","title":"<code>count_by_project(project_id)</code>  <code>async</code>","text":"<p>Count the total number of labels for a project.</p> <p>Parameters:</p> Name Type Description Default <code>project_id</code> <code>UUID</code> <p>The project to count labels for.</p> required <p>Returns:</p> Type Description <code>int</code> <p>The total label count.</p> Source code in <code>src/ulabel/infrastructure/repositories/sql/label_repository.py</code> <pre><code>async def count_by_project(self, project_id: UUID) -&gt; int:\n    \"\"\"Count the total number of labels for a project.\n\n    Args:\n        project_id: The project to count labels for.\n\n    Returns:\n        The total label count.\n    \"\"\"\n    result = await self._session.execute(\n        select(func.count(LabelRecordModel.id)).where(LabelRecordModel.project_id == project_id)\n    )\n    return result.scalar_one()\n</code></pre>"},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.sql.label_repository.SqlAlchemyLabelRepository.get_export_data","title":"<code>get_export_data(project_id)</code>  <code>async</code>","text":"<p>Retrieve label data joined with image storage keys for export as a stream.</p> <p>Parameters:</p> Name Type Description Default <code>project_id</code> <code>UUID</code> <p>The project to export labels for.</p> required <p>Yields:</p> Type Description <code>AsyncIterator[LabelExportRow]</code> <p>LabelExportRow objects ordered by image ID.</p> Source code in <code>src/ulabel/infrastructure/repositories/sql/label_repository.py</code> <pre><code>async def get_export_data(self, project_id: UUID) -&gt; AsyncIterator[LabelExportRow]:\n    \"\"\"Retrieve label data joined with image storage keys for export as a stream.\n\n    Args:\n        project_id: The project to export labels for.\n\n    Yields:\n        LabelExportRow objects ordered by image ID.\n    \"\"\"\n    stmt = (\n        select(\n            LabelRecordModel.image_id,\n            ImageModel.storage_key,\n            LabelRecordModel.label,\n        )\n        .join(ImageModel, LabelRecordModel.image_id == ImageModel.id)\n        .where(LabelRecordModel.project_id == project_id)\n        .order_by(LabelRecordModel.image_id)\n    )\n    result = await self._session.stream(stmt)\n    async for row in result:\n        yield LabelExportRow(\n            image_id=row.image_id,\n            storage_key=row.storage_key,\n            value=row.label,\n        )\n</code></pre>"},{"location":"reference/infrastructure/repositories/#user-repository","title":"User Repository","text":""},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.sql.user_repository","title":"<code>user_repository</code>","text":"<p>SQLAlchemy implementation of the user repository.</p> <p>Provides PostgreSQL-backed persistence for users with lookup by username, ID, prefix search, and upsert support.</p>"},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.sql.user_repository.SqlAlchemyUserRepository","title":"<code>SqlAlchemyUserRepository(session)</code>","text":"<p>               Bases: <code>UserRepository</code></p> <p>PostgreSQL-backed user repository using SQLAlchemy.</p> <p>Initialize with a shared async session.</p> <p>Parameters:</p> Name Type Description Default <code>session</code> <code>AsyncSession</code> <p>The async database session managed by the Unit of Work.</p> required Source code in <code>src/ulabel/infrastructure/repositories/sql/user_repository.py</code> <pre><code>def __init__(self, session: AsyncSession):\n    \"\"\"Initialize with a shared async session.\n\n    Args:\n        session: The async database session managed by the Unit of Work.\n    \"\"\"\n    self._session = session\n</code></pre>"},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.sql.user_repository.SqlAlchemyUserRepository.get_by_username","title":"<code>get_by_username(username)</code>  <code>async</code>","text":"<p>Retrieve a user by their unique username.</p> <p>Parameters:</p> Name Type Description Default <code>username</code> <code>str</code> <p>The username to look up.</p> required <p>Returns:</p> Type Description <code>User | None</code> <p>The domain User if found, otherwise None.</p> Source code in <code>src/ulabel/infrastructure/repositories/sql/user_repository.py</code> <pre><code>async def get_by_username(self, username: str) -&gt; User | None:\n    \"\"\"Retrieve a user by their unique username.\n\n    Args:\n        username: The username to look up.\n\n    Returns:\n        The domain User if found, otherwise None.\n    \"\"\"\n    result = await self._session.execute(\n        select(UserModel).where(UserModel.username == username)\n    )\n    model = result.scalar_one_or_none()\n    return model.to_domain() if model else None\n</code></pre>"},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.sql.user_repository.SqlAlchemyUserRepository.get_by_id","title":"<code>get_by_id(user_id)</code>  <code>async</code>","text":"<p>Retrieve a user by their unique ID.</p> <p>Parameters:</p> Name Type Description Default <code>user_id</code> <code>UUID</code> <p>The user's unique identifier.</p> required <p>Returns:</p> Type Description <code>User | None</code> <p>The domain User if found, otherwise None.</p> Source code in <code>src/ulabel/infrastructure/repositories/sql/user_repository.py</code> <pre><code>async def get_by_id(self, user_id: UUID) -&gt; User | None:\n    \"\"\"Retrieve a user by their unique ID.\n\n    Args:\n        user_id: The user's unique identifier.\n\n    Returns:\n        The domain User if found, otherwise None.\n    \"\"\"\n    result = await self._session.execute(select(UserModel).where(UserModel.id == user_id))\n    model = result.scalar_one_or_none()\n    return model.to_domain() if model else None\n</code></pre>"},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.sql.user_repository.SqlAlchemyUserRepository.get_by_ids","title":"<code>get_by_ids(user_ids)</code>  <code>async</code>","text":"<p>Retrieve multiple users by ID in a single query.</p> <p>Parameters:</p> Name Type Description Default <code>user_ids</code> <code>set[UUID]</code> <p>The set of user IDs to look up.</p> required <p>Returns:</p> Type Description <code>list[User]</code> <p>A list of found domain Users.</p> Source code in <code>src/ulabel/infrastructure/repositories/sql/user_repository.py</code> <pre><code>async def get_by_ids(self, user_ids: set[UUID]) -&gt; list[User]:\n    \"\"\"Retrieve multiple users by ID in a single query.\n\n    Args:\n        user_ids: The set of user IDs to look up.\n\n    Returns:\n        A list of found domain Users.\n    \"\"\"\n    if not user_ids:\n        return []\n    result = await self._session.execute(select(UserModel).where(UserModel.id.in_(user_ids)))\n    return [row.to_domain() for row in result.scalars()]\n</code></pre>"},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.sql.user_repository.SqlAlchemyUserRepository.search_by_username_prefix","title":"<code>search_by_username_prefix(prefix, *, role=None, limit=10)</code>  <code>async</code>","text":"<p>Search users by username prefix with optional role filter.</p> <p>Parameters:</p> Name Type Description Default <code>prefix</code> <code>str</code> <p>The username prefix to match (case-insensitive).</p> required <code>role</code> <code>str | None</code> <p>Optional role to filter by.</p> <code>None</code> <code>limit</code> <code>int</code> <p>Maximum number of results.</p> <code>10</code> <p>Returns:</p> Type Description <code>list[User]</code> <p>A list of matching Users ordered by username.</p> Source code in <code>src/ulabel/infrastructure/repositories/sql/user_repository.py</code> <pre><code>async def search_by_username_prefix(\n    self, prefix: str, *, role: str | None = None, limit: int = 10\n) -&gt; list[User]:\n    \"\"\"Search users by username prefix with optional role filter.\n\n    Args:\n        prefix: The username prefix to match (case-insensitive).\n        role: Optional role to filter by.\n        limit: Maximum number of results.\n\n    Returns:\n        A list of matching Users ordered by username.\n    \"\"\"\n    stmt = select(UserModel).where(UserModel.username.ilike(f\"{prefix}%\"))\n    if role is not None:\n        stmt = stmt.where(UserModel.role == role)\n    stmt = stmt.order_by(UserModel.username).limit(limit)\n    result = await self._session.execute(stmt)\n    return [row.to_domain() for row in result.scalars()]\n</code></pre>"},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.sql.user_repository.SqlAlchemyUserRepository.save","title":"<code>save(user)</code>  <code>async</code>","text":"<p>Save or update a user using upsert semantics.</p> <p>Parameters:</p> Name Type Description Default <code>user</code> <code>User</code> <p>The domain User to persist.</p> required Source code in <code>src/ulabel/infrastructure/repositories/sql/user_repository.py</code> <pre><code>async def save(self, user: User) -&gt; None:\n    \"\"\"Save or update a user using upsert semantics.\n\n    Args:\n        user: The domain User to persist.\n    \"\"\"\n    stmt = (\n        insert(UserModel)\n        .values(id=user.id, username=user.username, role=user.role.value)\n        .on_conflict_do_update(\n            index_elements=[\"id\"],\n            set_={\"username\": user.username, \"role\": user.role.value},\n        )\n    )\n    await self._session.execute(stmt)\n</code></pre>"},{"location":"reference/infrastructure/repositories/#stats-repository","title":"Stats Repository","text":""},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.sql.stats_repository","title":"<code>stats_repository</code>","text":"<p>SQLAlchemy implementation of the statistics repository.</p> <p>Provides PostgreSQL-backed queries for image counts, labeler rankings, class distributions, and daily activity breakdowns.</p>"},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.sql.stats_repository.SqlAlchemyStatsRepository","title":"<code>SqlAlchemyStatsRepository(session)</code>","text":"<p>               Bases: <code>StatsRepository</code></p> <p>PostgreSQL-backed statistics repository using SQLAlchemy.</p> <p>Initialize with a shared async session.</p> <p>Parameters:</p> Name Type Description Default <code>session</code> <code>AsyncSession</code> <p>The async database session managed by the Unit of Work.</p> required Source code in <code>src/ulabel/infrastructure/repositories/sql/stats_repository.py</code> <pre><code>def __init__(self, session: AsyncSession):\n    \"\"\"Initialize with a shared async session.\n\n    Args:\n        session: The async database session managed by the Unit of Work.\n    \"\"\"\n    self._session = session\n</code></pre>"},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.sql.stats_repository.SqlAlchemyStatsRepository.get_image_counts","title":"<code>get_image_counts(project_id)</code>  <code>async</code>","text":"<p>Get total and labeled image counts for a project.</p> <p>Parameters:</p> Name Type Description Default <code>project_id</code> <code>UUID</code> <p>The project to count images for.</p> required <p>Returns:</p> Type Description <code>ImageCounts</code> <p>An ImageCounts with total and labeled counts.</p> Source code in <code>src/ulabel/infrastructure/repositories/sql/stats_repository.py</code> <pre><code>async def get_image_counts(self, project_id: UUID) -&gt; ImageCounts:\n    \"\"\"Get total and labeled image counts for a project.\n\n    Args:\n        project_id: The project to count images for.\n\n    Returns:\n        An ImageCounts with total and labeled counts.\n    \"\"\"\n    stmt = select(\n        func.count().label(\"total\"),\n        func.count(case((ImageModel.status == \"done\", 1))).label(\"labeled\"),\n    ).where(ImageModel.project_id == project_id)\n    row = (await self._session.execute(stmt)).one()\n    return ImageCounts(total=row.total, labeled=row.labeled)\n</code></pre>"},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.sql.stats_repository.SqlAlchemyStatsRepository.get_labeler_class_counts","title":"<code>get_labeler_class_counts(project_id)</code>  <code>async</code>","text":"<p>Get per-labeler, per-class label counts for a project.</p> <p>Parameters:</p> Name Type Description Default <code>project_id</code> <code>UUID</code> <p>The project to query.</p> required <p>Returns:</p> Type Description <code>list[LabelerClassRow]</code> <p>A list of LabelerClassRow with labeler, class, and count data.</p> Source code in <code>src/ulabel/infrastructure/repositories/sql/stats_repository.py</code> <pre><code>async def get_labeler_class_counts(self, project_id: UUID) -&gt; list[LabelerClassRow]:\n    \"\"\"Get per-labeler, per-class label counts for a project.\n\n    Args:\n        project_id: The project to query.\n\n    Returns:\n        A list of LabelerClassRow with labeler, class, and count data.\n    \"\"\"\n    stmt = (\n        select(\n            LabelRecordModel.labeler_id,\n            UserModel.username,\n            LabelRecordModel.label,\n            func.count().label(\"cnt\"),\n        )\n        .join(UserModel, LabelRecordModel.labeler_id == UserModel.id)\n        .where(LabelRecordModel.project_id == project_id)\n        .group_by(\n            LabelRecordModel.labeler_id,\n            UserModel.username,\n            LabelRecordModel.label,\n        )\n    )\n    rows = (await self._session.execute(stmt)).all()\n    return [\n        LabelerClassRow(\n            labeler_id=r.labeler_id,\n            username=r.username,\n            label=r.label,\n            count=r.cnt,\n        )\n        for r in rows\n    ]\n</code></pre>"},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.sql.stats_repository.SqlAlchemyStatsRepository.get_daily_label_counts","title":"<code>get_daily_label_counts(project_id)</code>  <code>async</code>","text":"<p>Get daily per-labeler, per-class label counts for a project.</p> <p>Parameters:</p> Name Type Description Default <code>project_id</code> <code>UUID</code> <p>The project to query.</p> required <p>Returns:</p> Type Description <code>list[DailyLabelRow]</code> <p>A list of DailyLabelRow ordered by date.</p> Source code in <code>src/ulabel/infrastructure/repositories/sql/stats_repository.py</code> <pre><code>async def get_daily_label_counts(self, project_id: UUID) -&gt; list[DailyLabelRow]:\n    \"\"\"Get daily per-labeler, per-class label counts for a project.\n\n    Args:\n        project_id: The project to query.\n\n    Returns:\n        A list of DailyLabelRow ordered by date.\n    \"\"\"\n    day_col = cast(LabelRecordModel.created_at, Date).label(\"day\")\n    stmt = (\n        select(\n            LabelRecordModel.labeler_id,\n            UserModel.username,\n            day_col,\n            LabelRecordModel.label,\n            func.count().label(\"cnt\"),\n        )\n        .join(UserModel, LabelRecordModel.labeler_id == UserModel.id)\n        .where(LabelRecordModel.project_id == project_id)\n        .group_by(\n            LabelRecordModel.labeler_id,\n            UserModel.username,\n            day_col,\n            LabelRecordModel.label,\n        )\n        .order_by(day_col)\n    )\n    rows = (await self._session.execute(stmt)).all()\n    return [\n        DailyLabelRow(\n            labeler_id=r.labeler_id,\n            username=r.username,\n            day=r.day,\n            label=r.label,\n            count=r.cnt,\n        )\n        for r in rows\n    ]\n</code></pre>"},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.sql.stats_repository.SqlAlchemyStatsRepository.get_labeler_ranking","title":"<code>get_labeler_ranking(project_id, labeler_id)</code>  <code>async</code>","text":"<p>Compute a labeler's ranking within a project.</p> <p>Parameters:</p> Name Type Description Default <code>project_id</code> <code>UUID</code> <p>The project to compute rankings for.</p> required <code>labeler_id</code> <code>UUID</code> <p>The labeler whose ranking to determine.</p> required <p>Returns:</p> Type Description <code>LabelerSubmitStats</code> <p>A LabelerSubmitStats with count, ranking position, and total labelers.</p> Source code in <code>src/ulabel/infrastructure/repositories/sql/stats_repository.py</code> <pre><code>async def get_labeler_ranking(self, project_id: UUID, labeler_id: UUID) -&gt; LabelerSubmitStats:\n    \"\"\"Compute a labeler's ranking within a project.\n\n    Args:\n        project_id: The project to compute rankings for.\n        labeler_id: The labeler whose ranking to determine.\n\n    Returns:\n        A LabelerSubmitStats with count, ranking position, and total labelers.\n    \"\"\"\n    # Subquery: count per labeler in this project\n    counts_sq = (\n        select(\n            LabelRecordModel.labeler_id,\n            func.count().label(\"cnt\"),\n        )\n        .where(LabelRecordModel.project_id == project_id)\n        .group_by(LabelRecordModel.labeler_id)\n        .subquery()\n    )\n\n    # Get total labelers who have submitted at least one label\n    total_stmt = select(func.count()).select_from(counts_sq)\n    total_labelers = (await self._session.execute(total_stmt)).scalar_one()\n\n    # Get this labeler's count\n    labeler_cnt_stmt = select(counts_sq.c.cnt).where(counts_sq.c.labeler_id == labeler_id)\n    labeler_count = (await self._session.execute(labeler_cnt_stmt)).scalar_one_or_none() or 0\n\n    # Ranking: how many labelers have strictly more labels than this one\n    ranking_stmt = (\n        select(func.count()).select_from(counts_sq).where(counts_sq.c.cnt &gt; labeler_count)\n    )\n    above = (await self._session.execute(ranking_stmt)).scalar_one()\n    ranking = above + 1\n\n    return LabelerSubmitStats(\n        labeler_count=labeler_count,\n        ranking=ranking,\n        total_labelers=total_labelers,\n    )\n</code></pre>"},{"location":"reference/infrastructure/repositories/#import-job-repository","title":"Import Job Repository","text":""},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.sql.import_job_repository","title":"<code>import_job_repository</code>","text":"<p>SQLAlchemy implementation of the import job repository.</p> <p>Provides PostgreSQL-backed persistence for import jobs using upsert semantics.</p>"},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.sql.import_job_repository.SqlAlchemyImportJobRepository","title":"<code>SqlAlchemyImportJobRepository(session)</code>","text":"<p>               Bases: <code>ImportJobRepository</code></p> <p>PostgreSQL-backed import job repository using SQLAlchemy.</p> <p>Initialize with a shared async session.</p> <p>Parameters:</p> Name Type Description Default <code>session</code> <code>AsyncSession</code> <p>The async database session managed by the Unit of Work.</p> required Source code in <code>src/ulabel/infrastructure/repositories/sql/import_job_repository.py</code> <pre><code>def __init__(self, session: AsyncSession):\n    \"\"\"Initialize with a shared async session.\n\n    Args:\n        session: The async database session managed by the Unit of Work.\n    \"\"\"\n    self._session = session\n</code></pre>"},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.sql.import_job_repository.SqlAlchemyImportJobRepository.save","title":"<code>save(job)</code>  <code>async</code>","text":"<p>Save or update an import job using upsert semantics.</p> <p>Parameters:</p> Name Type Description Default <code>job</code> <code>ImportJob</code> <p>The domain ImportJob to persist.</p> required Source code in <code>src/ulabel/infrastructure/repositories/sql/import_job_repository.py</code> <pre><code>async def save(self, job: ImportJob) -&gt; None:\n    \"\"\"Save or update an import job using upsert semantics.\n\n    Args:\n        job: The domain ImportJob to persist.\n    \"\"\"\n    stmt = (\n        insert(ImportJobModel)\n        .values(\n            id=job.id,\n            project_id=job.project_id,\n            prefix=job.prefix,\n            status=job.status.value,\n            imported=job.imported,\n            error=job.error,\n        )\n        .on_conflict_do_update(\n            index_elements=[\"id\"],\n            set_={\n                \"status\": job.status.value,\n                \"imported\": job.imported,\n                \"error\": job.error,\n            },\n        )\n    )\n    await self._session.execute(stmt)\n</code></pre>"},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.sql.import_job_repository.SqlAlchemyImportJobRepository.get_by_id","title":"<code>get_by_id(job_id)</code>  <code>async</code>","text":"<p>Retrieve an import job by its ID.</p> <p>Parameters:</p> Name Type Description Default <code>job_id</code> <code>UUID</code> <p>The import job's unique identifier.</p> required <p>Returns:</p> Type Description <code>ImportJob | None</code> <p>The domain ImportJob if found, otherwise None.</p> Source code in <code>src/ulabel/infrastructure/repositories/sql/import_job_repository.py</code> <pre><code>async def get_by_id(self, job_id: UUID) -&gt; ImportJob | None:\n    \"\"\"Retrieve an import job by its ID.\n\n    Args:\n        job_id: The import job's unique identifier.\n\n    Returns:\n        The domain ImportJob if found, otherwise None.\n    \"\"\"\n    result = await self._session.execute(\n        select(ImportJobModel).where(ImportJobModel.id == job_id)\n    )\n    model = result.scalar_one_or_none()\n    return model.to_domain() if model else None\n</code></pre>"},{"location":"reference/infrastructure/repositories/#in-memory-repositories","title":"In-Memory Repositories","text":"<p>Used for testing purposes.</p>"},{"location":"reference/infrastructure/repositories/#project-repository_1","title":"Project Repository","text":""},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.in_memory.project_repository","title":"<code>project_repository</code>","text":"<p>In-memory implementation of the project repository for testing.</p>"},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.in_memory.project_repository.InMemoryProjectRepository","title":"<code>InMemoryProjectRepository(projects=None)</code>","text":"<p>               Bases: <code>ProjectRepository</code></p> <p>In-memory project repository backed by a dictionary.</p> <p>Initialize with optional seed projects.</p> <p>Parameters:</p> Name Type Description Default <code>projects</code> <code>list[Project] | None</code> <p>Optional list of projects to pre-populate the repository.</p> <code>None</code> Source code in <code>src/ulabel/infrastructure/repositories/in_memory/project_repository.py</code> <pre><code>def __init__(self, projects: list[Project] | None = None):\n    \"\"\"Initialize with optional seed projects.\n\n    Args:\n        projects: Optional list of projects to pre-populate the repository.\n    \"\"\"\n    self._projects: dict[UUID, Project] = {p.id: p for p in (projects or [])}\n</code></pre>"},{"location":"reference/infrastructure/repositories/#image-repository_1","title":"Image Repository","text":""},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.in_memory.image_repository","title":"<code>image_repository</code>","text":"<p>In-memory implementation of the image repository for testing.</p>"},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.in_memory.image_repository.InMemoryImageRepository","title":"<code>InMemoryImageRepository(images=None)</code>","text":"<p>               Bases: <code>ImageRepository</code></p> <p>In-memory image repository backed by a dictionary.</p> <p>Initialize with optional seed images.</p> <p>Parameters:</p> Name Type Description Default <code>images</code> <code>list[Image] | None</code> <p>Optional list of images to pre-populate the repository.</p> <code>None</code> Source code in <code>src/ulabel/infrastructure/repositories/in_memory/image_repository.py</code> <pre><code>def __init__(self, images: list[Image] | None = None):\n    \"\"\"Initialize with optional seed images.\n\n    Args:\n        images: Optional list of images to pre-populate the repository.\n    \"\"\"\n    self._images: dict[UUID, Image] = {i.id: i for i in (images or [])}\n</code></pre>"},{"location":"reference/infrastructure/repositories/#label-repository_1","title":"Label Repository","text":""},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.in_memory.label_repository","title":"<code>label_repository</code>","text":"<p>In-memory implementation of the label repository for testing.</p>"},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.in_memory.label_repository.InMemoryLabelRepository","title":"<code>InMemoryLabelRepository()</code>","text":"<p>               Bases: <code>LabelRepository</code></p> <p>In-memory label repository backed by a dictionary.</p> Source code in <code>src/ulabel/infrastructure/repositories/in_memory/label_repository.py</code> <pre><code>def __init__(self) -&gt; None:\n    self._records: dict[UUID, LabelRecord] = {}\n</code></pre>"},{"location":"reference/infrastructure/repositories/#user-repository_1","title":"User Repository","text":""},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.in_memory.user_repository","title":"<code>user_repository</code>","text":"<p>In-memory implementation of the user repository for testing.</p>"},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.in_memory.user_repository.InMemoryUserRepository","title":"<code>InMemoryUserRepository(users=None)</code>","text":"<p>               Bases: <code>UserRepository</code></p> <p>In-memory user repository backed by username and ID dictionaries.</p> <p>Initialize with optional seed users.</p> <p>Parameters:</p> Name Type Description Default <code>users</code> <code>list[User] | None</code> <p>Optional list of users to pre-populate the repository.</p> <code>None</code> Source code in <code>src/ulabel/infrastructure/repositories/in_memory/user_repository.py</code> <pre><code>def __init__(self, users: list[User] | None = None):\n    \"\"\"Initialize with optional seed users.\n\n    Args:\n        users: Optional list of users to pre-populate the repository.\n    \"\"\"\n    self._by_username: dict[str, User] = {u.username: u for u in (users or [])}\n    self._by_id: dict[UUID, User] = {u.id: u for u in (users or [])}\n</code></pre>"},{"location":"reference/infrastructure/repositories/#stats-repository_1","title":"Stats Repository","text":""},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.in_memory.stats_repository","title":"<code>stats_repository</code>","text":"<p>In-memory implementation of the statistics repository for testing.</p>"},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.in_memory.stats_repository.InMemoryStatsRepository","title":"<code>InMemoryStatsRepository(images, labels, usernames)</code>","text":"<p>               Bases: <code>StatsRepository</code></p> <p>In-memory implementation for testing. Operates on shared lists.</p> <p>Initialize with shared data collections.</p> <p>Parameters:</p> Name Type Description Default <code>images</code> <code>list[Image]</code> <p>Shared list of images to compute stats from.</p> required <code>labels</code> <code>list[LabelRecord]</code> <p>Shared list of label records.</p> required <code>usernames</code> <code>dict[UUID, str]</code> <p>Mapping of user IDs to usernames.</p> required Source code in <code>src/ulabel/infrastructure/repositories/in_memory/stats_repository.py</code> <pre><code>def __init__(\n    self,\n    images: list[Image],\n    labels: list[LabelRecord],\n    usernames: dict[UUID, str],\n):\n    \"\"\"Initialize with shared data collections.\n\n    Args:\n        images: Shared list of images to compute stats from.\n        labels: Shared list of label records.\n        usernames: Mapping of user IDs to usernames.\n    \"\"\"\n    self._images = images\n    self._labels = labels\n    self._usernames = usernames\n</code></pre>"},{"location":"reference/infrastructure/repositories/#import-job-repository_1","title":"Import Job Repository","text":""},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.in_memory.import_job_repository","title":"<code>import_job_repository</code>","text":"<p>In-memory implementation of the import job repository for testing.</p>"},{"location":"reference/infrastructure/repositories/#ulabel.infrastructure.repositories.in_memory.import_job_repository.InMemoryImportJobRepository","title":"<code>InMemoryImportJobRepository()</code>","text":"<p>               Bases: <code>ImportJobRepository</code></p> <p>In-memory import job repository backed by a dictionary.</p> <p>Initialize an empty repository.</p> Source code in <code>src/ulabel/infrastructure/repositories/in_memory/import_job_repository.py</code> <pre><code>def __init__(self) -&gt; None:\n    \"\"\"Initialize an empty repository.\"\"\"\n    self._jobs: dict[UUID, ImportJob] = {}\n</code></pre>"},{"location":"reference/infrastructure/storage/","title":"Storage Services","text":"<p>Implementations of the storage service port for file/image management.</p>"},{"location":"reference/infrastructure/storage/#s3-storage-service","title":"S3 Storage Service","text":""},{"location":"reference/infrastructure/storage/#ulabel.infrastructure.storage.s3_storage_service","title":"<code>s3_storage_service</code>","text":"<p>S3/MinIO storage service implementation.</p> <p>Provides object storage operations (upload, download URLs, listing) using the aioboto3 async S3 client.</p>"},{"location":"reference/infrastructure/storage/#ulabel.infrastructure.storage.s3_storage_service.S3StorageService","title":"<code>S3StorageService(endpoint, access_key, secret_key, bucket, secure=False, public_endpoint=None)</code>","text":"<p>               Bases: <code>StorageService</code></p> <p>S3-compatible storage service using aioboto3.</p> <p>Initialize the S3 storage service.</p> <p>Parameters:</p> Name Type Description Default <code>endpoint</code> <code>str</code> <p>Internal S3/MinIO endpoint URL or hostname.</p> required <code>access_key</code> <code>str</code> <p>AWS/MinIO access key.</p> required <code>secret_key</code> <code>str</code> <p>AWS/MinIO secret key.</p> required <code>bucket</code> <code>str</code> <p>Default bucket name.</p> required <code>secure</code> <code>bool</code> <p>Whether to use HTTPS.</p> <code>False</code> <code>public_endpoint</code> <code>str | None</code> <p>Optional public-facing endpoint for presigned URLs.</p> <code>None</code> Source code in <code>src/ulabel/infrastructure/storage/s3_storage_service.py</code> <pre><code>def __init__(\n    self,\n    endpoint: str,\n    access_key: str,\n    secret_key: str,\n    bucket: str,\n    secure: bool = False,\n    public_endpoint: str | None = None,\n):\n    \"\"\"Initialize the S3 storage service.\n\n    Args:\n        endpoint: Internal S3/MinIO endpoint URL or hostname.\n        access_key: AWS/MinIO access key.\n        secret_key: AWS/MinIO secret key.\n        bucket: Default bucket name.\n        secure: Whether to use HTTPS.\n        public_endpoint: Optional public-facing endpoint for presigned URLs.\n    \"\"\"\n    self._session = aioboto3.Session(\n        aws_access_key_id=access_key,\n        aws_secret_access_key=secret_key,\n    )\n    scheme = \"https\" if secure else \"http\"\n    self._endpoint_url = (\n        endpoint if endpoint.startswith((\"http://\", \"https://\")) else f\"{scheme}://{endpoint}\"\n    )\n    if public_endpoint:\n        self._public_endpoint_url = (\n            public_endpoint\n            if public_endpoint.startswith((\"http://\", \"https://\"))\n            else f\"{scheme}://{public_endpoint}\"\n        )\n    else:\n        self._public_endpoint_url = self._endpoint_url\n    self._bucket = bucket\n</code></pre>"},{"location":"reference/infrastructure/storage/#ulabel.infrastructure.storage.s3_storage_service.S3StorageService.get_presigned_url","title":"<code>get_presigned_url(storage_key, expires_in)</code>  <code>async</code>","text":"<p>Generate a presigned GET URL for an object.</p> <p>Parameters:</p> Name Type Description Default <code>storage_key</code> <code>str</code> <p>The object key in the bucket.</p> required <code>expires_in</code> <code>timedelta</code> <p>How long the URL should remain valid.</p> required <p>Returns:</p> Type Description <code>str</code> <p>A presigned URL string.</p> Source code in <code>src/ulabel/infrastructure/storage/s3_storage_service.py</code> <pre><code>async def get_presigned_url(self, storage_key: str, expires_in: timedelta) -&gt; str:\n    \"\"\"Generate a presigned GET URL for an object.\n\n    Args:\n        storage_key: The object key in the bucket.\n        expires_in: How long the URL should remain valid.\n\n    Returns:\n        A presigned URL string.\n    \"\"\"\n    async with self._session.client(\"s3\", endpoint_url=self._public_endpoint_url) as client:\n        url: str = await client.generate_presigned_url(\n            \"get_object\",\n            Params={\"Bucket\": self._bucket, \"Key\": storage_key},\n            ExpiresIn=int(expires_in.total_seconds()),\n        )\n        return url\n</code></pre>"},{"location":"reference/infrastructure/storage/#ulabel.infrastructure.storage.s3_storage_service.S3StorageService.upload_file","title":"<code>upload_file(key, data, content_type, size, metadata=None)</code>  <code>async</code>","text":"<p>Upload a file to the storage bucket.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>The object key to store the file under.</p> required <code>data</code> <code>bytes</code> <p>The file content as bytes.</p> required <code>content_type</code> <code>str</code> <p>The MIME type of the file.</p> required <code>size</code> <code>int</code> <p>The content length in bytes.</p> required <code>metadata</code> <code>dict[str, str] | None</code> <p>Optional metadata to attach to the object.</p> <code>None</code> Source code in <code>src/ulabel/infrastructure/storage/s3_storage_service.py</code> <pre><code>async def upload_file(\n    self,\n    key: str,\n    data: bytes,\n    content_type: str,\n    size: int,\n    metadata: dict[str, str] | None = None,\n) -&gt; None:\n    \"\"\"Upload a file to the storage bucket.\n\n    Args:\n        key: The object key to store the file under.\n        data: The file content as bytes.\n        content_type: The MIME type of the file.\n        size: The content length in bytes.\n        metadata: Optional metadata to attach to the object.\n    \"\"\"\n    async with self._client() as client:\n        kwargs: dict[str, Any] = dict(\n            Bucket=self._bucket,\n            Key=key,\n            Body=data,\n            ContentType=content_type,\n            ContentLength=size,\n        )\n        if metadata:\n            kwargs[\"Metadata\"] = metadata\n        await client.put_object(**kwargs)\n</code></pre>"},{"location":"reference/infrastructure/storage/#ulabel.infrastructure.storage.s3_storage_service.S3StorageService.upload_file_streaming","title":"<code>upload_file_streaming(key, chunks, content_type, metadata=None)</code>  <code>async</code>","text":"<p>Upload a file by streaming chunks via S3 multipart upload.</p> <p>Buffers chunks until reaching the 5 MB minimum part size before uploading each part. Aborts the upload on any error.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>The object key to store the file under.</p> required <code>chunks</code> <code>AsyncIterator[bytes]</code> <p>Async iterator yielding byte chunks.</p> required <code>content_type</code> <code>str</code> <p>The MIME type of the file.</p> required <code>metadata</code> <code>dict[str, str] | None</code> <p>Optional metadata to attach to the object.</p> <code>None</code> Source code in <code>src/ulabel/infrastructure/storage/s3_storage_service.py</code> <pre><code>async def upload_file_streaming(\n    self,\n    key: str,\n    chunks: AsyncIterator[bytes],\n    content_type: str,\n    metadata: dict[str, str] | None = None,\n) -&gt; None:\n    \"\"\"Upload a file by streaming chunks via S3 multipart upload.\n\n    Buffers chunks until reaching the 5 MB minimum part size before\n    uploading each part. Aborts the upload on any error.\n\n    Args:\n        key: The object key to store the file under.\n        chunks: Async iterator yielding byte chunks.\n        content_type: The MIME type of the file.\n        metadata: Optional metadata to attach to the object.\n    \"\"\"\n    async with self._client() as client:\n        create_kwargs: dict[str, Any] = {\n            \"Bucket\": self._bucket,\n            \"Key\": key,\n            \"ContentType\": content_type,\n        }\n        if metadata:\n            create_kwargs[\"Metadata\"] = metadata\n\n        response = await client.create_multipart_upload(**create_kwargs)\n        upload_id = response[\"UploadId\"]\n\n        parts: list[dict[str, Any]] = []\n        part_number = 1\n        buffer = bytearray()\n\n        try:\n            async for chunk in chunks:\n                buffer.extend(chunk)\n                if len(buffer) &gt;= self._MIN_PART_SIZE:\n                    part = await client.upload_part(\n                        Bucket=self._bucket,\n                        Key=key,\n                        UploadId=upload_id,\n                        PartNumber=part_number,\n                        Body=bytes(buffer),\n                    )\n                    parts.append({\"ETag\": part[\"ETag\"], \"PartNumber\": part_number})\n                    part_number += 1\n                    buffer.clear()\n\n            # Upload remaining buffer as the final part (can be &lt; 5MB)\n            if buffer or not parts:\n                part = await client.upload_part(\n                    Bucket=self._bucket,\n                    Key=key,\n                    UploadId=upload_id,\n                    PartNumber=part_number,\n                    Body=bytes(buffer),\n                )\n                parts.append({\"ETag\": part[\"ETag\"], \"PartNumber\": part_number})\n\n            await client.complete_multipart_upload(\n                Bucket=self._bucket,\n                Key=key,\n                UploadId=upload_id,\n                MultipartUpload={\"Parts\": parts},\n            )\n        except ClientError as e:\n            await client.abort_multipart_upload(\n                Bucket=self._bucket,\n                Key=key,\n                UploadId=upload_id,\n            )\n            if e.response[\"Error\"][\"Code\"] in (\"XMinioStorageFull\", \"NoSuchBucket\"):\n                raise StorageFull(str(e)) from e\n            raise\n        except Exception:\n            await client.abort_multipart_upload(\n                Bucket=self._bucket,\n                Key=key,\n                UploadId=upload_id,\n            )\n            raise\n</code></pre>"},{"location":"reference/infrastructure/storage/#ulabel.infrastructure.storage.s3_storage_service.S3StorageService.head_object","title":"<code>head_object(key)</code>  <code>async</code>","text":"<p>Retrieve metadata for an object without downloading it.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>The object key to check.</p> required <p>Returns:</p> Type Description <code>dict[str, str] | None</code> <p>A dict of metadata if the object exists, None if not found.</p> Source code in <code>src/ulabel/infrastructure/storage/s3_storage_service.py</code> <pre><code>async def head_object(self, key: str) -&gt; dict[str, str] | None:\n    \"\"\"Retrieve metadata for an object without downloading it.\n\n    Args:\n        key: The object key to check.\n\n    Returns:\n        A dict of metadata if the object exists, None if not found.\n    \"\"\"\n    async with self._client() as client:\n        try:\n            response = await client.head_object(Bucket=self._bucket, Key=key)\n            metadata: dict[str, str] = response.get(\"Metadata\", {})\n            return metadata\n        except ClientError as e:\n            if e.response[\"Error\"][\"Code\"] == \"404\":\n                return None\n            raise\n</code></pre>"},{"location":"reference/infrastructure/storage/#ulabel.infrastructure.storage.s3_storage_service.S3StorageService.list_objects","title":"<code>list_objects(prefix)</code>  <code>async</code>","text":"<p>List object keys under a given prefix using pagination.</p> <p>Parameters:</p> Name Type Description Default <code>prefix</code> <code>str</code> <p>The key prefix to filter objects by.</p> required <p>Yields:</p> Type Description <code>AsyncIterator[str]</code> <p>Object keys matching the prefix.</p> Source code in <code>src/ulabel/infrastructure/storage/s3_storage_service.py</code> <pre><code>async def list_objects(self, prefix: str) -&gt; AsyncIterator[str]:\n    \"\"\"List object keys under a given prefix using pagination.\n\n    Args:\n        prefix: The key prefix to filter objects by.\n\n    Yields:\n        Object keys matching the prefix.\n    \"\"\"\n    async with self._client() as client:\n        paginator = client.get_paginator(\"list_objects_v2\")\n        async for page in paginator.paginate(Bucket=self._bucket, Prefix=prefix):\n            for obj in page.get(\"Contents\", []):\n                yield obj[\"Key\"]\n</code></pre>"},{"location":"reference/infrastructure/storage/#ulabel.infrastructure.storage.s3_storage_service.S3StorageService.ensure_bucket","title":"<code>ensure_bucket()</code>  <code>async</code>","text":"<p>Ensure the configured bucket exists, creating it if necessary.</p> Source code in <code>src/ulabel/infrastructure/storage/s3_storage_service.py</code> <pre><code>async def ensure_bucket(self) -&gt; None:\n    \"\"\"Ensure the configured bucket exists, creating it if necessary.\"\"\"\n    async with self._client() as client:\n        try:\n            await client.head_bucket(Bucket=self._bucket)\n        except ClientError:\n            await client.create_bucket(Bucket=self._bucket)\n</code></pre>"},{"location":"reference/infrastructure/storage/#fake-storage-service","title":"Fake Storage Service","text":"<p>Used for testing purposes.</p>"},{"location":"reference/infrastructure/storage/#ulabel.infrastructure.storage.fake_storage_service","title":"<code>fake_storage_service</code>","text":"<p>Fake storage service implementation for testing.</p> <p>Returns predictable URLs and performs no actual I/O.</p>"},{"location":"reference/infrastructure/storage/#ulabel.infrastructure.storage.fake_storage_service.FakeStorageService","title":"<code>FakeStorageService(objects=None)</code>","text":"<p>               Bases: <code>StorageService</code></p> <p>No-op storage service that returns fake URLs for testing.</p> <p>Initialize with optional pre-existing object keys.</p> <p>Parameters:</p> Name Type Description Default <code>objects</code> <code>list[str] | None</code> <p>Optional list of object keys to simulate in the bucket.</p> <code>None</code> Source code in <code>src/ulabel/infrastructure/storage/fake_storage_service.py</code> <pre><code>def __init__(self, objects: list[str] | None = None):\n    \"\"\"Initialize with optional pre-existing object keys.\n\n    Args:\n        objects: Optional list of object keys to simulate in the bucket.\n    \"\"\"\n    self._objects = objects or []\n    self._uploaded: dict[str, dict[str, Any]] = {}\n</code></pre>"},{"location":"tutorials/getting-started/","title":"Getting Started","text":"<p>This tutorial walks you through setting up and running the uLabel backend API.</p>"},{"location":"tutorials/getting-started/#prerequisites","title":"Prerequisites","text":"<ul> <li>Python 3.12+</li> <li>uv package manager</li> <li>PostgreSQL database</li> <li>S3-compatible object storage (e.g., MinIO)</li> </ul>"},{"location":"tutorials/getting-started/#installation","title":"Installation","text":"<p>Clone the repository and install dependencies:</p> <pre><code>git clone &lt;repository-url&gt;\ncd ulabel/backend\nuv sync\n</code></pre>"},{"location":"tutorials/getting-started/#configuration","title":"Configuration","text":"<p>Copy the example environment file and configure it:</p> <pre><code>cp .env.example .env\n</code></pre> <p>Key environment variables:</p> Variable Description <code>DATABASE_URL</code> PostgreSQL connection string <code>S3_ENDPOINT</code> S3/MinIO endpoint URL <code>S3_BUCKET</code> Bucket name for image storage"},{"location":"tutorials/getting-started/#running-the-api","title":"Running the API","text":"<p>Start the development server:</p> <pre><code>make run\n</code></pre> <p>The API will be available at <code>http://localhost:8000</code>.</p>"},{"location":"tutorials/getting-started/#interactive-api-docs","title":"Interactive API Docs","text":"<p>FastAPI automatically generates interactive documentation:</p> <ul> <li>Swagger UI: <code>http://localhost:8000/docs</code></li> <li>ReDoc: <code>http://localhost:8000/redoc</code></li> </ul>"},{"location":"tutorials/getting-started/#database-migrations","title":"Database Migrations","text":"<p>Run database migrations with Alembic:</p> <pre><code>make migrate\n</code></pre>"},{"location":"tutorials/getting-started/#next-steps","title":"Next Steps","text":"<ul> <li>Explore the API Reference for detailed endpoint documentation.</li> <li>Read about the Architecture to understand the project structure.</li> </ul>"},{"location":"tutorials/quick-start/","title":"Quick Start","text":"<p>Get the full uLabel platform running locally in minutes.</p>"},{"location":"tutorials/quick-start/#prerequisites","title":"Prerequisites","text":"<ul> <li>Docker and Docker Compose (v2+)</li> <li>Make</li> </ul>"},{"location":"tutorials/quick-start/#start-the-platform","title":"Start the platform","text":"<pre><code>git clone git@github.com:pablo-campillo/ulabel.git\ncd ulabel\nmake bootstrap\n</code></pre> <p>This single command will:</p> <ol> <li>Build and start all services (backend, frontend, database, MinIO, observability stack)</li> <li>Wait for the database and API to be healthy</li> <li>Run database migrations</li> <li>Create default users (<code>admin</code> and <code>labeler1</code> through <code>labeler10</code>)</li> <li>Download the Dogs vs. Cats dataset (~800 MB) and upload it to MinIO</li> </ol> <p>Note</p> <p>The first run takes a few minutes due to Docker image builds and dataset download.</p> <p>Once finished, the terminal will display all access URLs and credentials.</p>"},{"location":"tutorials/quick-start/#access-the-services","title":"Access the services","text":"Service URL Credentials Frontend http://localhost:5173 See seeded users below Backend API http://localhost:8000 - API Docs (ReDoc) http://localhost:8000/redoc - Documentation http://localhost:8080 - Grafana http://localhost:3000 <code>admin</code> / <code>admin</code> Prometheus http://localhost:9090 - MinIO Console http://localhost:9001 <code>minioadmin</code> / <code>minioadmin</code> <p>Seeded users (no password required):</p> Username Role <code>admin</code> Admin <code>labeler1</code> ... <code>labeler10</code> Labeler"},{"location":"tutorials/quick-start/#user-guide","title":"User guide","text":""},{"location":"tutorials/quick-start/#admin-view","title":"Admin view","text":"<p>Log in as <code>admin</code> at http://localhost:5173 to access the admin panel.</p>"},{"location":"tutorials/quick-start/#projects-list","title":"Projects list","text":"<p>The admin dashboard shows all projects with their progress, number of labels, assigned labelers, and creation date.</p> <p></p>"},{"location":"tutorials/quick-start/#create-a-project","title":"Create a project","text":"<p>Click the + button to create a new project. Set a name, description, labels, and assign labelers.</p> <p></p> <p>You can upload images directly or import from MinIO by prefix (useful for the seeded dataset).</p> <p></p>"},{"location":"tutorials/quick-start/#project-dashboard","title":"Project dashboard","text":"<p>Inside a project, the admin can track progress with real-time statistics: total images, labeled count, completion percentage, daily progress chart with trend line and estimated completion, label distribution, labeler leaderboard, and individual activity charts.</p> <p>Export labels in JSON or CSV format using the buttons at the top.</p> <p></p>"},{"location":"tutorials/quick-start/#labeler-view","title":"Labeler view","text":"<p>Log in as <code>labeler1</code> (or any labeler) to see assigned projects.</p>"},{"location":"tutorials/quick-start/#projects-list_1","title":"Projects list","text":"<p>Each labeler sees their assigned projects with their personal label count and ranking.</p> <p></p>"},{"location":"tutorials/quick-start/#labeling-interface","title":"Labeling interface","text":"<p>Select a project to start labeling. The interface shows the image and the available labels. Click a label (or use keyboard shortcuts <code>1</code>, <code>2</code>, ...) to classify the image and move to the next one.</p> <p></p>"},{"location":"tutorials/quick-start/#grafana-dashboards","title":"Grafana dashboards","text":"<p>Open http://localhost:3000 (credentials: <code>admin</code> / <code>admin</code>). Pre-configured dashboards are available for:</p> <ul> <li>uLabel Overview - Platform-wide metrics</li> <li>uLabel Labeling Activity - Labeling throughput and patterns</li> <li>uLabel Traces &amp; Logs - Request traces and application logs</li> </ul>"},{"location":"tutorials/quick-start/#stop-the-platform","title":"Stop the platform","text":"<pre><code>make down\n</code></pre> <p>To stop and delete all data (database, uploaded images):</p> <pre><code>make down-v\n</code></pre>"}]}